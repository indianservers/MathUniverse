# Phase 1 Workspace Baseline

This folder anchors the recoverable Phase 1 workspace baseline for automated QA.

The original backup branch is `codex/phase-1-baseline-20260611`. The zip artifact in this folder is intentionally small and repo-local so baseline guard tests do not depend on a developer-specific absolute path.

Protected folders:

- `src/pages`
- `src/components`
- `src/workspace`
- `src/data`
- `src/modules`
- `src/visualizations`
