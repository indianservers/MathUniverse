import{ArrowDown,ArrowLeft,ChevronLeft,ChevronRight,MoreVertical,RefreshCcw,X}from"lucide-react";import{useState}from"react";import{useNavigate}from"react-router-dom";import"./divisibility-eight-mobile.css";
const samples=[12376,45126,90712,68435];export default function DivisibilityEightMobileProof(){const nav=useNavigate(),[idx,setIdx]=useState(0),[grouped,setGrouped]=useState(true),[menu,setMenu]=useState(false),value=samples[idx],digits=String(value).split(""),last=value%1000,q=Math.floor(last/8),rem=last%8,ok=rem===0,reset=()=>{setIdx(0);setGrouped(true);setMenu(false)},change=(d:number)=>{setIdx(v=>(v+d+samples.length)%samples.length);setGrouped(false)};return <main className="dr8"><header><button onClick={()=>nav(-1)} aria-label="Back to visual proofs"><ArrowLeft/></button><div><h1>Divisibility Rule for 8</h1><p>41 / 69</p></div><button onClick={()=>setMenu(v=>!v)} aria-label="Lesson options"><MoreVertical/></button></header><section className="dr8-stage"><div className="dr8-digits" aria-label={`Number ${value}`}>{digits.map((d,i)=><span key={i} className={i>=digits.length-3?"active":""}>{d}</span>)}</div><p>Thousands contribute groups of 1000, and 1000 ÷ 8 = 125 exactly.</p><ArrowDown/><button className="last" aria-label="Group last three digits into eights" aria-pressed={grouped} onClick={()=>setGrouped(v=>!v)}>{last}</button><div className={grouped?"eight-groups show":"eight-groups"}>{Array.from({length:Math.min(q,12)},(_,g)=><div key={g}>{Array.from({length:8},(__,i)=><i key={i}/>)}</div>)}{q>12?<b>… {q} groups total</b>:null}{rem?<div className="rem">{Array.from({length:rem},(_,i)=><i key={i}/>)}</div>:null}</div><div className={`dr8-result ${ok?"yes":"no"}`}><strong>{last} ÷ 8 = {q}{rem?` remainder ${rem}`:""}</strong><small>{value} is {ok?"divisible":"not divisible"} by 8</small></div></section><nav className="dr8-controls"><button onClick={()=>change(-1)} aria-label="Previous example"><ChevronLeft/></button><strong>{value}</strong><button onClick={()=>change(1)} aria-label="Next example"><ChevronRight/></button><button onClick={reset} aria-label="Reset rule for 8"><RefreshCcw/></button></nav>{menu?<aside role="dialog"><button onClick={()=>setMenu(false)} aria-label="Close lesson options"><X/></button><h2>Why three digits are enough</h2><p>1000 is divisible by 8, so every higher place contributes no remainder. The final three-digit number has exactly the same remainder as the whole number.</p></aside>:null}</main>}
