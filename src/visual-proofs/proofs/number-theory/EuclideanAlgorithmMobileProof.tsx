import{ArrowLeft,ChevronDown,ChevronUp,MoreVertical,RefreshCcw,X}from"lucide-react";import{useState}from"react";import{useNavigate}from"react-router-dom";import"./euclidean-algorithm-mobile.css";type Step={a:number;b:number;q:number;r:number};const euclid=(x:number,y:number)=>{const s:Step[]=[];while(y){const q=Math.floor(x/y),r=x%y;s.push({a:x,b:y,q,r});x=y;y=r}return s};const samples=[[252,105],[84,30],[391,299],[144,60]];export default function EuclideanAlgorithmMobileProof(){const nav=useNavigate(),[sample,setSample]=useState(0),[stage,setStage]=useState(3),[menu,setMenu]=useState(false),[x,y]=samples[sample],steps=euclid(x,y),gcd=steps.at(-1)?.b??x,reset=()=>{setSample(0);setStage(3);setMenu(false)};return <main className="euc"><header><button onClick={()=>nav(-1)} aria-label="Back to visual proofs"><ArrowLeft/></button><div><h1>Euclidean Algorithm</h1><p>50 / 69</p></div><button onClick={()=>setMenu(v=>!v)} aria-label="Lesson options"><MoreVertical/></button></header><section aria-label={`Euclidean algorithm for ${x} and ${y}`}>{steps.map((s,i)=><article className={i<stage?"shown":""} key={i}><h2>{s.a} = <i>{s.q}</i>·<b>{s.b}</b>{s.r?<> + <em>{s.r}</em></>:null}</h2><div className="bar">{Array.from({length:s.q},(_,j)=><span key={j} style={{flex:s.b}}>{s.b}</span>)}{s.r?<strong style={{flex:s.r}}>{s.r}</strong>:null}</div>{s.r?<small>gcd({s.a},{s.b}) = gcd({s.b},{s.r})</small>:<small>remainder 0 — stop</small>}</article>)}<div className="gcd">GCD = <strong>{gcd}</strong></div></section><nav><button onClick={()=>setStage(v=>Math.max(1,v-1))} aria-label="Previous Euclidean step"><ChevronDown/></button><button onClick={()=>{setSample(v=>(v+1)%samples.length);setStage(1)}} aria-label="Change number pair">{x}, {y}</button><button onClick={()=>setStage(v=>Math.min(steps.length,v+1))} aria-label="Next Euclidean step"><ChevronUp/></button><button onClick={reset} aria-label="Reset Euclidean algorithm"><RefreshCcw/></button></nav>{menu?<aside role="dialog"><button onClick={()=>setMenu(false)} aria-label="Close lesson options"><X/></button><h2>Why the GCD is preserved</h2><p>If a=qb+r, every common divisor of a and b also divides r, and every common divisor of b and r divides a. Replace (a,b) by (b,r) until the remainder is zero.</p></aside>:null}</main>}
