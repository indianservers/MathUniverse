import{ArrowLeft,ChevronLeft,ChevronRight,Eye,MoreVertical,RefreshCcw,X}from"lucide-react";import{useState}from"react";import{useNavigate}from"react-router-dom";import"./divisibility-eleven-mobile.css";const samples=[5728,2728,9185,1331];export default function D(){const nav=useNavigate(),[idx,setIdx]=useState(0),[split,setSplit]=useState(true),[menu,setMenu]=useState(false),value=samples[idx],ds=String(value).split("").map(Number),a=ds[0]+ds[2],b=ds[1]+ds[3],diff=a-b,ok=diff%11===0,reset=()=>{setIdx(0);setSplit(true);setMenu(false)},change=(d:number)=>{setIdx(v=>(v+d+samples.length)%samples.length);setSplit(false)};return <main className="dr11"><header><button onClick={()=>nav(-1)} aria-label="Back to visual proofs"><ArrowLeft/></button><div><h1>Divisibility Rule for 11</h1><p>43 / 69</p></div><button onClick={()=>setMenu(v=>!v)} aria-label="Lesson options"><MoreVertical/></button></header><section><button className="reveal" onClick={()=>setSplit(v=>!v)} aria-label="Split alternating digits" aria-pressed={split}><Eye/></button><div className="d11digits" aria-label={`Alternating digits of ${value}`}>{ds.map((d,i)=><span className={i%2?"minus":"plus"} key={i}>{d}<small>{i%2?"−":"+"}</small></span>)}</div><div className={split?"balance split":"balance"}><article><h2>{ds[0]} + {ds[2]}</h2><strong>{a}</strong></article><b>−</b><article><h2>{ds[1]} + {ds[3]}</h2><strong>{b}</strong></article></div><div className="d11calc">{ds[0]} − {ds[1]} + {ds[2]} − {ds[3]} = {diff}</div><div className={`d11verdict ${ok?"yes":""}`}>{value} is {ok?"divisible":"not divisible"} by 11<small>{diff} {ok?"is":"is not"} a multiple of 11</small></div></section><nav><button onClick={()=>change(-1)} aria-label="Previous example"><ChevronLeft/></button><strong>{value}</strong><button onClick={()=>change(1)} aria-label="Next example"><ChevronRight/></button><button onClick={reset} aria-label="Reset rule for 11"><RefreshCcw/></button></nav>{menu?<aside role="dialog"><button onClick={()=>setMenu(false)} aria-label="Close lesson options"><X/></button><h2>Alternating signs</h2><p>Powers of 10 alternate between +1 and −1 modulo 11, so the alternating digit sum has the same remainder.</p></aside>:null}</main>}
