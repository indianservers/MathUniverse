import{ArrowLeft,Check,ChevronLeft,ChevronRight,MoreVertical,RotateCcw,X}from"lucide-react";import{useState}from"react";import{useNavigate}from"react-router-dom";import"./divisibility-two-mobile.css";
const samples=[4738,2461,9154,7205,6320];const colors=["#396da9","#199ca4","#dc8921","#e55e4d"];
export default function DivisibilityTwoMobileProof(){const nav=useNavigate(),[idx,setIdx]=useState(0),[dropped,setDropped]=useState(true),[menu,setMenu]=useState(false),value=samples[idx],digits=String(value).split(""),last=value%10,even=last%2===0,reset=()=>{setIdx(0);setDropped(true);setMenu(false)};const change=(dir:number)=>{setIdx(v=>(v+dir+samples.length)%samples.length);setDropped(false)};return <main className="dr2"><header><button onClick={()=>nav(-1)} aria-label="Back to visual proofs"><ArrowLeft/></button><div><h1>Divisibility Rule for 2</h1><p>38 / 69</p></div><button onClick={()=>setMenu(v=>!v)} aria-label="Lesson options"><MoreVertical/></button></header><section className="dr2-stage"><div className="digit-tray" aria-label={`Number ${value}`}>{digits.map((d,i)=><button key={i} className={i===digits.length-1?"last":""} onClick={()=>i===digits.length-1&&setDropped(true)} aria-label={i===digits.length-1?`Test final digit ${d}`:`Digit ${d}, place ${digits.length-i}`}><strong>{d}</strong><small>{i===digits.length-1?"ones":10**(digits.length-i-1)}</small></button>)}</div><div className="place-work"><div>{digits.slice(0,-1).map((d,i)=><span key={i}>{d} × {10**(digits.length-i-1)} is even</span>)}</div><b>Only the ones digit remains</b><span className="arrow">↓</span><button className={`drop ${dropped?"active":""}`} onClick={()=>setDropped(v=>!v)} aria-label="Drop final digit into result" aria-pressed={dropped}>{last}</button></div><div className="bins"><article className={even&&dropped?"chosen":""}><Check/><h2>EVEN</h2><p>0, 2, 4, 6, 8</p>{even&&dropped?<strong>{value} ÷ 2 = {value/2}</strong>:null}</article><article className={!even&&dropped?"chosen odd":""}><X/><h2>ODD</h2><p>1, 3, 5, 7, 9</p>{!even&&dropped?<strong>remainder 1</strong>:null}</article></div><div className={`verdict ${dropped?"show":""}`}>{value} is {even?"divisible":"not divisible"} by 2</div></section><nav className="dr2-controls"><button onClick={()=>change(-1)} aria-label="Previous example"><ChevronLeft/></button><strong>{value}</strong><button onClick={()=>change(1)} aria-label="Next example"><ChevronRight/></button><button onClick={reset} aria-label="Reset divisibility rule"><RotateCcw/></button></nav>{menu?<aside role="dialog"><button onClick={()=>setMenu(false)} aria-label="Close lesson options"><X/></button><h2>Why the last digit works</h2><p>Tens, hundreds, and every higher power of 10 are divisible by 2. Therefore the remainder depends only on the ones digit.</p></aside>:null}</main>}
