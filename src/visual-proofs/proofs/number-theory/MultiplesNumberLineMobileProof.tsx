import{ArrowLeft,MoreVertical,RefreshCcw,X}from"lucide-react";import{useState}from"react";import{useNavigate}from"react-router-dom";import"./multiples-number-line-mobile.css";const gcd=(a:number,b:number):number=>b?gcd(b,a%b):a;export default function D(){const nav=useNavigate(),[a,setA]=useState(4),[b,setB]=useState(6),[animate,setAnimate]=useState(true),[menu,setMenu]=useState(false),max=30,lcm=a*b/gcd(a,b),common=Array.from({length:Math.floor(max/lcm)},(_,i)=>(i+1)*lcm),reset=()=>{setA(4);setB(6);setAnimate(true);setMenu(false)};const jumps=(step:number,y:number,color:string)=>Array.from({length:Math.floor(max/step)},(_,i)=>{const x1=20+i*350/max*step,x2=20+(i+1)*350/max*step,m=(x1+x2)/2;return <path key={i} d={`M${x1} ${y} Q${m} ${y-38} ${x2} ${y}`} stroke={color}/>});return <main className="mnl"><header><button onClick={()=>nav(-1)} aria-label="Back to visual proofs"><ArrowLeft/></button><div><h1>Multiples on the Number Line</h1><p>47 / 69</p></div><button onClick={()=>setMenu(v=>!v)} aria-label="Lesson options"><MoreVertical/></button></header><section><svg viewBox="0 0 390 330" role="img" aria-label={`Multiples of ${a} and ${b} through ${max}`}><g className={animate?"jumps on":"jumps"}>{jumps(a,120,"#18b5c5")}{jumps(b,245,"#f36955")}</g><line x1="20" y1="165" x2="370" y2="165"/><g>{Array.from({length:16},(_,i)=>{const n=i*2,x=20+n*350/max,isCommon=common.includes(n);return <g key={n}><line x1={x} y1="158" x2={x} y2="172"/><text x={x} y="191">{n}</text>{n%a===0?<circle cx={x} cy="120" r={isCommon?10:7} className={isCommon?"common":"a"}/>:null}{n%b===0?<circle cx={x} cy="245" r={isCommon?10:7} className={isCommon?"common":"b"}/>:null}</g>})}</g></svg><button className="animate" onClick={()=>setAnimate(v=>!v)} aria-label="Animate number-line jumps" aria-pressed={animate}>jump</button><div className="dials"><label><button onClick={()=>setA(v=>Math.max(2,v-1))} aria-label="Decrease first jump">−</button><strong>{a}</strong><button onClick={()=>setA(v=>Math.min(10,v+1))} aria-label="Increase first jump">+</button></label><label><button onClick={()=>setB(v=>Math.max(2,v-1))} aria-label="Decrease second jump">−</button><strong>{b}</strong><button onClick={()=>setB(v=>Math.min(10,v+1))} aria-label="Increase second jump">+</button></label></div><div className="common-list"><b>Common multiples</b>{common.length?common.map(n=><span key={n}>{n}</span>):<span>none ≤ {max}</span>}</div></section><nav><span>LCM({a},{b}) = <strong>{lcm}</strong></span><button onClick={reset} aria-label="Reset multiples"><RefreshCcw/></button></nav>{menu?<aside role="dialog"><button onClick={()=>setMenu(false)} aria-label="Close lesson options"><X/></button><h2>Repeated equal jumps</h2><p>Landing after each {a}-step jump marks multiples of {a}; the second line marks multiples of {b}. A shared landing point is a common multiple.</p></aside>:null}</main>}
