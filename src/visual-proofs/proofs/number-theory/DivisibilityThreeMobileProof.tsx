import{ArrowDown,ArrowLeft,ChevronLeft,ChevronRight,MoreVertical,RefreshCcw,X}from"lucide-react";import{useState}from"react";import{useNavigate}from"react-router-dom";import"./divisibility-three-mobile.css";
const samples=[4725,3814,9630,1572],colors=["#ee644f","#efa91b","#2caebd","#5a45bd"],places=[1000,100,10,1];
export default function DivisibilityThreeMobileProof(){const nav=useNavigate(),[idx,setIdx]=useState(0),[regrouped,setRegrouped]=useState(true),[menu,setMenu]=useState(false),value=samples[idx],digits=String(value).padStart(4,"0").split("").map(Number),sum=digits.reduce((a,b)=>a+b,0),divisible=sum%3===0,reset=()=>{setIdx(0);setRegrouped(true);setMenu(false)},change=(d:number)=>{setIdx(v=>(v+d+samples.length)%samples.length);setRegrouped(false)};return <main className="dr3"><header><button onClick={()=>nav(-1)} aria-label="Back to visual proofs"><ArrowLeft/></button><div><h1>Divisibility Rule for 3</h1><p>39 / 69</p></div><button onClick={()=>setMenu(v=>!v)} aria-label="Lesson options"><MoreVertical/></button></header><section className="dr3-stage"><div className="place-cards" aria-label={`Place value model for ${value}`}>{digits.map((digit,i)=><article key={places[i]} style={{"--c":colors[i]} as React.CSSProperties}><b>{places[i]}</b><div className="block">{places[i]===1000?"▦":places[i]===100?"▥":places[i]===10?"▮":"▪"}</div><small>{digit} × {places[i]}</small></article>)}</div><ArrowDown className="down"/><button className="regroup" aria-label="Regroup place values modulo 3" aria-pressed={regrouped} onClick={()=>setRegrouped(v=>!v)}><RefreshCcw/></button><div className={regrouped?"remainders shown":"remainders"}>{digits.map((digit,i)=><article key={i} style={{"--c":colors[i]} as React.CSSProperties}><div>{Array.from({length:digit},(_,j)=><i key={j}/>)}</div><strong>{digit}</strong><small>{places[i]} ≡ 1 (mod 3)</small></article>)}</div><div className="digit-sum">{digits.map((d,i)=><span key={i} style={{color:colors[i]}}>{d}</span>).reduce<React.ReactNode[]>((acc,d,i)=>i?[...acc,<b key={`p${i}`}> + </b>,d]:[d],[])} <b>= {sum}</b></div><ArrowDown className="down final"/><div className={`result ${divisible?"yes":"no"}`}>{sum} ÷ 3 = {Math.floor(sum/3)}{divisible?"":" remainder "+sum%3}<small>{value} is {divisible?"divisible":"not divisible"} by 3</small></div></section><nav className="dr3-controls"><button onClick={()=>change(-1)} aria-label="Previous example"><ChevronLeft/></button><strong>{value}</strong><button onClick={()=>change(1)} aria-label="Next example"><ChevronRight/></button><button onClick={reset} aria-label="Reset rule for 3"><RefreshCcw/></button></nav>{menu?<aside role="dialog"><button onClick={()=>setMenu(false)} aria-label="Close lesson options"><X/></button><h2>Why digit sums work</h2><p>10, 100, 1000, and every power of ten leave remainder 1 when divided by 3. So the number and its digit sum have the same remainder.</p></aside>:null}</main>}
