import{ArrowLeft,ChevronDown,ChevronUp,MoreVertical,RefreshCcw,Repeat2,X}from"lucide-react";import{useState}from"react";import{useNavigate}from"react-router-dom";import"./prime-building-blocks-mobile.css";
const primeFactors=(n:number)=>{const out:number[]=[];for(let p=2;p*p<=n;p++)while(n%p===0){out.push(p);n/=p}if(n>1)out.push(n);return out},colors:Record<number,string>={2:"#ed604b",3:"#eda71d",5:"#22aabc",7:"#6753c6"};
export default function PrimeBuildingBlocksMobileProof(){const nav=useNavigate(),[n,setN]=useState(60),[alt,setAlt]=useState(false),[stage,setStage]=useState(2),[menu,setMenu]=useState(false),f=primeFactors(n),a=alt?4:6,b=n/a,unique=[...new Set(f)],reset=()=>{setN(60);setAlt(false);setStage(2);setMenu(false)},blocks=(items:number[])=><div>{items.map((p,i)=><i key={i} style={{background:colors[p]??"#666"}}>{p}</i>)}</div>;return <main className="pbb"><header><button onClick={()=>nav(-1)} aria-label="Back to visual proofs"><ArrowLeft/></button><div><h1>Prime Factorization as Building Blocks</h1><p>48 / 69</p></div><button onClick={()=>setMenu(v=>!v)} aria-label="Lesson options"><MoreVertical/></button></header><section><div className="stone">{n}</div><ChevronDown/><div className={stage>=1?"split shown":"split"}><button onClick={()=>setAlt(v=>!v)} aria-label="Use alternate factor split" aria-pressed={alt}>{a}</button><b>×</b><button onClick={()=>setAlt(v=>!v)} aria-label="Use other factor from alternate split">{b}</button></div><ChevronDown/><div className={stage>=2?"blocks shown":"blocks"} aria-label={`Prime factors of ${n}: ${f.join(", ")}`}>{f.map((p,i)=><i key={i} style={{background:colors[p]??"#666"}}>{p}</i>)}</div><div className="exponents">{unique.map((p,i)=><span key={p}>{i>0?" × ":""}{p}<sup>{f.filter(x=>x===p).length>1?f.filter(x=>x===p).length:""}</sup></span>)}</div><div className="trees"><article><strong>{n}</strong><span>{a} × {b}</span>{blocks(f)}</article><Repeat2/><article><strong>{n}</strong><span>{alt?6:4} × {n/(alt?6:4)}</span>{blocks([...f].reverse())}</article></div><p>Different factor trees finish with the same prime blocks.</p></section><nav><button onClick={()=>setStage(v=>Math.max(0,v-1))} aria-label="Previous factorization stage"><ChevronDown/></button><button onClick={()=>setN(v=>v===60?72:v===72?84:60)} aria-label="Change number">{n}</button><button onClick={()=>setStage(v=>Math.min(2,v+1))} aria-label="Next factorization stage"><ChevronUp/></button><button onClick={reset} aria-label="Reset prime factorization"><RefreshCcw/></button></nav>{menu?<aside role="dialog"><button onClick={()=>setMenu(false)} aria-label="Close lesson options"><X/></button><h2>Unique prime blocks</h2><p>You may split a composite number in different ways, but continuing until every leaf is prime always gives the same multiset of primes.</p></aside>:null}</main>}
