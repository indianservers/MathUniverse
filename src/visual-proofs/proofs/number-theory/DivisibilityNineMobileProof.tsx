import{ArrowDown,ArrowLeft,ChevronLeft,ChevronRight,MoreVertical,RefreshCcw,X}from"lucide-react";import{useState}from"react";import{useNavigate}from"react-router-dom";import"./divisibility-three-mobile.css";const samples=[7452,3814,9639,1575],colors=["#ee644f","#efa11b","#22aeb8","#4c5fbd"],places=[1000,100,10,1];export default function DivisibilityNineMobileProof(){const nav=useNavigate(),[idx,setIdx]=useState(0),[grouped,setGrouped]=useState(true),[menu,setMenu]=useState(false),value=samples[idx],digits=String(value).split("").map(Number),sum=digits.reduce((a,b)=>a+b,0),rem=sum%9,ok=rem===0,reset=()=>{setIdx(0);setGrouped(true);setMenu(false)},change=(d:number)=>{setIdx(v=>(v+d+samples.length)%samples.length);setGrouped(false)};return <main className="dr3"><header><button onClick={()=>nav(-1)} aria-label="Back to visual proofs"><ArrowLeft/></button><div><h1>Divisibility Rule for 9</h1><p>42 / 69</p></div><button onClick={()=>setMenu(v=>!v)} aria-label="Lesson options"><MoreVertical/></button></header><section className="dr3-stage"><div className="place-cards" aria-label={`Digit model for ${value}`}>{digits.map((d,i)=><article key={i} style={{"--c":colors[i]} as React.CSSProperties}><b>{d}</b><div className="block">{d}</div><small>{d} × {places[i]}</small></article>)}</div><ArrowDown className="down"/><button className="regroup" aria-label="Collect digit sum into groups of 9" aria-pressed={grouped} onClick={()=>setGrouped(v=>!v)}><RefreshCcw/></button><div className={grouped?"remainders shown":"remainders"}>{digits.map((d,i)=><article key={i} style={{"--c":colors[i]} as React.CSSProperties}><div>{Array.from({length:d},(_,j)=><i key={j}/>)}</div><strong>{d}</strong><small>{places[i]} ≡ 1 (mod 9)</small></article>)}</div><div className="digit-sum">{digits.join(" + ")} = {sum}</div><ArrowDown className="down final"/><div className={`result ${ok?"yes":"no"}`}>{sum} ÷ 9 = {Math.floor(sum/9)}{rem?` r ${rem}`:""}<small>{value} is {ok?"divisible":"not divisible"} by 9</small></div></section><nav className="dr3-controls"><button onClick={()=>change(-1)} aria-label="Previous example"><ChevronLeft/></button><strong>{value}</strong><button onClick={()=>change(1)} aria-label="Next example"><ChevronRight/></button><button onClick={reset} aria-label="Reset rule for 9"><RefreshCcw/></button></nav>{menu?<aside role="dialog"><button onClick={()=>setMenu(false)} aria-label="Close lesson options"><X/></button><h2>Why digit sums work</h2><p>Every power of 10 leaves remainder 1 when divided by 9. So replacing each place-value block by its digit preserves the remainder.</p></aside>:null}</main>}
