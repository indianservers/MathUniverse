import{ArrowDown,ArrowLeft,ChevronLeft,ChevronRight,MoreVertical,RefreshCcw,X}from"lucide-react";import{useState}from"react";import{useNavigate}from"react-router-dom";import"./divisibility-four-mobile.css";
const samples=[83124,57218,40632,99107];
export default function DivisibilityFourMobileProof(){const nav=useNavigate(),[idx,setIdx]=useState(0),[isolated,setIsolated]=useState(true),[menu,setMenu]=useState(false),value=samples[idx],digits=String(value).split(""),lastTwo=value%100,q=Math.floor(lastTwo/4),rem=lastTwo%4,ok=rem===0,reset=()=>{setIdx(0);setIsolated(true);setMenu(false)},change=(d:number)=>{setIdx(v=>(v+d+samples.length)%samples.length);setIsolated(false)};return <main className="dr4"><header><button onClick={()=>nav(-1)} aria-label="Back to visual proofs"><ArrowLeft/></button><div><h1>Divisibility Rule for 4</h1><p>40 / 69</p></div><button onClick={()=>setMenu(v=>!v)} aria-label="Lesson options"><MoreVertical/></button></header><section className="dr4-stage"><div className="dr4-digits" aria-label={`Number ${value}`}>{digits.map((d,i)=><span key={i} className={i>=digits.length-2?"active":""}><b>{d}</b><small>{10**(digits.length-i-1)}</small></span>)}</div><div className="hundreds"><div>{digits.slice(0,-2).map((d,i)=><i key={i}>{d}×{10**(digits.length-i-1)}</i>)}</div><strong>Each higher place is a multiple of 100—and 100 is divisible by 4.</strong></div><ArrowDown className="dr4-down"/><button className="isolate" onClick={()=>setIsolated(v=>!v)} aria-label="Isolate last two digits" aria-pressed={isolated}>{lastTwo}</button><div className={isolated?"groups show":"groups"} aria-label={`${q} complete groups of 4 and remainder ${rem}`}>{Array.from({length:q},(_,g)=><div key={g}>{Array.from({length:4},(__,i)=><i key={i}/>)}</div>)}{rem?<div className="remainder">{Array.from({length:rem},(_,i)=><i key={i}/>)}</div>:null}</div><div className={`dr4-result ${ok?"yes":"no"}`}><strong>{lastTwo} ÷ 4 = {q}{rem?` remainder ${rem}`:""}</strong><small>{value} is {ok?"divisible":"not divisible"} by 4</small></div></section><nav className="dr4-controls"><button onClick={()=>change(-1)} aria-label="Previous example"><ChevronLeft/></button><strong>{value}</strong><button onClick={()=>change(1)} aria-label="Next example"><ChevronRight/></button><button onClick={reset} aria-label="Reset rule for 4"><RefreshCcw/></button></nav>{menu?<aside role="dialog"><button onClick={()=>setMenu(false)} aria-label="Close lesson options"><X/></button><h2>Why two digits are enough</h2><p>100 is divisible by 4, so thousands, hundreds, and all higher place contributions add no remainder. Test only the final two-digit number.</p></aside>:null}</main>}
