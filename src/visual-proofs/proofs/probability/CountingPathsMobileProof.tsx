import{ArrowLeft,MoreVertical,Pause,Play,RotateCcw,X}from"lucide-react";import{useEffect,useState}from"react";import{useNavigate}from"react-router-dom";import"./counting-paths-mobile.css";
const choose=(n:number,k:number)=>{let v=1;for(let i=1;i<=k;i++)v=v*(n-k+i)/i;return Math.round(v)};
export default function CountingPathsMobileProof(){const nav=useNavigate(),[size,setSize]=useState(4),[reveal,setReveal]=useState(8),[selected,setSelected]=useState<[number,number]>([2,3]),[playing,setPlaying]=useState(false),[menu,setMenu]=useState(false),gap=62,startX=55,startY=500;const count=(i:number,j:number)=>j===0?1:i<=j+1?choose(j+1,i):0;
useEffect(()=>{if(!playing)return;if(reveal>=size*2){setPlaying(false);return}const t=setTimeout(()=>setReveal(v=>v+1),420);return()=>clearTimeout(t)},[playing,reveal,size]);const reset=()=>{setSize(4);setReveal(8);setSelected([2,3]);setPlaying(false);setMenu(false)};
return <main className="paths-mobile"><header><button onClick={()=>nav(-1)} aria-label="Back to visual proofs"><ArrowLeft/></button><h1>Counting Paths</h1><p>29 / 69</p><button onClick={()=>setMenu(v=>!v)} aria-label="Lesson options"><MoreVertical/></button></header><section className="paths-stage"><svg viewBox="0 0 390 650" aria-label={`${2**(size+1)} Pascal paths reach the end on a ${size} by ${size} grid`}>
{Array.from({length:size+1},(_,i)=>Array.from({length:size+1},(__,j)=><g key={`${i}-${j}`}>{i<size?<line className="edge" x1={startX+i*gap} y1={startY-j*gap} x2={startX+(i+1)*gap} y2={startY-j*gap}/>:null}{j<size?<line className="edge" x1={startX+i*gap} y1={startY-j*gap} x2={startX+i*gap} y2={startY-(j+1)*gap}/>:null}</g>))}
<path className="sample-path" d={`M${startX} ${startY}H${startX+gap*size}V${startY-gap*size}H355`}/>
{Array.from({length:size+1},(_,i)=>Array.from({length:size+1},(__,j)=>{const value=count(i,j),visible=i+j<=reveal,active=selected[0]===i&&selected[1]===j;return <g key={`n${i}-${j}`} role="button" tabIndex={0} aria-label={`Node ${i}, ${j} has ${value} paths`} aria-pressed={active} onClick={()=>setSelected([i,j])} className={active?"node active":"node"}><circle cx={startX+i*gap} cy={startY-j*gap} r="18"/><text x={startX+i*gap} y={startY-j*gap+6}>{i===0&&j===0?"S":visible?value:"?"}</text></g>}))}
<g className="node end"><circle cx="355" cy={startY-gap*size} r="20"/><text x="355" y={startY-gap*size+6}>E</text></g>
<g className="recurrence" transform="translate(195 585)"><rect x="-165" y="-45" width="330" height="90" rx="14"/><text x="0" y="-6">paths({selected[0]},{selected[1]}) = {count(selected[0],selected[1])}</text><text className="rule" x="0" y="24">each row adds adjacent incoming counts</text></g>
</svg></section><nav className="paths-controls"><button onClick={reset} aria-label="Reset counting paths"><RotateCcw/></button><button onClick={()=>{const next=Math.max(2,size-1);setSize(next);setReveal(next*2)}} aria-label="Smaller path grid">−</button><span>{size} × {size}</span><button onClick={()=>{const next=Math.min(5,size+1);setSize(next);setReveal(next*2)}} aria-label="Larger path grid">+</button><button className="play" onClick={()=>{if(!playing&&reveal>=size*2)setReveal(0);setPlaying(v=>!v)}} aria-label={playing?"Pause path counting":"Play path counting"}>{playing?<Pause/>:<Play/>}</button></nav>
{menu?<aside role="dialog"><button onClick={()=>setMenu(false)} aria-label="Close lesson options"><X/></button><h2>Two ways into every node</h2><p>A path arrives from the left or from below. Those cases do not overlap, so their counts add. The end count is the binomial coefficient C(2n,n).</p></aside>:null}</main>}
