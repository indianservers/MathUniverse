export type PayoffOutcome={id:number;outcome:number;payoff:number;probability:number};
export const dicePayoffPreset:PayoffOutcome[]=[2,3,4,5,6,7,8,9].map((outcome,index)=>({id:outcome,outcome,payoff:[10,20,30,40,50,40,30,20][index],probability:[.05,.1,.15,.2,.2,.15,.1,.05][index]}));
export function expectedValueSummary(rows:PayoffOutcome[]){const contributions=rows.map(row=>row.payoff*row.probability),probabilityTotal=rows.reduce((sum,row)=>sum+row.probability,0),expectedValue=contributions.reduce((sum,value)=>sum+value,0);return{contributions,probabilityTotal,expectedValue,fairness:expectedValue>0?"Favorable":expectedValue<0?"Unfavorable":"Fair"};}
function rng(seed:number){let state=seed>>>0;return()=>{state=(1664525*state+1013904223)>>>0;return state/4294967296}}
export function simulateExpectedValue(rows:PayoffOutcome[],trials:number,seed=513){const count=Math.max(1,Math.round(trials)),total=rows.reduce((sum,row)=>sum+Math.max(0,row.probability),0),random=rng(seed),recent:PayoffOutcome[]=[],points:Array<{trial:number;average:number}>=[];let sum=0;for(let trial=1;trial<=count;trial+=1){let cursor=random()*total,selected=rows.at(-1)!;for(const row of rows){cursor-=Math.max(0,row.probability);if(cursor<=0){selected=row;break}}sum+=selected.payoff;if(trial>count-10)recent.push(selected);if(trial<=20||trial===count||trial%Math.max(1,Math.floor(count/40))===0)points.push({trial,average:sum/trial});}return{average:sum/count,recent,points};}
