import { describe, expect, it } from "vitest";
import { ordinaryMean, totalWeight, weightedDefault, weightedMean, weightedSum, weightedAnswer } from "./weightedMeanLessonModel";
describe("Weighted mean model", () => { it("computes weighted products and comparison means", () => { expect(weightedSum(weightedDefault)).toBe(52); expect(totalWeight(weightedDefault)).toBe(10); expect(weightedMean(weightedDefault)).toBe(5.2); expect(ordinaryMean(weightedDefault)).toBe(5); }); it("handles zero weights and numeric answer validation", () => { expect(weightedMean([{ value: 4, weight: 0 }])).toBeNull(); expect(weightedAnswer([{ value: 3, weight: 1 }, { value: 5, weight: 2 }, { value: 7, weight: 3 }], "5.6667")).toBe(true); expect(weightedAnswer([{ value: 3, weight: 1 }, { value: 5, weight: 2 }, { value: 7, weight: 3 }], "5")).toBe(false); }); });
