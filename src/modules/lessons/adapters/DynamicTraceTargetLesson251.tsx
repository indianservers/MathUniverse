import { ArrowLeft, ArrowRight, Check, Eye, Hand, Lightbulb, Maximize2, Pause, Play, RotateCcw, Share2, SkipBack, SkipForward, StepBack, StepForward, Target, TriangleAlert } from "lucide-react";
import { useEffect, useMemo, useRef, useState, type PointerEvent as ReactPointerEvent } from "react";
import type { LessonAdapterProps } from "../types";
import "./DynamicTraceTargetLesson251.css";

type Point={x:number;y:number};
const initialPoint={x:1,y:1};
const initialTrace=Array.from({length:12},(_,i)=>({x:.25+i*.068,y:.25+i*.068}));

export default function DynamicTraceTargetLesson251({resetToken,onInteraction}:LessonAdapterProps){
  const [source,setSource]=useState<Point>(initialPoint),[k,setK]=useState(2),[traceOn,setTraceOn]=useState(true),[fade,setFade]=useState(72),[speed,setSpeed]=useState(1),[history,setHistory]=useState<Point[]>(initialTrace),[playing,setPlaying]=useState(false),[tab,setTab]=useState("Interaction + visualization"),[answer,setAnswer]=useState("A"),[feedback,setFeedback]=useState("Great! That’s correct. B′ = (3 · (−2), 3 · 1) = (−6, 3)");
  const image=useMemo(()=>dilate(source,k),[source,k]);
  const commit=(point:Point,nextK=k)=>{const clean={x:clamp(point.x,-6,6),y:clamp(point.y,-4,4)};setSource(clean);if(traceOn)setHistory(items=>[...items,dilate(clean,nextK)].slice(-48));onInteraction()};
  const reset=(notify=true)=>{setSource(initialPoint);setK(2);setTraceOn(true);setFade(72);setSpeed(1);setHistory(initialTrace);setPlaying(false);setTab("Interaction + visualization");setAnswer("A");setFeedback("Great! That’s correct. B′ = (3 · (−2), 3 · 1) = (−6, 3)");if(notify)onInteraction()};
  useEffect(()=>{setSource(initialPoint);setK(2);setTraceOn(true);setFade(72);setSpeed(1);setHistory(initialTrace);setPlaying(false);setTab("Interaction + visualization");setAnswer("A");setFeedback("Great! That’s correct. B′ = (3 · (−2), 3 · 1) = (−6, 3)")},[resetToken]);
  useEffect(()=>{if(!playing)return;const timer=window.setInterval(()=>{setSource(current=>{const angle=Math.atan2(current.y,current.x)+.075*speed,radius=Math.max(1.1,Math.hypot(current.x,current.y));const next={x:clamp(radius*Math.cos(angle),-3,3),y:clamp(radius*Math.sin(angle),-2,2)};if(traceOn)setHistory(items=>[...items,dilate(next,k)].slice(-48));return next})},90);return()=>window.clearInterval(timer)},[playing,speed,k,traceOn]);
  const setScale=(value:number)=>{const next=clamp(value,-4,4);setK(next);if(traceOn)setHistory(items=>[...items,dilate(source,next)].slice(-48));onInteraction()};
  const setImageAxis=(axis:keyof Point,value:number)=>{const nextImage={...image,[axis]:value},denominator=Math.abs(k)<.01?1:k;commit({x:nextImage.x/denominator,y:nextImage.y/denominator})};
  const step=(amount:number)=>{const angle=Math.atan2(source.y,source.x)+amount*.12,radius=Math.max(1.1,Math.hypot(source.x,source.y));commit({x:radius*Math.cos(angle),y:radius*Math.sin(angle)})};
  const check=()=>{const correct=answer==="A";setFeedback(correct?"Great! That’s correct. B′ = (3 · (−2), 3 · 1) = (−6, 3)":"Not yet. Multiply both coordinates of A by the scale factor k = 3.");onInteraction()};
  return <section className="target-trace-page" data-testid="dynamic-geometry-mockup-0308" data-dedicated-lesson="251" data-object-model="dependent-dilation-image-with-temporal-trace" data-source-x={source.x.toFixed(3)} data-source-y={source.y.toFixed(3)} data-image-x={image.x.toFixed(3)} data-image-y={image.y.toFixed(3)} data-scale={k.toFixed(3)} data-trace-count={history.length} data-playing={playing} data-trace-enabled={traceOn}>
    <header className="target-trace-header"><span>Geometry</span><span>Transformations and Loci</span><h1>Dynamic Trace</h1><p>Record motion visually.</p><section><b>♙ Intermediate-Advanced</b><b>ϟ Investigation Lab</b><b>▣ Transformation / Locus Tools</b><b>◷ 6-10 min</b></section><footer><button type="button">⚑ English (English)⌄</button><button type="button" onClick={()=>reset()}><RotateCcw/>Reset</button><button type="button" onClick={()=>{void navigator.clipboard?.writeText(`A ${point(source)} maps to B′ ${point(image)} for k=${fmt(k)}`);onInteraction()}}><Share2/>Share</button><button type="button">↗ Workspace</button></footer></header>
    <nav className="target-trace-tabs">{[[Eye,"Interaction + visualization"],[Lightbulb,"Explain"],[Target,"Examples"],[Target,"Formulas"],[Lightbulb,"Know more"]].map(([Icon,name])=><button type="button" key={String(name)} className={tab===name?"is-active":""} onClick={()=>{setTab(String(name));onInteraction()}}><Icon/>{String(name)}</button>)}</nav>
    <section className="target-trace-stages">{[[Eye,"Observe","A moving point leaves a fading trace of all positions."],[Hand,"Manipulate","Drag A or move sliders to change the transformation."],[Lightbulb,"Notice","The image point B′ obeys a fixed rule. The trace shows the locus of B′."],[Target,"Understand","Dynamic Trace helps you test, observe and prove transformations."]].map(([Icon,title,text])=><article key={String(title)}><Icon/><span><b>{String(title)}</b><p>{String(text)}</p></span></article>)}</section>
    <section className="target-trace-work"><header><div><h2>Work directly on the model</h2><b>Dynamic Trace - reusable 2D geometry engine</b></div><aside><label>Trace <input aria-label="Trace enabled" type="checkbox" checked={traceOn} onChange={e=>{setTraceOn(e.target.checked);onInteraction()}}/></label><label>Fade <input aria-label="Trace fade" type="range" min="10" max="100" value={fade} onChange={e=>{setFade(Number(e.target.value));onInteraction()}}/></label><label>Speed <input aria-label="Trace speed" type="range" min="1" max="4" step="1" value={speed} onChange={e=>{setSpeed(Number(e.target.value));onInteraction()}}/></label><button aria-label="First trace position" type="button" onClick={()=>commit({x:-2,y:-1})}><SkipBack/></button><button aria-label="Previous trace position" type="button" onClick={()=>step(-1)}><StepBack/></button><button aria-label={playing?"Pause trace animation":"Play trace animation"} type="button" onClick={()=>{setPlaying(value=>!value);onInteraction()}}>{playing?<Pause/>:<Play/>}</button><button aria-label="Next trace position" type="button" onClick={()=>step(1)}><StepForward/></button><button aria-label="Last trace position" type="button" onClick={()=>commit({x:2,y:1})}><SkipForward/></button><button type="button" onClick={()=>{setHistory([]);onInteraction()}}>Clear trace</button><button aria-label="Fullscreen trace lab" type="button"><Maximize2/></button></aside></header><div><article className="target-trace-canvas"><TraceGraph source={source} image={image} history={history} fade={fade} onSource={commit}/><footer><span><i/>A (source) <b>{point(source)}</b></span><span><i/>B′ (image) <b>{point(image)}</b></span><span>Trace length: <b>{history.length}</b></span><span className="trace-swatch">new ●●●●● old</span></footer></article><aside className="target-trace-controls"><h2>2D geometry params</h2><nav><button>Point</button><button className="is-active">Trace</button><button>History</button></nav><h3>Transform rule sliders</h3><Control label="A x" min={-6} max={6} value={source.x} onChange={value=>commit({...source,x:value})}/><Control label="A y" min={-4} max={4} value={source.y} onChange={value=>commit({...source,y:value})}/><Control label="B′ x" min={-6} max={6} value={image.x} onChange={value=>setImageAxis("x",value)}/><Control label="B′ y" min={-4} max={4} value={image.y} onChange={value=>setImageAxis("y",value)}/><Control label="Transform k" min={-4} max={4} step={.25} value={k} onChange={setScale}/><section className="target-trace-metrics"><p>Distance<br/><b>AB′</b><strong>{distance(source,image).toFixed(2)}</strong></p><p>Angle<br/><b>A → B′</b><strong>{angle(source,image).toFixed(1)}°</strong></p><p>Midpoint<br/><b>{point({x:(source.x+image.x)/2,y:(source.y+image.y)/2})}</b></p><p>Perimeter<br/><b>(square)</b><strong>{(4*Math.hypot(image.x,image.y)).toFixed(2)}</strong></p></section><section className="target-trace-rule"><h3>Rule summary</h3><b>B′ = (k x<sub>A</sub>, k y<sub>A</sub>)</b><p>Here, k = {fmt(k)}</p></section></aside></div><section className="target-trace-evaluation"><article><h3>What the rule means</h3><p>The image B′ is the dilation of A from the origin by a factor k.</p><p>Every position of A produces a position of B′ that is k times as far from O in the same direction.</p></article><article><h3>Live evaluation</h3><b>A {point(source)} → B′ {point(image)}</b><p><Check/> Satisfies rule: B′ = ({fmt(k)} · {fmt(source.x)}, {fmt(k)} · {fmt(source.y)}) = {point(image)}</p><p>As you move A, the fading purple trace shows the locus of B′.</p></article></section></section>
    <section className="target-trace-learning"><article><h2>Correct rule (formula)</h2><p>Dilation about origin by factor k</p><b>B′ = (kx<sub>A</sub>, ky<sub>A</sub>)</b><p>Domain: x<sub>A</sub> ∈ [-6,6], y<sub>A</sub> ∈ [-4,4], k ∈ [-4,4]</p></article><article><h2>Worked example</h2><p>Given: A (3, -2), k = 2</p><p>Find: B′</p><b>B′ = (kx<sub>A</sub>, ky<sub>A</sub>)<br/>= (2 · 3, 2 · (-2))<br/>= (6, -4)</b><WorkedMini/></article><article><h2><TriangleAlert/>Common misconception</h2><b>Misreading the trace</b><p>The fading purple trace is the locus of B′, not the path of A.</p><p>A moves freely; B′ follows the rule. Do not assume the trace is a line or circle without checking the rule.</p><TraceMini/></article></section>
    <section className="target-trace-practice"><h2>Practice challenge (apply what you learned)</h2><p>Let k = 3. Move A to (-2, 1). What should be the image B′?</p><small>Select the correct image coordinates.</small><section>{[["A","(-6, 3)"],["B","(-5, 4)"],["C","(-2, 3)"],["D","(-3, 1)"]].map(([key,text])=><label key={key}><input aria-label={`Dynamic trace challenge ${key}`} type="radio" name="trace-answer" checked={answer===key} onChange={()=>setAnswer(key)}/>{key} {text}</label>)}</section><button type="button" onClick={check}>Check answer</button><aside><h3>Hint</h3><p>Use the rule B′ = (kx<sub>A</sub>, ky<sub>A</sub>).</p><p>Given: k = 3, A = (-2, 1).</p></aside><article><h3>Instant feedback</h3><p role="status" className={feedback.startsWith("Great")?"correct":"incorrect"}>{feedback}</p></article></section>
    <nav className="target-trace-nav"><a href="/lessons/geometry/250-envelope-of-lines"><ArrowLeft/><span><b>Previous</b>Envelope of Lines</span></a><a href="/lessons/geometry/252-conjecture-testing"><span><b>Next</b>Conjecture Testing</span><ArrowRight/></a></nav>
  </section>
}

function TraceGraph({source,image,history,fade,onSource}:{source:Point;image:Point;history:Point[];fade:number;onSource:(p:Point)=>void}){const ref=useRef<SVGSVGElement>(null),drag=useRef(false),origin={x:300,y:250},scale=42,s=(p:Point)=>({x:origin.x+p.x*scale,y:origin.y-p.y*scale}),a=s(source),b=s(image),world=(e:ReactPointerEvent<SVGSVGElement>)=>{const matrix=ref.current?.getScreenCTM();if(!matrix)return null;const p=new DOMPoint(e.clientX,e.clientY).matrixTransform(matrix.inverse());return{x:round((p.x-origin.x)/scale),y:round((origin.y-p.y)/scale)}};return <svg ref={ref} className="target-trace-graph" viewBox="0 0 600 500" role="img" aria-label="Interactive dilation with source point and dependent traced image" onPointerMove={e=>{if(!drag.current)return;const p=world(e);if(p)onSource(p)}} onPointerUp={()=>drag.current=false}><Grid/><line x1="0" x2="600" y1={origin.y} y2={origin.y} stroke="#334155"/><line x1={origin.x} x2={origin.x} y1="0" y2="500" stroke="#334155"/><line x1={a.x} y1={a.y} x2={b.x} y2={b.y} stroke="#f59e0b" strokeWidth="2" strokeDasharray="5 4"/>{history.map((p,index)=>{const q=s(p),age=(index+1)/Math.max(1,history.length);return <circle key={`history-${index}`} cx={q.x} cy={q.y} r={3+age*2} fill="#8b5cf6" opacity={Math.max(.08,age*fade/100)}/>})}<circle data-testid="dynamic-trace-source-a" data-x={source.x.toFixed(3)} data-y={source.y.toFixed(3)} cx={a.x} cy={a.y} r="8" fill="#1685f8" onPointerDown={e=>{drag.current=true;e.currentTarget.setPointerCapture(e.pointerId)}}/><text x={a.x-18} y={a.y-12} fill="#1685f8" fontWeight="900">A</text><circle data-testid="dynamic-trace-image-b" data-x={image.x.toFixed(3)} data-y={image.y.toFixed(3)} cx={b.x} cy={b.y} r="9" fill="#6d28d9"/><text x={b.x+8} y={b.y-10} fill="#6d28d9" fontWeight="900">B′</text></svg>}
function Grid(){return <g stroke="#e5edf6">{Array.from({length:25},(_,i)=><line key={`v${i}`} x1={i*25} x2={i*25} y1="0" y2="500"/>)}{Array.from({length:21},(_,i)=><line key={`h${i}`} x1="0" x2="600" y1={i*25} y2={i*25}/>)}</g>}
function Control({label,min,max,step=.25,value,onChange}:{label:string;min:number;max:number;step?:number;value:number;onChange:(value:number)=>void}){return <label className="target-trace-control"><span>{label}</span><small>{min}</small><input aria-label={label} type="range" min={min} max={max} step={step} value={value} onChange={e=>onChange(Number(e.target.value))}/><small>{max}</small><input aria-label={`${label} exact value`} type="number" min={min} max={max} step={step} value={value} onChange={e=>onChange(Number(e.target.value))}/></label>}
function WorkedMini(){return <svg viewBox="0 0 180 100"><g stroke="#e2e8f0">{[20,40,60,80,100,120,140,160].map(x=><line key={x} x1={x} x2={x} y1="5" y2="95"/>)}{[20,40,60,80].map(y=><line key={y} x1="5" x2="175" y1={y} y2={y}/>)}</g><line x1="90" x2="90" y1="5" y2="95" stroke="#334155"/><line x1="5" x2="175" y1="50" y2="50" stroke="#334155"/><circle cx="120" cy="70" r="5" fill="#1685f8"/><circle cx="150" cy="90" r="5" fill="#6d28d9"/><text x="124" y="67" fontSize="8">A (3,-2)</text><text x="140" y="84" fontSize="8">B′ (6,-4)</text></svg>}
function TraceMini(){return <svg viewBox="0 0 190 100"><line x1="20" x2="170" y1="85" y2="85" stroke="#64748b"/><path d="M22 80 C62 54 96 68 125 32 S168 18 172 50" fill="none" stroke="#8b5cf6" strokeWidth="2"/><circle cx="22" cy="80" r="5" fill="#1685f8"/><circle cx="172" cy="50" r="6" fill="#6d28d9"/><text x="28" y="78" fontSize="8">A</text><text x="145" y="45" fontSize="8">Trace of B′</text></svg>}
function dilate(p:Point,k:number){return{x:p.x*k,y:p.y*k}}function distance(a:Point,b:Point){return Math.hypot(b.x-a.x,b.y-a.y)}function angle(a:Point,b:Point){return Math.atan2(b.y-a.y,b.x-a.x)*180/Math.PI}function point(p:Point){return`(${fmt(p.x)}, ${fmt(p.y)})`}function fmt(n:number){return Number(n.toFixed(2)).toString()}function round(n:number){return Math.round(n*20)/20}function clamp(n:number,min:number,max:number){return Math.min(max,Math.max(min,Number.isFinite(n)?n:min))}
