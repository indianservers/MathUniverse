import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it,vi}from"vitest";import{lessonCatalog}from"../catalog/lessonCatalog";import ProbabilityLessonAdapter from"./ProbabilityLessonAdapter";
describe("Student t Distribution dedicated surface",()=>{it("routes lesson 529 to its critical-value lab",()=>{const lesson=lessonCatalog.find(item=>item.id===529)!;const html=renderToStaticMarkup(<ProbabilityLessonAdapter lesson={lesson} resetToken={0} onInteraction={vi.fn()}/>);expect(html).toContain('data-testid="probability-mockup-0492"');expect(html).toContain("Explore Student&#x27;s t Distribution");expect(html).toContain("2.228");expect(html).toContain("0.9500")})});
