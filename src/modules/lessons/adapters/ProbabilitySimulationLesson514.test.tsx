import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it,vi}from"vitest";import{lessonCatalog}from"../catalog/lessonCatalog";import ProbabilityLessonAdapter from"./ProbabilityLessonAdapter";
describe("Probability Simulation dedicated surface",()=>{it("routes lesson 514 to its multi-experiment convergence simulator",()=>{const lesson=lessonCatalog.find(item=>item.id===514)!;const html=renderToStaticMarkup(<ProbabilityLessonAdapter lesson={lesson} resetToken={0} onInteraction={vi.fn()}/>);expect(html).toContain('data-testid="probability-mockup-0477"');expect(html).toContain("Start Simulation");expect(html).toContain("Experimental Probability Convergence");expect(html).toContain("600 / 1,000");});});
