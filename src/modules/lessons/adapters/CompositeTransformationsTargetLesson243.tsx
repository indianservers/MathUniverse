import { ArrowLeft, ArrowRight, Grid3X3, Lightbulb, RotateCcw, Shuffle, Target } from "lucide-react";
import { useEffect, useMemo, useRef, useState, type PointerEvent as ReactPointerEvent } from "react";
import type { LessonAdapterProps } from "../types";
import "./CompositeTransformationsTargetLesson243.css";

type Point={x:number;y:number};type Key="A"|"B"|"C";type Triangle=Record<Key,Point>;type Operation={kind:"rotate"|"translate"|"reflect"|"dilate";angle?:number;dx?:number;dy?:number;scale?:number};
const INITIAL:Triangle={A:{x:-2,y:1},B:{x:-1,y:3},C:{x:-3,y:2}},DEFAULT_OPS:[Operation,Operation]=[{kind:"rotate",angle:90},{kind:"translate",dx:3,dy:-1}],KEYS:Key[]=["A","B","C"];
const LIBRARY:[Operation,string,string][]=[[{kind:"translate",dx:3,dy:-1},"Translate","By vector (a,b)"],[{kind:"rotate",angle:90},"Rotate","About a point O"],[{kind:"reflect"},"Reflect","Across line"],[{kind:"dilate",scale:2},"Dilate","Center O, scale k"]];

export default function CompositeTransformationsTargetLesson243({resetToken,onInteraction}:LessonAdapterProps){const [source,setSource]=useState<Triangle>(INITIAL),[ops,setOps]=useState<[Operation,Operation]>(DEFAULT_OPS),[selectedStep,setSelectedStep]=useState<0|1>(0),[grid,setGrid]=useState(true),[axes,setAxes]=useState(true),[practice,setPractice]=useState(["",""]),[feedback,setFeedback]=useState(""),[challenge,setChallenge]=useState(1);const model=useMemo(()=>compose(source,ops),[source,ops]),reverse=useMemo(()=>compose(source,[ops[1],ops[0]]),[source,ops]);const reset=()=>{setSource(INITIAL);setOps(DEFAULT_OPS);setSelectedStep(0);setGrid(true);setAxes(true);setPractice(["",""]);setFeedback("");onInteraction()};useEffect(()=>{setSource(INITIAL);setOps(DEFAULT_OPS);setSelectedStep(0);setGrid(true);setAxes(true);setPractice(["",""]);setFeedback("");setChallenge(1)},[resetToken]);const setOperation=(op:Operation)=>{setOps(current=>current.map((value,index)=>index===selectedStep?op:value) as [Operation,Operation]);onInteraction()};const swap=()=>{setOps(([a,b])=>[b,a]);onInteraction()};const check=()=>{const ok=practice[0]==="rotate"&&practice[1]==="translate";setFeedback(ok?"Correct composition: rotate 90° first, then translate by (3, -1).":"Not yet. Compare the target coordinates with the intermediate and final images.");onInteraction()};return <section className="target-composite-page" data-testid="dynamic-geometry-mockup-0300" data-dedicated-lesson="243" data-object-model="ordered-two-step-affine-composition" data-final-a={fp(model.final.A)} data-order-different={different(model.final,reverse.final)?"true":"false"}>
  <header className="target-composite-header"><div><span>Geometry</span><h1>Composite Transformations</h1><p>Combine mappings. Order matters.</p><footer><b>♙ Intermediate–Advanced</b><b>◇ Investigation Lab⌄</b><b>◷ 10–15 min</b><b>⚑ English (English)⌄</b></footer></div><aside><Lightbulb/><b>Key Insight</b><p>Composition is applied <strong>right-to-left.</strong></p><i>T₂ ∘ T₁ (P) = T₂(T₁(P))</i></aside></header>
  <section className="target-composite-toolbar"><div><h2>Transformation Composer</h2><p>Build a 2-step composition. Drag points or adjust controls to explore.</p></div><label><input type="checkbox" checked={grid} onChange={e=>{setGrid(e.target.checked);onInteraction()}}/><Grid3X3/>Grid</label><label><input type="checkbox" checked={axes} onChange={e=>{setAxes(e.target.checked);onInteraction()}}/>Axes</label><button type="button" onClick={reset}><RotateCcw/>Reset</button></section>
  <section className="target-composite-workspace"><article><div className="target-composite-graph-wrap"><aside><h3>Legend</h3><b>● Original △ABC</b><b>● After T₁: △A'B'C'</b><b>● After T₂: △A''B''C''</b></aside><CompositeGraph source={source} intermediate={model.intermediate} final={model.final} grid={grid} axes={axes} onPoint={(key,p)=>{setSource(s=>({...s,[key]:p}));onInteraction()}}/></div><section className="target-composite-steps"><button type="button" className={selectedStep===0?"is-active":""} onClick={()=>setSelectedStep(0)}><i>1</i><strong>T₁</strong><b>{describe(ops[0])}</b><span>Edit</span></button><button type="button" onClick={swap} aria-label="Swap transformation order"><Shuffle/></button><button type="button" className={selectedStep===1?"is-active":""} onClick={()=>setSelectedStep(1)}><i>2</i><strong>T₂</strong><b>{describe(ops[1])}</b><span>Edit</span></button></section><div className="target-composite-mapping"><b>Mapping</b><i>A {fp(source.A)} → A' {fp(model.intermediate.A)} → A'' {fp(model.final.A)}</i><button type="button">Show all⌄</button></div></article><aside><section><h2>Transformation Library</h2><p>Apply a card into Step 1 or Step 2.</p>{LIBRARY.map(([op,name,copy])=><button draggable type="button" key={name} onClick={()=>setOperation(op)}><i>{name==="Translate"?"→":name==="Rotate"?"⟳":name==="Reflect"?"⌁":"↗"}</i><span><b>{name}</b><small>{copy}</small></span></button>)}</section><section className="target-composite-order"><h2>Order Matters</h2><p>Compare the two orders.</p><div><span><b>T₂ ∘ T₁</b><MiniTriangle points={model.final}/></span><span><b>T₁ ∘ T₂</b><MiniTriangle points={reverse.final}/></span></div><footer>Different final position!</footer></section></aside></section>
  <section className="target-composite-learning"><article><h2>◉ Observe</h2><p>Notice the initial triangle and the intermediate and final images.</p><MiniSequence source={source} mid={model.intermediate} final={model.final}/></article><article><h2>☝ Manipulate</h2><p>Choose, reorder, and adjust transformations. Drag points or change parameters.</p><b>Step 1 &nbsp; {describe(ops[0])}</b><b>Step 2 &nbsp; {describe(ops[1])}</b></article><article><h2>⌕ Notice</h2><p>The final image changes if you swap the order.</p><strong>T₂ ∘ T₁ &nbsp; ≠ &nbsp; T₁ ∘ T₂</strong><MiniSequence source={source} mid={model.intermediate} final={model.final}/></article><article><h2>☆ Rule</h2><p>Composition is applied <b>right-to-left.</b></p><strong>T₂ ∘ T₁(P) = T₂(T₁(P))</strong></article></section>
  <section className="target-composite-explanation"><article><h2>Worked Example</h2><p>Rotate △ABC 90° counter-clockwise about the origin, then translate by (3,−1).</p><table><thead><tr><th>Point</th><th>Original (x,y)</th><th>After Rotate 90° T₁</th><th>After Translate T₂∘T₁</th></tr></thead><tbody>{KEYS.map(k=><tr key={k}><td>{k}</td><td>{fp(source[k])}</td><td>{fp(model.intermediate[k])}</td><td>{fp(model.final[k])}</td></tr>)}</tbody></table></article><article><h2>⚠ Common Misconception</h2><b>Thinking the order doesn't matter.</b><p>Translation then rotation sends A to {fp(reverse.final.A)}, which is not the same as {fp(model.final.A)}.</p><div><span>T₂ ∘ T₁ ✓</span><span>T₁ ∘ T₂ ✕</span></div></article></section>
  <section className="target-composite-practice"><h2><Target/>Your Turn: Practice Challenge</h2><p>Create the target image of △ABC using two transformations from the library.</p><div><span><b>Start</b><MiniTriangle points={INITIAL}/></span><span><b>Target</b><MiniTriangle points={compose(INITIAL,DEFAULT_OPS).final}/></span><article><h3>Your Composition <small>(build it)</small></h3><label>1<select aria-label="First practice transformation" value={practice[0]} onChange={e=>setPractice([e.target.value,practice[1]])}><option value="">Choose first transformation</option><option value="rotate">Rotate 90°</option><option value="translate">Translate (3,-1)</option><option value="reflect">Reflect x-axis</option></select></label><label>2<select aria-label="Second practice transformation" value={practice[1]} onChange={e=>setPractice([practice[0],e.target.value])}><option value="">Choose second transformation</option><option value="rotate">Rotate 90°</option><option value="translate">Translate (3,-1)</option><option value="reflect">Reflect x-axis</option></select></label><button type="button" onClick={()=>setPractice(([a,b])=>[b,a])}><Shuffle/></button></article><aside><h3>Check your work</h3><p>Apply your steps to see if you reach the target.</p><button type="button" onClick={check}>Check</button><button type="button" onClick={()=>{setChallenge(c=>c+1);setPractice(["",""]);setFeedback("");onInteraction()}}><RotateCcw/>New Challenge {challenge}</button></aside></div>{feedback&&<p role="status">{feedback}</p>}</section>
  <nav className="target-composite-nav"><a href="/lessons/geometry/242-matrix-transformation"><ArrowLeft/><span><b>Previous</b>Matrix Transformation</span></a><span><b>Lesson Progress</b><i/><i/><i/><i/><i/><i/><i/><i/></span><a href="/lessons/geometry/244-transformation-mapping"><span><b>Next</b>Transformation Mapping</span><ArrowRight/></a></nav>
  </section>}

function CompositeGraph({source,intermediate,final,grid,axes,onPoint}:{source:Triangle;intermediate:Triangle;final:Triangle;grid:boolean;axes:boolean;onPoint:(k:Key,p:Point)=>void}){const ref=useRef<SVGSVGElement>(null),drag=useRef<Key|null>(null),origin={x:285,y:220},scale=36,s=(p:Point)=>({x:origin.x+p.x*scale,y:origin.y-p.y*scale}),a=mapTriangle(source,s),b=mapTriangle(intermediate,s),c=mapTriangle(final,s);const world=(e:ReactPointerEvent<SVGSVGElement>)=>{const m=ref.current?.getScreenCTM();if(!m)return null;const p=new DOMPoint(e.clientX,e.clientY).matrixTransform(m.inverse());return{x:(p.x-origin.x)/scale,y:(origin.y-p.y)/scale}};return <svg ref={ref} className="target-composite-graph" viewBox="0 0 570 470" role="img" aria-label="Interactive two-step composite transformation graph" onPointerMove={e=>{if(!drag.current)return;const p=world(e);if(p)onPoint(drag.current,{x:clamp(round(p.x),-6,6),y:clamp(round(p.y),-6,6)})}} onPointerUp={()=>drag.current=null}>{grid&&<Grid/>}{axes&&<g stroke="#475569"><line x1="0" x2="570" y1={origin.y} y2={origin.y}/><line x1={origin.x} x2={origin.x} y1="0" y2="470"/></g>}<polygon points={poly(a)} fill="#1685f833" stroke="#1685f8" strokeWidth="2"/><polygon points={poly(b)} fill="#14b8a633" stroke="#0faaa0" strokeWidth="2"/><polygon data-testid="composite-final-triangle" points={poly(c)} fill="#a855f733" stroke="#8b3de2" strokeWidth="2"/>{KEYS.map(k=><g key={k}><circle data-testid={`composite-source-${k.toLowerCase()}`} data-x={source[k].x} data-y={source[k].y} cx={a[k].x} cy={a[k].y} r="6" fill="#1685f8" onPointerDown={e=>{drag.current=k;e.currentTarget.setPointerCapture(e.pointerId)}}/><circle data-testid={`composite-intermediate-${k.toLowerCase()}`} data-x={intermediate[k].x.toFixed(4)} data-y={intermediate[k].y.toFixed(4)} cx={b[k].x} cy={b[k].y} r="6" fill="#0faaa0"/><circle data-testid={`composite-final-${k.toLowerCase()}`} data-x={final[k].x.toFixed(4)} data-y={final[k].y.toFixed(4)} cx={c[k].x} cy={c[k].y} r="6" fill="#8b3de2"/><text x={a[k].x-30} y={a[k].y-9} fill="#0873ca" fontSize="12">{k}{fp(source[k])}</text><text x={b[k].x-30} y={b[k].y-9} fill="#07877f" fontSize="12">{k}'{fp(intermediate[k])}</text><text x={c[k].x+7} y={c[k].y-7} fill="#7c3aed" fontSize="12">{k}''{fp(final[k])}</text></g>)}</svg>}
function Grid(){return <g stroke="#e5edf5">{Array.from({length:18},(_,i)=><line key={`v${i}`} x1={i*36} x2={i*36} y1="0" y2="470"/>)}{Array.from({length:15},(_,i)=><line key={`h${i}`} x1="0" x2="570" y1={i*36} y2={i*36}/>)}</g>}
function MiniTriangle({points}:{points:Triangle}){const xs=KEYS.map(k=>points[k].x),ys=KEYS.map(k=>points[k].y),minX=Math.min(...xs),minY=Math.min(...ys),maxX=Math.max(...xs),maxY=Math.max(...ys),s=(p:Point)=>({x:12+(p.x-minX)/(maxX-minX||1)*55,y:65-(p.y-minY)/(maxY-minY||1)*48}),m=mapTriangle(points,s);return <svg viewBox="0 0 80 75"><polygon points={poly(m)} fill="#60a5fa44" stroke="#1685f8" strokeWidth="2"/></svg>}function MiniSequence({source,mid,final}:{source:Triangle;mid:Triangle;final:Triangle}){return <div className="target-composite-mini"><MiniTriangle points={source}/><MiniTriangle points={mid}/><MiniTriangle points={final}/></div>}
function compose(source:Triangle,ops:[Operation,Operation]){const intermediate=mapTriangle(source,p=>apply(p,ops[0])),final=mapTriangle(intermediate,p=>apply(p,ops[1]));return{intermediate,final}}function apply(p:Point,o:Operation){if(o.kind==="rotate"){const t=(o.angle??90)*Math.PI/180;return{x:p.x*Math.cos(t)-p.y*Math.sin(t),y:p.x*Math.sin(t)+p.y*Math.cos(t)}}if(o.kind==="translate")return{x:p.x+(o.dx??0),y:p.y+(o.dy??0)};if(o.kind==="reflect")return{x:p.x,y:-p.y};return{x:p.x*(o.scale??1),y:p.y*(o.scale??1)}}function describe(o:Operation){if(o.kind==="rotate")return `Rotate ${o.angle}° about O`;if(o.kind==="translate")return `Translate by vector (${o.dx}, ${o.dy})`;if(o.kind==="reflect")return "Reflect across x-axis";return `Dilate by k=${o.scale}`}function mapTriangle(t:Triangle,fn:(p:Point)=>Point){return Object.fromEntries(KEYS.map(k=>[k,fn(t[k])])) as Triangle}function different(a:Triangle,b:Triangle){return KEYS.some(k=>Math.hypot(a[k].x-b[k].x,a[k].y-b[k].y)>.001)}function poly(t:Triangle){return KEYS.map(k=>`${t[k].x},${t[k].y}`).join(" ")}function fp(p:Point){return `(${f(p.x)}, ${f(p.y)})`}function f(n:number){return Number(n.toFixed(2)).toString()}function round(n:number){return Math.round(n*2)/2}function clamp(n:number,min:number,max:number){return Math.min(max,Math.max(min,Number.isFinite(n)?n:min))}
