import { ArrowLeft, ArrowRight, Check, Eye, Lightbulb, Maximize2, Move, RotateCcw, Share2, Sparkles, Target, TriangleAlert } from "lucide-react";
import { useEffect, useMemo, useRef, useState, type PointerEvent as ReactPointerEvent } from "react";
import type { LessonAdapterProps } from "../types";
import "./ConjectureTestingTargetLesson252.css";

type Point={x:number;y:number};type DragKey="a"|"b"|"vector";type Trial={claim:string;holds:boolean;original:number;image:number;midpointShift:number};
const initial={a:{x:0,y:0},b:{x:2,y:1},t:{x:2,y:0}};

export default function ConjectureTestingTargetLesson252({resetToken,onInteraction}:LessonAdapterProps){
  const [a,setA]=useState<Point>(initial.a),[b,setB]=useState<Point>(initial.b),[t,setT]=useState<Point>(initial.t),[claim,setClaim]=useState("A translation preserves the length of segment AB."),[trials,setTrials]=useState<Trial[]>([]),[status,setStatus]=useState<"unverified"|"likely"|"disproved">("unverified"),[tab,setTab]=useState("Interaction + Visualization"),[answer,setAnswer]=useState(""),[feedback,setFeedback]=useState("");
  const model=useMemo(()=>translationModel(a,b,t),[a,b,t]);
  const update=(setter:(point:Point)=>void,point:Point)=>{setter({x:clamp(point.x,-6,6),y:clamp(point.y,-4,4)});onInteraction()};
  const reset=(notify=true)=>{setA(initial.a);setB(initial.b);setT(initial.t);setClaim("A translation preserves the length of segment AB.");setTrials([]);setStatus("unverified");setTab("Interaction + Visualization");setAnswer("");setFeedback("");if(notify)onInteraction()};
  useEffect(()=>{setA(initial.a);setB(initial.b);setT(initial.t);setClaim("A translation preserves the length of segment AB.");setTrials([]);setStatus("unverified");setTab("Interaction + Visualization");setAnswer("");setFeedback("")},[resetToken]);
  const testClaim=()=>{const holds=evaluateClaim(claim,model),trial={claim,holds,original:model.originalLength,image:model.imageLength,midpointShift:model.midpointShift};setTrials(items=>[...items,trial].slice(-12));setStatus(holds?"likely":"disproved");onInteraction()};
  const variedTrial=()=>{const index=trials.length+1,nextA={x:round(-3+(index*1.7)%5),y:round(-2+(index*1.1)%4)},nextB={x:round(nextA.x+1.5+(index%3)),y:round(nextA.y-1+(index%2)*2)},nextT={x:round(-2+(index*.8)%4),y:round(-1.5+(index*.6)%3)},next=translationModel(nextA,nextB,nextT),holds=evaluateClaim(claim,next);setA(nextA);setB(nextB);setT(nextT);setTrials(items=>[...items,{claim,holds,original:next.originalLength,image:next.imageLength,midpointShift:next.midpointShift}].slice(-12));setStatus(holds?"likely":"disproved");onInteraction()};
  const checkChallenge=()=>{const correct=answer==="C";setFeedback(correct?"Correct. Translation moves the midpoint by the same vector, so its coordinates change.":"Not yet. Compare the midpoint before and after a non-zero translation.");onInteraction()};
  return <section className="target-conjecture-page" data-testid="dynamic-geometry-mockup-0309" data-dedicated-lesson="252" data-object-model="measured-translation-conjecture-trial-engine" data-original-length={model.originalLength.toFixed(4)} data-image-length={model.imageLength.toFixed(4)} data-length-difference={model.lengthDifference.toFixed(4)} data-midpoint-shift={model.midpointShift.toFixed(4)} data-orientation-preserved={model.orientationPreserved} data-trial-count={trials.length} data-claim-status={status}>
    <header className="target-conjecture-header"><span>Geometry</span><span>Transformations and Loci</span><h1>Conjecture Testing</h1><p>Test geometric claims.</p><section><b>♙ Intermediate-Advanced</b><b>ϟ Investigation Lab</b><b>▣ Transformation / Locus Tools</b><b>◷ 6-10 min</b></section><footer><button type="button">⚑ English (English)⌄</button><button type="button" onClick={()=>reset()}><RotateCcw/>Reset</button><button type="button" onClick={()=>{void navigator.clipboard?.writeText(`${claim} Status: ${status}`);onInteraction()}}><Share2/>Share</button><button type="button">↗ Workspace</button></footer></header>
    <section className="target-conjecture-stages">{[[Eye,"Observe","See the construction and measurements."],[Move,"Manipulate","Drag the points or use sliders."],[Lightbulb,"Notice","Look for patterns. Test many cases."],[Target,"Understand","State a conjecture. Find a counterexample."]].map(([Icon,title,text],index)=><article key={String(title)}><Icon/><span><b>{String(title)}</b><p>{String(text)}</p></span>{index<3&&<ArrowRight/>}</article>)}</section>
    <nav className="target-conjecture-tabs">{[[Eye,"Interaction + Visualization"],[Lightbulb,"Explain"],[Target,"Examples"],[Sparkles,"Formulas"],[Sparkles,"Conjecture & Evidence"],[Sparkles,"Know more"]].map(([Icon,name])=><button type="button" key={String(name)} className={tab===name?"is-active":""} onClick={()=>{setTab(String(name));onInteraction()}}><Icon/>{String(name)}</button>)}</nav>
    <section className="target-conjecture-work"><header><div><b>INTERACTION + VISUALIZATION</b><h2>Work directly on the model</h2></div><aside><span className={status}>{status==="unverified"?"Awaiting interaction":status==="likely"?"Supported so far":"Counterexample found"}</span><b>{trials.length} trials</b><button aria-label="Fullscreen conjecture lab"><Maximize2/></button></aside></header><h3>Conjecture Testing - reusable 2D geometry engine <span>Transforming construction</span></h3><div className="target-conjecture-model"><article className="target-conjecture-canvas"><ConjectureGraph model={model} onPoint={(key,p)=>key==="a"?update(setA,p):key==="b"?update(setB,p):update(setT,{x:p.x-a.x,y:p.y-a.y})}/></article><aside className="target-conjecture-controls"><h2>2D geometry params</h2><nav><button>Point</button><button className="is-active">Measure</button><button>Verify</button></nav><Control label="A x" min={-6} max={6} value={a.x} onChange={value=>update(setA,{...a,x:value})}/><Control label="A y" min={-4} max={4} value={a.y} onChange={value=>update(setA,{...a,y:value})}/><Control label="B x" min={-6} max={6} value={b.x} onChange={value=>update(setB,{...b,x:value})}/><Control label="B y" min={-4} max={4} value={b.y} onChange={value=>update(setB,{...b,y:value})}/><Control label="Translation x" min={-4} max={4} value={t.x} onChange={value=>update(setT,{...t,x:value})}/><Control label="Translation y" min={-4} max={4} value={t.y} onChange={value=>update(setT,{...t,y:value})}/><section><p>Original<br/><b>AB = {model.originalLength.toFixed(3)}</b></p><p>Image<br/><b>A′B′ = {model.imageLength.toFixed(3)}</b></p><p>Midpoint shift<br/><b>{model.midpointShift.toFixed(3)}</b></p><p>Orientation<br/><b>{model.orientationPreserved?"same":"reversed"}</b></p></section></aside></div><section className="target-conjecture-measure"><h3>Live measurement panel</h3><div><p>A (x, y)<b>{point(a)}</b></p><p>B (x, y)<b>{point(b)}</b></p><p>Translation<b>{point(t)}</b></p><p>Length difference<b>{model.lengthDifference.toFixed(4)}</b></p><p>Midpoint A′B′<b>{point(model.imageMidpoint)}</b></p></div><small>Drag A, B, or the translation handle. All measurements update instantly.</small></section><section className="target-conjecture-test"><article><h3>Conjecture statement</h3><p>Make a claim about a relationship that stays the same (or doesn’t). Then test it with many positions and try to break it.</p><textarea aria-label="Conjecture statement" value={claim} onChange={e=>setClaim(e.target.value)}/><div><button type="button" onClick={testClaim}>Check with current construction</button><button type="button" onClick={variedTrial}>New test case</button><button type="button" onClick={()=>{setTrials([]);setStatus("unverified");onInteraction()}}>Clear</button></div><footer><span>Status</span><b className={status}><i/>{status==="unverified"?"Unverified":status==="likely"?"Likely true (not proved)":"Disproved"}</b><b>Trials: {trials.length}</b></footer></article><article><h3>Relevant rule / formula</h3><b>Translation (vector T = (a,b))</b><p>Every point P(x,y) moves to P′(x+a,y+b).</p><strong>P′ = (x + a, y + b)</strong><p>Distances and angles are preserved.</p><ul><li>d(P₁,P₂) = d(P′₁,P′₂)</li><li>Angle measures remain unchanged.</li></ul><RuleMini/></article></section></section>
    <section className="target-conjecture-learning"><article><h2><TriangleAlert/>Misconception warning</h2><b>Thinking one example proves the conjecture.</b><p>One case can look true even if the claim is false.</p><p>Always test many different positions and try to find a counterexample.</p><footer><b>Good habit:</b> Change both the translation and the original points to explore more cases.</footer></article><article><h2><Check/>Worked example (correct)</h2><p>Claim: A translation preserves distance.</p><p>Test: Translate by T = (2,0).</p><table><thead><tr><th>Point</th><th>Original</th><th>Translated</th><th>Distance from A</th></tr></thead><tbody><tr><td>A</td><td>(0,0)</td><td>A′ (2,0)</td><td>-</td></tr><tr><td>B</td><td>(3,1)</td><td>B′ (5,1)</td><td>3.162</td></tr><tr><td>C</td><td>(-2,4)</td><td>C′ (0,4)</td><td>4.472</td></tr></tbody></table><small>Pattern: Distances stay the same → claim holds so far.</small></article><article><h2><Target/>Quick challenge</h2><p>Is it always true that the midpoint of AB and the midpoint of A′B′ are the same after a translation?</p>{[["A","Always true"],["B","Sometimes true"],["C","Not true"]].map(([key,text])=><label key={key}><input aria-label={`Conjecture challenge ${key}`} type="radio" name="conjecture-answer" checked={answer===key} onChange={()=>setAnswer(key)}/>{text}</label>)}<button type="button" onClick={checkChallenge}>Check my answer</button><button type="button" onClick={()=>{setAnswer("");setFeedback("");onInteraction()}}>Reset challenge</button><p role="status" className={feedback.startsWith("Correct")?"correct":"incorrect"}>{feedback}</p></article></section>
    <nav className="target-conjecture-nav"><a href="/lessons/geometry/251-dynamic-trace"><ArrowLeft/><span><b>Previous</b>Dynamic Trace</span></a><a href="/lessons/geometry/253-exact-proof"><span><b>Next</b>Exact Proof</span><ArrowRight/></a></nav>
  </section>
}

function ConjectureGraph({model,onPoint}:{model:ReturnType<typeof translationModel>;onPoint:(key:DragKey,p:Point)=>void}){const ref=useRef<SVGSVGElement>(null),drag=useRef<DragKey|null>(null),origin={x:320,y:250},scale=42,s=(p:Point)=>({x:origin.x+p.x*scale,y:origin.y-p.y*scale}),world=(e:ReactPointerEvent<SVGSVGElement>)=>{const matrix=ref.current?.getScreenCTM();if(!matrix)return null;const q=new DOMPoint(e.clientX,e.clientY).matrixTransform(matrix.inverse());return{x:round((q.x-origin.x)/scale),y:round((origin.y-q.y)/scale)}};const points={a:s(model.a),b:s(model.b),ap:s(model.ap),bp:s(model.bp)},handle=s({x:model.a.x+model.t.x,y:model.a.y+model.t.y});return <svg ref={ref} className="target-conjecture-graph" viewBox="0 0 640 500" role="img" aria-label="Draggable segment and its translated image for conjecture testing" onPointerMove={e=>{if(!drag.current)return;const p=world(e);if(p)onPoint(drag.current,p)}} onPointerUp={()=>drag.current=null}><Grid/><line x1="0" x2="640" y1={origin.y} y2={origin.y} stroke="#334155"/><line x1={origin.x} x2={origin.x} y1="0" y2="500" stroke="#334155"/><line x1={points.a.x} y1={points.a.y} x2={points.b.x} y2={points.b.y} stroke="#1685f8" strokeWidth="3"/><line x1={points.ap.x} y1={points.ap.y} x2={points.bp.x} y2={points.bp.y} stroke="#f59e0b" strokeWidth="3"/><line x1={points.a.x} y1={points.a.y} x2={points.ap.x} y2={points.ap.y} stroke="#8b5cf6" strokeDasharray="6 4"/><circle data-testid="conjecture-point-a" data-x={model.a.x.toFixed(3)} data-y={model.a.y.toFixed(3)} cx={points.a.x} cy={points.a.y} r="8" fill="#087ccf" onPointerDown={e=>{drag.current="a";e.currentTarget.setPointerCapture(e.pointerId)}}/><circle data-testid="conjecture-point-b" data-x={model.b.x.toFixed(3)} data-y={model.b.y.toFixed(3)} cx={points.b.x} cy={points.b.y} r="8" fill="#087ccf" onPointerDown={e=>{drag.current="b";e.currentTarget.setPointerCapture(e.pointerId)}}/><circle data-testid="conjecture-point-ap" cx={points.ap.x} cy={points.ap.y} r="8" fill="#f59e0b"/><circle data-testid="conjecture-point-bp" cx={points.bp.x} cy={points.bp.y} r="8" fill="#f59e0b"/><circle data-testid="conjecture-vector-handle" cx={handle.x} cy={handle.y} r="12" fill="transparent" stroke="#8b5cf6" strokeWidth="3" onPointerDown={e=>{drag.current="vector";e.currentTarget.setPointerCapture(e.pointerId)}}/>{[[points.a,"A"],[points.b,"B"],[points.ap,"A′"],[points.bp,"B′"]].map(([p,label])=><text key={String(label)} x={(p as {x:number}).x+9} y={(p as {y:number}).y-10} fontWeight="900" fill={String(label).includes("′")?"#d97706":"#087ccf"}>{String(label)}</text>)}</svg>}
function Grid(){return <g stroke="#e5edf6">{Array.from({length:27},(_,i)=><line key={`v${i}`} x1={i*25} x2={i*25} y1="0" y2="500"/>)}{Array.from({length:21},(_,i)=><line key={`h${i}`} x1="0" x2="640" y1={i*25} y2={i*25}/>)}</g>}
function Control({label,min,max,value,onChange}:{label:string;min:number;max:number;value:number;onChange:(value:number)=>void}){return <label className="target-conjecture-control"><span>{label}</span><small>{min}</small><input aria-label={label} type="range" min={min} max={max} step=".25" value={value} onChange={e=>onChange(Number(e.target.value))}/><small>{max}</small><input aria-label={`${label} exact value`} type="number" min={min} max={max} step=".25" value={value} onChange={e=>onChange(Number(e.target.value))}/></label>}
function RuleMini(){return <svg viewBox="0 0 170 80"><circle cx="24" cy="55" r="6" fill="#1685f8"/><circle cx="146" cy="25" r="6" fill="#f59e0b"/><path d="M35 52 Q90 60 135 30" fill="none" stroke="#334155" markerEnd="url(#arrow)"/><text x="15" y="73" fontSize="9">P (x,y)</text><text x="115" y="16" fontSize="9">P′ (x+a,y+b)</text></svg>}
function translationModel(a:Point,b:Point,t:Point){const ap={x:a.x+t.x,y:a.y+t.y},bp={x:b.x+t.x,y:b.y+t.y},midpoint={x:(a.x+b.x)/2,y:(a.y+b.y)/2},imageMidpoint={x:(ap.x+bp.x)/2,y:(ap.y+bp.y)/2},originalLength=distance(a,b),imageLength=distance(ap,bp);return{a,b,t,ap,bp,midpoint,imageMidpoint,originalLength,imageLength,lengthDifference:Math.abs(originalLength-imageLength),midpointShift:distance(midpoint,imageMidpoint),orientationPreserved:true}}
function evaluateClaim(claim:string,model:ReturnType<typeof translationModel>){const text=claim.toLowerCase();if(text.includes("midpoint")&&(text.includes("same")||text.includes("unchanged")||text.includes("fixed")))return model.midpointShift<.001;if(text.includes("origin"))return Math.abs(distance(model.a,{x:0,y:0})-distance(model.ap,{x:0,y:0}))<.001;if(text.includes("orientation"))return model.orientationPreserved;return model.lengthDifference<.001}function distance(a:Point,b:Point){return Math.hypot(b.x-a.x,b.y-a.y)}function point(p:Point){return`(${fmt(p.x)}, ${fmt(p.y)})`}function fmt(n:number){return Number(n.toFixed(2)).toString()}function round(n:number){return Math.round(n*20)/20}function clamp(n:number,min:number,max:number){return Math.min(max,Math.max(min,Number.isFinite(n)?n:min))}
