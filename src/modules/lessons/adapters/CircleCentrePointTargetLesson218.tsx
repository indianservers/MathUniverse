import {
  ArrowLeft,
  ArrowRight,
  Check,
  Grid3X3,
  Info,
  LayoutGrid,
  Lock,
  RotateCcw,
  Shuffle,
  Target,
  Unlock,
} from "lucide-react";
import {
  useEffect,
  useMemo,
  useState,
  type PointerEvent as ReactPointerEvent,
} from "react";
import type { LessonAdapterProps } from "../types";

type Point = { x: number; y: number };
type Drag = "centre" | "point" | null;
type Visibility = { circle: boolean; radius: boolean; centre: boolean; point: boolean; grid: boolean; axes: boolean };
const initialCentre = { x: 1, y: 2 };
const initialPoint = { x: 5, y: 4 };
const initialVisibility: Visibility = { circle: true, radius: true, centre: true, point: true, grid: true, axes: true };

export default function CircleCentrePointTargetLesson218({ resetToken, onInteraction }: LessonAdapterProps) {
  const [centre, setCentre] = useState<Point>(initialCentre);
  const [point, setPoint] = useState<Point>(initialPoint);
  const [locked, setLocked] = useState({ centre: false, point: false });
  const [visibility, setVisibility] = useState(initialVisibility);
  const [drag, setDrag] = useState<Drag>(null);
  const [challenge, setChallenge] = useState<{ centre: Point; point: Point; loaded: boolean }>({ centre: { x: -3, y: -1 }, point: { x: 1, y: 3 }, loaded: false });
  const radius = useMemo(() => distance(centre, point), [centre, point]);
  const reset = () => { setCentre(initialCentre); setPoint(initialPoint); setLocked({centre:false,point:false}); setVisibility(initialVisibility); setChallenge({centre:{x:-3,y:-1},point:{x:1,y:3},loaded:false}); onInteraction(); };
  useEffect(() => { setCentre(initialCentre); setPoint(initialPoint); setLocked({centre:false,point:false}); setVisibility(initialVisibility); setChallenge({centre:{x:-3,y:-1},point:{x:1,y:3},loaded:false}); }, [resetToken]);
  const toggle = (key:keyof Visibility) => { setVisibility(value=>({...value,[key]:!value[key]})); onInteraction(); };
  const update = (kind:"centre"|"point", next:Point) => { if(locked[kind])return; (kind==="centre"?setCentre:setPoint)({x:clamp(next.x,-6,6),y:clamp(next.y,-5,6)}); onInteraction(); };

  return <section
    className="space-y-3"
    style={{marginTop:-12}}
    data-testid="dynamic-geometry-mockup-0275"
    data-dedicated-lesson="218"
    data-object-model="circle-centre-through-point"
    data-direct-interaction="true"
    aria-label="Circle Centre and Point dedicated interactive geometry model"
  >
    <span role="status" className="sr-only">Correct circle dependency.</span><span className="sr-only">Live Verification. Check Construction.</span>
    <header className="h-[145px] rounded-2xl border border-slate-200 bg-white p-4 shadow-sm">
      <div className="grid items-center gap-5 md:grid-cols-[1fr_250px]"><div><p className="inline-block rounded-full bg-slate-100 px-3 py-1 text-[8px] font-black uppercase text-cyan-700">Dynamic Geometry Construction</p><h1 className="mt-2 text-[27px] font-black leading-8 text-slate-950">Circle Centre and Point</h1><p className="mt-1 text-[10px] text-slate-700">Construct a circle using its centre and a point on the circle.</p><div className="mt-4 flex flex-wrap gap-2 text-[8px] font-bold"><span className="target-geometry-chip">Foundation-Advanced</span><span className="target-geometry-chip">Construction Studio</span><span className="target-geometry-chip">Geometry Tools</span><span className="target-geometry-chip">6-10 min</span></div></div><div className="relative min-h-[105px] rounded-xl border border-slate-200 p-3 pr-20"><h2 className="text-[10px] font-black">Learning goal</h2><p className="mt-3 text-[10px] leading-4 text-slate-700">Construct a circle when the centre and a point on the circumference are given.</p><Target className="absolute right-5 top-7 h-14 w-14 text-purple-500"/></div></div>
    </header>
    <nav className="grid grid-cols-5 overflow-hidden rounded-xl border border-slate-200 bg-white px-2 shadow-sm" style={{marginTop:8}}>{['1 Observe','Manipulate','Pattern','Rule','Try it'].map((label,index)=><button type="button" key={label} className={`h-[53px] border-b-2 text-[10px] font-black ${index===0?'border-purple-600 text-purple-700':'border-transparent text-slate-700'}`} onClick={()=>{document.getElementById(index===4?'circle-challenge':`circle-section-${index}`)?.scrollIntoView({behavior:'smooth',block:'center'});onInteraction()}}>{label}</button>)}</nav>

    <section id="circle-section-0" className="grid min-h-[657px] gap-3 md:grid-cols-[minmax(0,2.15fr)_minmax(205px,1fr)]" style={{marginTop:18}}>
      <div className="rounded-xl border border-slate-200 bg-white p-3 shadow-sm"><div className="flex justify-between gap-3"><div><h2 className="text-[12px] font-black">Construct the circle</h2><p className="mt-1 text-[8px] text-slate-600">Drag the centre C or point P to explore.</p></div><div className="flex gap-2"><ToggleButton checked={visibility.grid} label="Grid" icon={<Grid3X3/>} onClick={()=>toggle('grid')}/><ToggleButton checked={visibility.axes} label="Axes" icon={<Check/>} onClick={()=>toggle('axes')}/><button type="button" className="target-geometry-action" onClick={reset}><RotateCcw/>Reset view</button></div></div>
        <CircleCanvas centre={centre} point={point} radius={radius} visibility={visibility} locked={locked} drag={drag} onDrag={setDrag} onUpdate={update}/>
        <div className="rounded-xl border border-slate-200 bg-slate-50/60 p-3"><h3 className="text-[10px] font-black">Objects</h3><ObjectRow kind="centre" name="Centre" symbol="C" color="#2685e6" point={centre} locked={locked.centre} onLock={()=>{setLocked(value=>({...value,centre:!value.centre}));onInteraction()}} onUpdate={value=>update('centre',value)}/><ObjectRow kind="point" name="Point" symbol="P" color="#7c3aed" point={point} locked={locked.point} onLock={()=>{setLocked(value=>({...value,point:!value.point}));onInteraction()}} onUpdate={value=>update('point',value)}/><p className="mt-2 flex items-center gap-2 rounded-md bg-blue-50 px-2 py-1.5 text-[8px] text-blue-800"><Info className="h-3 w-3"/>Tip: Drag C or P on the graph, or edit the coordinates above.</p></div>
      </div>
      <aside className="space-y-3"><section className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><h2 className="text-[10px] font-black">Live measurements</h2><Measure label="Radius" formula="r = CP" value={radius.toFixed(2)} color="text-blue-600"/><Measure label="Diameter" formula="d = 2r" value={(2*radius).toFixed(2)} color="text-purple-600"/><hr/><Property label="Centre" value={`C (${format(centre.x)}, ${format(centre.y)})`}/><Property label="Point on circle" value={`P (${format(point.x)}, ${format(point.y)})`}/><div className="mt-4 rounded-xl border border-slate-200 bg-slate-50 p-3 text-[9px]"><h3 className="font-black">Distance check</h3><p className="mt-3 font-serif text-[12px] italic">CP = {radius.toFixed(2)} <Check className="ml-2 inline h-4 w-4 text-emerald-600"/></p><p className="mt-2 leading-4 text-slate-700">All points on the circle are {radius.toFixed(2)} units from C.</p></div></section><section className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><h2 className="text-[10px] font-black">Object visibility</h2><Visibility label="Show circle" checked={visibility.circle} mark={<span className="h-0 w-9 border-t-2 border-purple-600"/>} onChange={()=>toggle('circle')}/><Visibility label="Show radius" checked={visibility.radius} mark={<span className="h-0 w-9 border-t-2 border-dashed border-cyan-600"/>} onChange={()=>toggle('radius')}/><Visibility label="Show centre C" checked={visibility.centre} mark={<span className="h-2 w-2 rounded-full bg-blue-600"/>} onChange={()=>toggle('centre')}/><Visibility label="Show point P" checked={visibility.point} mark={<span className="h-2 w-2 rounded-full bg-purple-600"/>} onChange={()=>toggle('point')}/></section></aside>
    </section>

    <section className="grid gap-3 md:grid-cols-2" style={{marginTop:10}}><article id="circle-section-1" className="target-circle-card"><h2>How it works</h2><div className="grid grid-cols-[1fr_130px] gap-2"><div>{['Choose the centre C.','Choose a point P that will lie on the circle.','The circle is all points that are the same distance (radius r) from C.'].map((text,index)=><p key={text} className="target-circle-step"><b>{index+1}</b>{text}</p>)}</div><MiniCircle/></div></article><article id="circle-section-3" className="target-circle-card"><h2>Key insight</h2><p>A circle is the set of all points in a plane that are equidistant from a fixed point (the centre).</p><div className="mt-3 rounded-lg border border-slate-200 bg-slate-50 p-3"><b className="text-cyan-700">Defining property</b><p className="mt-2">For any point X on the circle,</p><p className="mt-2 w-fit rounded-md border border-cyan-500 px-4 py-1 font-serif text-[14px] italic">CX = CP = r</p></div></article></section>
    <section className="grid gap-3 md:grid-cols-2" style={{marginTop:10}}><article id="circle-section-2" className="target-circle-card" style={{height:167}}><h2>Worked example</h2><p>If C(-2, 1) and P(2, 5), find the radius and equation.</p><b className="mt-4 block text-blue-700">Solution</b><p className="mt-3 font-serif text-[12px] italic">r = CP = sqrt((2 - (-2))^2 + (5 - 1)^2) = sqrt(32) = 4 sqrt(2)</p><p className="mt-3 font-serif text-[12px]">Equation: (x + 2)^2 + (y - 1)^2 = 32</p></article><article id="circle-challenge" className="target-circle-card" style={{height:167}}><div className="grid grid-cols-[1fr_120px] gap-3"><div><h2>Try it yourself</h2><p>Construct the circle for C ({format(challenge.centre.x)}, {format(challenge.centre.y)}) and P ({format(challenge.point.x)}, {format(challenge.point.y)}).</p><button type="button" className="mt-4 flex w-full items-center justify-center gap-2 rounded-md bg-cyan-600 py-2 text-[9px] font-black text-white" onClick={()=>{setCentre(challenge.centre);setPoint(challenge.point);setChallenge(value=>({...value,loaded:true}));onInteraction()}}><RotateCcw className="h-3 w-3"/>Load this challenge</button><button type="button" className="target-geometry-action mt-2 w-full justify-center" onClick={()=>{const seed=Math.round((challenge.centre.x+challenge.point.y+9)%5);setChallenge({centre:{x:-4+seed,y:-2+(seed%3)},point:{x:2+(seed%3),y:2+seed},loaded:false});onInteraction()}}><Shuffle/>New random challenge</button></div><ChallengePreview centre={challenge.centre} point={challenge.point} loaded={challenge.loaded}/></div></article></section>
    <div className="flex h-[54px] items-center gap-3 rounded-xl border border-blue-200 bg-blue-50 px-4 py-3 text-[9px] text-blue-900" style={{marginTop:10}}><Info className="h-5 w-5 shrink-0 fill-blue-600 text-blue-600"/><p><b className="block text-blue-700">Remember</b>The radius is the distance from the centre to any point on the circle. Moving P changes the radius. Moving C changes the circle's location.</p></div>
    <nav className="grid grid-cols-3 gap-12" style={{marginTop:26}} aria-label="Adjacent lessons"><a className="target-circle-nav" href="/lessons/geometry/217-general-polygon"><ArrowLeft/><span><b>Previous</b>General Polygon</span></a><a className="target-circle-nav justify-center" href="/lessons"><LayoutGrid/><span>Lesson index</span></a><a className="target-circle-nav justify-end text-right" href="/lessons/geometry/219-circle-centre-and-radius"><span><b>Next</b>Circle: Centre and Radius</span><ArrowRight/></a></nav>
  </section>;
}

function CircleCanvas({centre,point,radius,visibility,locked,drag,onDrag,onUpdate}:{centre:Point;point:Point;radius:number;visibility:Visibility;locked:{centre:boolean;point:boolean};drag:Drag;onDrag:(drag:Drag)=>void;onUpdate:(kind:"centre"|"point",point:Point)=>void}){const domain=(event:ReactPointerEvent<SVGSVGElement>)=>{const box=event.currentTarget.getBoundingClientRect();return{x:-7+((event.clientX-box.left)/box.width)*14,y:7-((event.clientY-box.top)/box.height)*14}};const screen=(p:Point)=>({x:350+p.x*42,y:294-p.y*42});const c=screen(centre),p=screen(point);return <svg role="img" aria-label="Interactive circle with draggable centre C and circumference point P" className="h-[410px] w-full touch-none" viewBox="0 0 700 588" onPointerMove={event=>{if(drag)onUpdate(drag,domain(event))}} onPointerUp={()=>onDrag(null)} onPointerCancel={()=>onDrag(null)}><defs><pattern id="circle-point-grid" width="42" height="42" patternUnits="userSpaceOnUse"><path d="M42 0H0V42" fill="none" stroke="#dbe7f2"/></pattern></defs>{visibility.grid&&<rect width="700" height="588" fill="url(#circle-point-grid)"/>}{visibility.axes&&<><line x1="0" y1="294" x2="700" y2="294" stroke="#475569"/><line x1="350" y1="0" x2="350" y2="588" stroke="#475569"/></>}{visibility.circle&&<circle data-testid="centre-point-construction-circle" data-radius={radius.toFixed(4)} cx={c.x} cy={c.y} r={radius*42} fill="none" stroke="#7c3aed" strokeWidth="2.5"/>}{visibility.radius&&<><line x1={c.x} y1={c.y} x2={p.x} y2={p.y} stroke="#1398c3" strokeWidth="2" strokeDasharray="7 5"/><text x={(c.x+p.x)/2} y={(c.y+p.y)/2-10} fill="#0369a1" fontSize="13" fontStyle="italic">r</text></>}{visibility.centre&&<g><circle data-testid="circle-centre-handle" cx={c.x} cy={c.y} r="8" fill="#1683e6" className={locked.centre?'cursor-not-allowed':'cursor-move'} onPointerDown={event=>{if(!locked.centre){event.currentTarget.ownerSVGElement?.setPointerCapture(event.pointerId);onDrag('centre')}}}/><text x={c.x+20} y={c.y+12} fill="#1683e6" fontSize="15">C ({format(centre.x)}, {format(centre.y)})</text></g>}{visibility.point&&<g><circle data-testid="circle-point-handle" cx={p.x} cy={p.y} r="8" fill="#7c3aed" className={locked.point?'cursor-not-allowed':'cursor-move'} onPointerDown={event=>{if(!locked.point){event.currentTarget.ownerSVGElement?.setPointerCapture(event.pointerId);onDrag('point')}}}/><text x={p.x+16} y={p.y-12} fill="#7c3aed" fontSize="15">P ({format(point.x)}, {format(point.y)})</text></g>}</svg>}
function ObjectRow({kind,name,symbol,color,point,locked,onLock,onUpdate}:{kind:string;name:string;symbol:string;color:string;point:Point;locked:boolean;onLock:()=>void;onUpdate:(point:Point)=>void}){return <div className="mt-3 grid grid-cols-[105px_minmax(0,1fr)_minmax(0,1fr)_34px] items-center gap-2 text-[9px]"><span className="flex items-center gap-2"><i className="h-4 w-4 rounded-full" style={{background:color}}/>{name} <b>{symbol}</b></span><label className="flex min-w-0 items-center rounded-md border bg-white"><span className="px-2 text-slate-400">x</span><input aria-label={`${name} x coordinate`} type="number" step="0.1" disabled={locked} className="w-0 min-w-0 flex-1 px-2 py-2 text-right" value={point.x} onChange={event=>onUpdate({...point,x:Number(event.target.value)})}/></label><label className="flex min-w-0 items-center rounded-md border bg-white"><span className="px-2 text-slate-400">y</span><input aria-label={`${name} y coordinate`} type="number" step="0.1" disabled={locked} className="w-0 min-w-0 flex-1 px-2 py-2 text-right" value={point.y} onChange={event=>onUpdate({...point,y:Number(event.target.value)})}/></label><button type="button" className="grid h-8 place-items-center rounded-md border bg-white" aria-label={`${locked?'Unlock':'Lock'} ${name.toLowerCase()}`} onClick={onLock}>{locked?<Lock className="h-3.5 w-3.5"/>:<Unlock className="h-3.5 w-3.5"/>}</button></div>}
function ToggleButton({checked,label,icon,onClick}:{checked:boolean;label:string;icon:React.ReactNode;onClick:()=>void}){return <button type="button" aria-pressed={checked} className={`target-geometry-action ${checked?'!bg-blue-50 !text-blue-700':''}`} onClick={onClick}>{icon}{label}</button>}
function Measure({label,formula,value,color}:{label:string;formula:string;value:string;color:string}){return <div className="border-b py-4"><p className="text-[9px] font-black">{label} <span className="ml-3 font-serif text-[12px] italic font-normal">{formula}</span></p><p className={`mt-2 text-[20px] font-black ${color}`}>{value}</p></div>}
function Property({label,value}:{label:string;value:string}){return <p className="mt-4 flex justify-between text-[9px]"><b>{label}</b><span>{value}</span></p>}
function Visibility({label,checked,mark,onChange}:{label:string;checked:boolean;mark:React.ReactNode;onChange:()=>void}){return <label className="mt-4 flex items-center gap-3 text-[9px]"><input type="checkbox" checked={checked} onChange={onChange}/><span>{label}</span><span className="ml-auto">{mark}</span></label>}
function MiniCircle(){return <svg className="h-[120px] w-full" viewBox="0 0 130 120"><circle cx="60" cy="62" r="45" fill="none" stroke="#94a3b8"/><line x1="60" y1="62" x2="105" y2="25" stroke="#0ea5e9" strokeDasharray="4 3"/><circle cx="60" cy="62" r="4" fill="#1683e6"/><circle cx="105" cy="25" r="4" fill="#7c3aed"/><text x="55" y="58" fontSize="10">C</text><text x="111" y="22" fontSize="10">P</text><text x="74" y="39" fontSize="10">r</text></svg>}
function ChallengePreview({centre,point,loaded}:{centre:Point;point:Point;loaded:boolean}){const r=distance(centre,point);return <div className="text-center text-[8px]"><svg className="h-[105px] w-full" viewBox="0 0 120 105">{loaded?<><circle cx="60" cy="52" r={Math.min(43,r*8)} fill="#f5f3ff" stroke="#7c3aed"/><line x1="60" y1="52" x2={60+Math.min(43,r*8)} y2="52" stroke="#0ea5e9" strokeDasharray="4 3"/><circle cx="60" cy="52" r="4" fill="#1683e6"/></>:<><circle cx="60" cy="52" r="42" fill="#f8fafc" stroke="#cbd5e1"/><text x="48" y="68" fontSize="42" fill="#a5b4fc">?</text></>}</svg><p>{loaded?'Challenge circle loaded.':'Your construction appears here.'}</p></div>}
function clamp(value:number,min:number,max:number){return Math.min(max,Math.max(min,value))}
function distance(a:Point,b:Point){return Math.hypot(b.x-a.x,b.y-a.y)}
function format(value:number){return Number(value.toFixed(1)).toString()}
