import { ArrowLeft, ArrowRight, Brain, Check, Eye, Hand, Lightbulb, RefreshCw, Sparkles, TriangleAlert } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import type { LessonAdapterProps } from "../../types";
import "./EliminateVariablesTargetLesson436.css";

type Equation={a:number;b:number;c:number};const ROWS:[Equation,Equation]=[{a:2,b:3,c:-2},{a:1,b:-1,c:4}];const PRACTICE:[Equation,Equation]=[{a:3,b:2,c:5},{a:5,b:-2,c:-1}];
export default function EliminateVariablesTargetLesson436({resetToken,onInteraction}:LessonAdapterProps){
 const [multipliers,setMultipliers]=useState<[number,number]>([1,-2]),[autoCheck,setAutoCheck]=useState(true),[actions,setActions]=useState(0),[practiceMultipliers,setPracticeMultipliers]=useState<[number,number]>([5,-3]),[practiceAnswer,setPracticeAnswer]=useState(""),[feedback,setFeedback]=useState<"idle"|"correct"|"incorrect">("idle");
 useEffect(()=>{setMultipliers([1,-2]);setAutoCheck(true);setActions(0);setPracticeMultipliers([5,-3]);setPracticeAnswer("");setFeedback("idle")},[resetToken]);
 const model=useMemo(()=>eliminate(ROWS,multipliers),[multipliers]),practiceModel=useMemo(()=>eliminate(PRACTICE,practiceMultipliers),[practiceMultipliers]);const act=(run:()=>void)=>{run();setActions(v=>v+1);onInteraction()};const setMultiplier=(index:number,value:number)=>act(()=>setMultipliers(current=>current.map((item,i)=>i===index?value:item) as [number,number]));const setPracticeMultiplier=(index:number,value:number)=>act(()=>setPracticeMultipliers(current=>current.map((item,i)=>i===index?value:item) as [number,number]));const checkPractice=()=>act(()=>setFeedback(Math.abs(Number(practiceAnswer)-practiceModel.y)<1e-8?"correct":"incorrect"));
 return <section className="elim436-page" data-testid="symbolic-cas-mockup-0342" data-dedicated-lesson="436" data-object-model="row-multiplier-matrix-addition-reduction-back-substitution-practice" data-multipliers={multipliers.join(",")} data-result={`${model.sum.a},${model.sum.b},${model.sum.c}`} data-solution={`${model.x},${model.y}`} data-eliminated={model.eliminated} data-actions={actions} data-practice-result={`${practiceModel.sum.a},${practiceModel.sum.b},${practiceModel.sum.c}`} data-practice-feedback={feedback}>
  <h2 className="sr-only">Eliminate Variables</h2><section className="elim436-flow">{[[Eye,"Observe","A system with one variable can become a single equation using multipliers and elimination."],[Hand,"Manipulate","Apply row multipliers so the coefficients of one variable are opposites."],[Sparkles,"Notice","One variable disappears, leaving a simpler equation."],[Brain,"Understand","Solve the reduced equation, then back-substitute and verify."]].map(([Icon,title,text],i)=><article key={String(title)}><Icon/><h2>{String(title)}</h2><p>{String(text)}</p>{i<3&&<ArrowRight/>}</article>)}</section>
  <section className="elim436-lab"><header><h2>Work directly on the model</h2><aside><span>Live: changes update instantly</span><label><input data-lesson-control="eliminate-auto-check" type="checkbox" checked={autoCheck} onChange={e=>act(()=>setAutoCheck(e.target.checked))}/> Auto-check</label></aside></header><div className="elim436-top"><article className="system"><h3>SYSTEM OF EQUATIONS</h3><p><b>R1</b>{equation(ROWS[0])}</p><p><b>R2</b>{equation(ROWS[1])}</p></article><ArrowRight/><Matrix title="ELIMINATION MATRIX" rows={ROWS} multipliers={multipliers} onMultiplier={setMultiplier}/><span className="plus">+</span><Result model={model}/></div><p className="tip"><Lightbulb/> Tip: Make the x-coefficients opposites ({model.scaled[0].a} and {model.scaled[1].a}) so they cancel when added.</p><div className="elim436-bottom"><article><h3>REDUCED EQUATION (in y)</h3><strong>{term(model.sum.a,"x")} {signed(model.sum.b,"y")} = {fmt(model.sum.c)}</strong><ArrowRight/><output>y = {fmt(model.y)}</output></article><ArrowRight/><article><h3>SOLUTION VERIFICATION</h3><b>Back-substitute y = {fmt(model.y)}</b><p>Eq. 1: {verify(ROWS[0],model.x,model.y)} {autoCheck&&<Check/>}</p><p>Eq. 2: {verify(ROWS[1],model.x,model.y)} {autoCheck&&<Check/>}</p></article><article><h3>FINAL SOLUTION</h3><strong>x = {fmt(model.x)}<br/>y = {fmt(model.y)}</strong><span><Check/> Verified</span></article></div></section>
  <section className="elim436-explain"><article><h2>How it works (Row operation rule)</h2><p>If R2 =&gt; kR2, then R1 + R2 =&gt; R1 + kR2. This preserves equality and gives a new equation.</p><div><b>R1</b><i>+</i><span>(k x R2)</span><i>=</i><strong>New equation<br/>(one variable)</strong></div></article><article><h2><TriangleAlert/> Common Misconception</h2><p>Multiplying a row and forgetting to add the rows before solving.</p><p><b>Fix:</b> Always add the rows after applying the multiplier. That is how a variable is eliminated.</p></article></section>
  <section className="elim436-worked"><h2>Worked Example (this lesson)</h2><div><b>Given:</b><p>{equation(ROWS[0])}<br/>{equation(ROWS[1])}</p></div><ArrowRight/><div><b>Elimination steps:</b><ol><li>Multiply R2 by -2.</li><li>Add R1 + (-2)R2.</li><li>Solve for y, then back-substitute.</li></ol></div><ArrowRight/><div><b>Result:</b><p>y = {fmt(model.y)}<br/>x = {fmt(model.x)}</p><span><Check/> Verified</span></div><ArrowRight/><div><b>Check:</b><p>{verify(ROWS[0],model.x,model.y)}<br/>{verify(ROWS[1],model.x,model.y)}</p></div></section>
  <section className="elim436-practice"><header><h2>Your Turn: Practice Challenge</h2><span>1 of 1 challenge</span><button data-lesson-control="eliminate-practice-check" onClick={checkPractice}><Check/> Check Answer</button></header><div><article><h3>Eliminate x and solve for y.</h3><p>{equation(PRACTICE[0])}<br/>{equation(PRACTICE[1])}</p></article><Matrix title="Your elimination matrix" rows={PRACTICE} multipliers={practiceMultipliers} onMultiplier={setPracticeMultiplier}/><span className="plus">+</span><Result model={practiceModel}/></div><label>Then solve and enter your answer. <span>y = <input data-lesson-control="eliminate-practice-y" aria-label="Practice y answer" value={practiceAnswer} onChange={e=>setPracticeAnswer(e.target.value)}/></span><button data-lesson-control="eliminate-practice-verify" onClick={checkPractice}><RefreshCw/> Verify Back-Substitution</button></label>{feedback!=="idle"&&<em className={feedback}>{feedback==="correct"?`Correct: y = ${fmt(practiceModel.y)}.`:"Recalculate the scaled rows before adding."}</em>}</section>
  <nav className="elim436-nav"><a href="/lessons/symbolic-mathematics/435-solve-systems"><ArrowLeft/><span><small>Previous</small>Solve Systems</span></a><a href="/lessons/symbolic-mathematics/437-partial-fractions"><span><small>Next</small>Partial Fractions</span><ArrowRight/></a></nav>
 </section>
}
function Matrix({title,rows,multipliers,onMultiplier}:{title:string;rows:[Equation,Equation];multipliers:[number,number];onMultiplier:(index:number,value:number)=>void}){return <article className="matrix"><h3>{title}</h3><header><span>Multiplier</span><span>x-coef</span><span>y-coef</span><span>Constant</span></header>{rows.map((row,index)=>{const scaled=scale(row,multipliers[index]);return <p key={index}><b>R{index+1}</b><select data-lesson-control={`eliminate-multiplier-${title.startsWith("Your")?"practice-":""}${index}`} aria-label={`${title} row ${index+1} multiplier`} value={multipliers[index]} onChange={e=>onMultiplier(index,Number(e.target.value))}>{[-5,-4,-3,-2,-1,1,2,3,4,5].map(n=><option key={n}>{n}</option>)}</select><span>{fmt(scaled.a)}</span><span>{fmt(scaled.b)}</span><span>{fmt(scaled.c)}</span></p>})}</article>}
function Result({model}:{model:ReturnType<typeof eliminate>}){return <article className="result"><h3>ROW ADDITION RESULT</h3><header><span>x-coef</span><span>y-coef</span><span>Constant</span></header><p><strong>{fmt(model.sum.a)}</strong><strong>{fmt(model.sum.b)}</strong><strong>{fmt(model.sum.c)}</strong></p></article>}
function eliminate(rows:[Equation,Equation],multipliers:[number,number]){const scaled:[Equation,Equation]=[scale(rows[0],multipliers[0]),scale(rows[1],multipliers[1])],sum={a:scaled[0].a+scaled[1].a,b:scaled[0].b+scaled[1].b,c:scaled[0].c+scaled[1].c},eliminated=Math.abs(sum.a)<1e-8?"x":Math.abs(sum.b)<1e-8?"y":"none";let x=NaN,y=NaN;if(eliminated==="x"){y=sum.c/sum.b;x=(rows[0].c-rows[0].b*y)/rows[0].a}else if(eliminated==="y"){x=sum.c/sum.a;y=(rows[0].c-rows[0].a*x)/rows[0].b}return{scaled,sum,eliminated,x,y}}
function scale(row:Equation,k:number){return{a:row.a*k,b:row.b*k,c:row.c*k}}function fmt(n:number){return Number.isFinite(n)?String(Number(n.toFixed(3))):"-"}function term(n:number,v:string){return`${fmt(n)}${v}`}function signed(n:number,v:string){return`${n<0?"-":"+"} ${fmt(Math.abs(n))}${v}`}function equation(row:Equation){return`${term(row.a,"x")} ${signed(row.b,"y")} = ${fmt(row.c)}`}function verify(row:Equation,x:number,y:number){return`${term(row.a,"x")} + ${fmt(row.b)}(${fmt(y)}) = ${fmt(row.a*x+row.b*y)} = ${fmt(row.c)}`}
