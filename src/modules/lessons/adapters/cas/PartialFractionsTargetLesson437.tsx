import { ArrowLeft, ArrowRight, Check, Eye, Hand, Info, Lightbulb, Link, Plus, RotateCcw, Target, TriangleAlert, X } from "lucide-react";
import { useEffect, useMemo, useState, type DragEvent, type KeyboardEvent } from "react";
import type { LessonAdapterProps } from "../../types";
import "./PartialFractionsTargetLesson437.css";

const INITIAL=[1,2];type Feedback="idle"|"correct"|"incorrect";
export default function PartialFractionsTargetLesson437({resetToken,onInteraction}:LessonAdapterProps){
 const [factors,setFactors]=useState(INITIAL),[activeTab,setActiveTab]=useState("Overview"),[actions,setActions]=useState(0),[quickCheck,setQuickCheck]=useState(true),[practice,setPractice]=useState<[string,string]>(["",""]),[feedback,setFeedback]=useState<Feedback>("idle"),[hints,setHints]=useState(false);const model=useMemo(()=>decompose(factors),[factors]);
 useEffect(()=>{setFactors(INITIAL);setActiveTab("Overview");setActions(0);setQuickCheck(true);setPractice(["",""]);setFeedback("idle");setHints(false)},[resetToken]);const act=(run:()=>void)=>{run();setActions(v=>v+1);onInteraction()};const swap=(from:number,to:number)=>act(()=>setFactors(current=>{const next=[...current];[next[from],next[to]]=[next[to],next[from]];return next}));const add=()=>act(()=>setFactors(current=>current.includes(3)?current:[...current,3]));const check=()=>act(()=>{const a=Number(practice[0]),b=Number(practice[1]);setFeedback(Math.abs(a-.25)<1e-8&&Math.abs(b+.25)<1e-8?"correct":"incorrect")});
 return <section className="pf437-page" data-testid="symbolic-cas-mockup-0343" data-dedicated-lesson="437" data-object-model="distinct-linear-factor-residue-coefficients-drag-recombine-practice" data-factors={factors.join(",")} data-coefficients={model.coefficients.join(",")} data-recombined={model.recombined} data-active-tab={activeTab} data-actions={actions} data-practice-feedback={feedback}>
  <h2 className="sr-only">Partial Fractions</h2><nav className="pf437-tabs">{["Overview","Build Decomposition","Solve Coefficients","Recombine & Verify"].map(tab=><button key={tab} className={activeTab===tab?"active":""} data-lesson-control={`partial-tab-${tab.toLowerCase().replaceAll(" ","-")}`} onClick={()=>act(()=>setActiveTab(tab))}>{tab}</button>)}<button onClick={()=>act(()=>setActiveTab("Overview"))}><Info/> How it works</button></nav>
  <section className="pf437-work"><h2>How this CAS Workspace works</h2><div className="guide">{[[Eye,"1 Observe","See the target rational expression to decompose."],[Hand,"2 Manipulate","Build the decomposition and solve coefficients."],[Lightbulb,"3 Notice","Watch terms combine to recreate the original."],[Target,"4 Understand","You have decomposed the rational expression."]].map(([Icon,title,text],i)=><article key={String(title)}><Icon/><span><b>{String(title)}</b><p>{String(text)}</p></span>{i<3&&<ArrowRight/>}</article>)}</div><div className="pf437-build"><article className="target"><h3>Target expression <Info/></h3><b>Read:</b><strong>{targetExpression(factors)}</strong><b>Factorized denominator</b><p>{factorProduct(factors)}</p><div><b>Rule (Distinct linear factors)</b><p>If the denominator factors are distinct, write one term for each factor and solve its constant coefficient.</p><span>P(x) / product = A1/(x-r1) + ... + An/(x-rn)</span></div></article><article className="decomposition"><h3><i>1</i> Build decomposition</h3><p>Construct the partial fraction form.</p><div className="formula">{model.coefficients.map((coefficient,index)=><span key={factors[index]}><b>{fmt(coefficient)}</b><i>{factor(factors[index])}</i></span>)}</div><h4>Denominator factors (slots)</h4><div className="slots">{factors.map((value,index)=><FactorSlot key={value} value={value} index={index} onSwap={swap}/>) }<button data-lesson-control="partial-add-factor" onClick={add}><Plus/> Add term</button></div></article><article className="coefficients"><h3><i>2</i> Solve coefficients</h3><p>Determine {model.coefficients.map((_,i)=>String.fromCharCode(65+i)).join(", ")}.</p><h4>Set up equations</h4>{factors.map((value,index)=><p key={value}>x = {-value} &nbsp; =&gt; &nbsp; 1 = {String.fromCharCode(65+index)} x {fmt(model.denominators[index])}</p>)}<h4>Solutions</h4><div>{model.coefficients.map((value,index)=><strong key={index}>{String.fromCharCode(65+index)} = {fmt(value)} <Check/></strong>)}</div></article></div><section className="recombine"><h3><i>3</i> Recombine & verify</h3><p>Combine the partial fractions to get the original expression.</p><div>{model.coefficients.map((value,index)=><span key={index}>{fmt(value)} / {factor(factors[index])}</span>)}<ArrowRight/><strong>Numerator = {fmt(model.recombined)}</strong><ArrowRight/><b>{targetExpression(factors)}</b></div><footer><Check/> Verified! The recombined expression matches the target.</footer></section></section>
  <section className="pf437-challenge"><article><h2><TriangleAlert/> Common misconception</h2><p>Do not use A/x + B/x when the denominator is (x+1)(x+2). Each term must contain one factor in its own denominator.</p><h3>Incorrect form:</h3><p>A/x + B/x <X/></p><h3>Correct form:</h3><p>A/(x+1) + B/(x+2) <Check/></p></article><article><header><h2>Your turn: Practice</h2><label>Quick check <input data-lesson-control="partial-quick-check" type="checkbox" checked={quickCheck} onChange={e=>act(()=>setQuickCheck(e.target.checked))}/></label></header><p>Decompose the expression and find A, B.</p><strong>1 / ((x-1)(x+3))</strong><div><section><b>Decomposition</b><p>A/(x-1) + B/(x+3)</p><label>A = <input data-lesson-control="partial-practice-a" aria-label="Practice coefficient A" value={practice[0]} onChange={e=>setPractice([e.target.value,practice[1]])}/></label><label>B = <input data-lesson-control="partial-practice-b" aria-label="Practice coefficient B" value={practice[1]} onChange={e=>setPractice([practice[0],e.target.value])}/></label></section><ArrowRight/><section><b>Solutions</b><p>A = {practice[0]||"?"}</p><p>B = {practice[1]||"?"}</p></section><ArrowRight/><section><b>Recombined result</b><output>{practice[0]&&practice[1]?fmt(3*Number(practice[0])-Number(practice[1])):""}</output></section></div><footer><button data-lesson-control="partial-practice-check" onClick={check}><Check/> Check answer</button><button data-lesson-control="partial-practice-hints" onClick={()=>act(()=>setHints(v=>!v))}><Lightbulb/> Show hints</button><button data-lesson-control="partial-practice-reset" onClick={()=>act(()=>{setPractice(["",""]);setFeedback("idle")})}><RotateCcw/> Reset</button></footer>{hints&&<p className="hint">Set x=1 to isolate A, then x=-3 to isolate B.</p>}{feedback!=="idle"&&<em className={feedback}>{feedback==="correct"?"Correct: A=1/4 and B=-1/4.":"Use the roots of each denominator factor."}</em>}</article></section>
  <section className="pf437-takeaways"><article><h2><Link/> Key takeaways</h2><ul><li>Break the denominator into linear factors.</li><li>Write one term for each factor.</li><li>Use convenient values to solve coefficients.</li><li>Recombine to verify.</li></ul></article><article><h2><Link/> When to use</h2><p>Use partial fractions to simplify integration, Laplace transforms, and rational expression manipulation.</p><span>CAS Essential</span></article></section><nav className="pf437-nav"><a href="/lessons/symbolic-mathematics/436-eliminate-variables"><ArrowLeft/><span><small>Previous</small>Eliminate Variables</span></a><a href="/lessons/symbolic-mathematics/438-polynomial-division"><span><small>Next</small>Polynomial Division</span><ArrowRight/></a></nav>
 </section>
}
function FactorSlot({value,index,onSwap}:{value:number;index:number;onSwap:(from:number,to:number)=>void}){const drag=(e:DragEvent<HTMLButtonElement>)=>e.dataTransfer.setData("application/x-partial-factor",String(index)),drop=(e:DragEvent<HTMLButtonElement>)=>{e.preventDefault();const from=Number(e.dataTransfer.getData("application/x-partial-factor"));if(Number.isInteger(from))onSwap(from,index)},key=(e:KeyboardEvent<HTMLButtonElement>)=>{if(e.key==="ArrowLeft"&&index>0)onSwap(index,index-1)};return <button draggable data-lesson-control={`partial-factor-${value}`} onDragStart={drag} onDragOver={e=>e.preventDefault()} onDrop={drop} onKeyDown={key}>{factor(value)} <Check/></button>}
function decompose(factors:number[]){const denominators=factors.map((value,index)=>factors.reduce((product,other,j)=>j===index?product:product*(other-value),1)),coefficients=denominators.map(value=>1/value),polynomial=coefficients.reduce((sum,coefficient,index)=>add(sum,scalePoly(factors.reduce((product,factorValue,j)=>j===index?product:multiply(product,[factorValue,1]),[1]),coefficient)),[0]);return{denominators,coefficients,recombined:polynomialText(polynomial)}}function multiply(a:number[],b:number[]){const out=Array(a.length+b.length-1).fill(0);a.forEach((left,i)=>b.forEach((right,j)=>{out[i+j]+=left*right}));return out}function scalePoly(values:number[],factor:number){return values.map(value=>value*factor)}function add(a:number[],b:number[]){return Array.from({length:Math.max(a.length,b.length)},(_,i)=>(a[i]||0)+(b[i]||0))}function polynomialText(values:number[]){const cleaned=values.map(value=>Math.abs(value)<1e-9?0:value);if(cleaned.slice(1).every(value=>value===0))return fmt(cleaned[0]);return cleaned.map((value,index)=>value?`${fmt(value)}${index?`x${index>1?`^${index}`:""}`:""}`:"").filter(Boolean).reverse().join(" + ")}function factor(value:number){return`(x + ${value})`}function factorProduct(values:number[]){return values.map(factor).join("")}function targetExpression(values:number[]){return`1 / ${factorProduct(values)}`}function fmt(value:number|string){if(typeof value==="string")return value;if(Number.isInteger(value))return String(value);const fourth=Math.round(value*4);return Math.abs(fourth/4-value)<1e-9?`${fourth}/4`:value.toFixed(3)}
