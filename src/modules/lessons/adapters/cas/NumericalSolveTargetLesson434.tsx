import { ArrowLeft, ArrowRight, Check, Eye, Expand, Hand, Lightbulb, Play, RotateCcw, Search, Target, TriangleAlert } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import type { LessonAdapterProps } from "../../types";
import "./NumericalSolveTargetLesson434.css";

type Method="bisection"|"newton";type Row={k:number;a:number;b:number;c:number;fc:number};
const INITIAL={expression:"x^2-5x+6",a:1,b:4,tolerance:1e-6,max:50};

export default function NumericalSolveTargetLesson434({resetToken,onInteraction}:LessonAdapterProps){
 const [expression,setExpression]=useState(INITIAL.expression),[a,setA]=useState(INITIAL.a),[b,setB]=useState(INITIAL.b),[tolerance,setTolerance]=useState(INITIAL.tolerance),[max,setMax]=useState(INITIAL.max),[method,setMethod]=useState<Method>("bisection"),[solved,setSolved]=useState(true),[actions,setActions]=useState(0),[fullscreen,setFullscreen]=useState(false),[practice,setPractice]=useState<[string,string]>(["0","1"]),[practiceResult,setPracticeResult]=useState<"idle"|"correct"|"incorrect">("idle"),[hint,setHint]=useState(false);
 useEffect(()=>{setExpression(INITIAL.expression);setA(INITIAL.a);setB(INITIAL.b);setTolerance(INITIAL.tolerance);setMax(INITIAL.max);setMethod("bisection");setSolved(true);setActions(0);setFullscreen(false);setPractice(["0","1"]);setPracticeResult("idle");setHint(false)},[resetToken]);
 const model=useMemo(()=>parseQuadratic(expression),[expression]);const result=useMemo(()=>{const midpoint=(a+b)/2,guess=Math.abs(model.df(midpoint))<1e-8?a:midpoint;return method==="bisection"?bisect(model,a,b,tolerance,max):newton(model,guess,tolerance,max)},[model,a,b,tolerance,max,method]);
 const act=(run:()=>void)=>{run();setActions(v=>v+1);onInteraction()};const reset=()=>act(()=>{setExpression(INITIAL.expression);setA(INITIAL.a);setB(INITIAL.b);setTolerance(INITIAL.tolerance);setMax(INITIAL.max);setSolved(false)});
 const checkPractice=()=>act(()=>{const l=Number(practice[0]),r=Number(practice[1]);setPracticeResult(l<=0&&r>=1&&Math.cos(l)-l>0&&Math.cos(r)-r<0?"correct":"incorrect")});
 return <section className={`num434-page${fullscreen?" fullscreen":""}`} data-testid="symbolic-cas-mockup-0340" data-dedicated-lesson="434" data-object-model="adaptive-bisection-newton-iteration-graph-residual-practice" data-method={method} data-root={result.root.toFixed(6)} data-residual={Math.abs(result.fc).toExponential(2)} data-iterations={result.rows.length} data-converged={result.converged} data-actions={actions} data-practice-feedback={practiceResult}>
  <h2 className="sr-only">Numerical Solve</h2>
  <section className="num434-lab"><header><div><b>CAS WORKSPACE - Graph-linked root finder</b><p>Find a root (solution) of <i>f(x) = 0</i> using Bisection or Newton's method.</p></div><aside><strong>{solved?"Active":"Ready"}</strong><span>{actions} actions</span><button data-lesson-control="numerical-fullscreen" aria-label="Toggle numerical solver fullscreen" onClick={()=>act(()=>setFullscreen(v=>!v))}><Expand/></button></aside></header>
   <div className="num434-work"><aside className="num434-controls"><ControlTitle n="1" text="METHOD"/><p>Choose method</p><div className="method"><button className={method==="bisection"?"active":""} data-lesson-control="numerical-bisection" onClick={()=>act(()=>setMethod("bisection"))}>Bisection</button><button className={method==="newton"?"active":""} data-lesson-control="numerical-newton" onClick={()=>act(()=>setMethod("newton"))}>Newton</button></div><ControlTitle n="2" text="FUNCTION"/><p>Define <i>f(x)</i></p><input data-lesson-control="numerical-expression" aria-label="Function expression" value={expression} onChange={e=>act(()=>{setExpression(e.target.value);setSolved(false)})}/><small>Example: x^2-5x+6</small><ControlTitle n="3" text="INTERVAL (a, b)"/><p>Start with an interval to scan</p><div className="interval"><label>a<input data-lesson-control="numerical-a" aria-label="Interval start" type="number" value={a} onChange={e=>act(()=>{setA(Number(e.target.value));setSolved(false)})}/></label><label>b<input data-lesson-control="numerical-b" aria-label="Interval end" type="number" value={b} onChange={e=>act(()=>{setB(Number(e.target.value));setSolved(false)})}/></label></div><div className="signs"><span>f(a) = {fmt(model.f(a))}</span><span>f(b) = {fmt(model.f(b))}</span></div><em>{model.f(a)*model.f(b)<0?<><Check/> Sign change</>:"Midpoint scan isolates a root"}</em><ControlTitle n="4" text="PARAMETERS"/><label className="parameter">Tolerance e<input data-lesson-control="numerical-tolerance" aria-label="Tolerance" type="number" value={tolerance} onChange={e=>act(()=>setTolerance(Number(e.target.value)))}/></label><label className="parameter">Max iterations<input data-lesson-control="numerical-max" aria-label="Maximum iterations" type="number" value={max} onChange={e=>act(()=>setMax(Number(e.target.value)))}/></label><button className="solve" data-lesson-control="numerical-start" onClick={()=>act(()=>setSolved(true))}><Play/> Start Solve</button><button className="reset" data-lesson-control="numerical-reset" onClick={reset}><RotateCcw/> Reset All</button></aside>
    <main className="num434-graph"><h3>GRAPH OF <i>f(x) = {expression}</i></h3><Graph model={model} a={a} b={b} root={result.root}/><div className="legend"><span className="curve">f(x) = {expression}</span><span className="root">Root (solution)</span><span className="a">a = {a}</span><span className="b">b = {b}</span><span className="bracket">Current bracket [a, b]</span></div><p><Search/><b>Observe:</b> The curve crosses the x-axis at x = 2 and x = 3. The solver follows the first root isolated inside the interval.</p></main>
    <aside className="num434-results"><ControlTitle n="5" text="RESULTS"/><h3>Iteration Table</h3><table><thead><tr><th>k</th><th>a</th><th>b</th><th>c</th><th>f(c)</th></tr></thead><tbody>{visibleRows(result.rows).map(row=><tr key={row.k}><td>{row.k}</td><td>{row.a.toFixed(4)}</td><td>{row.b.toFixed(4)}</td><td>{row.c.toFixed(4)}</td><td>{row.fc.toExponential(2)}</td></tr>)}</tbody></table><section className="converged"><h3>Converged <Check/></h3><p><span>Root (c)</span><b>{result.root.toFixed(6)}</b></p><p><span>f(c)</span><b>{result.fc.toExponential(2)}</b></p><p><span>Iterations</span><b>{result.rows.length}</b></p><p><span>Method</span><b>{method==="bisection"?"Adaptive Bisection":"Newton"}</b></p></section><section className="residual"><h3>Residual</h3><p>|f(c)| = {Math.abs(result.fc).toExponential(2)} &lt; e = {tolerance.toExponential(0)} <Check/></p></section></aside></div>
  </section>
  <section className="num434-flow">{[[Eye,"Observe","The parabola crosses the x-axis where f(x) = 0. Here the roots are near x=2 and x=3."],[Hand,"Manipulate","Adjust the interval so the midpoint isolates a sign change, then solve."],[Search,"Notice","Each iteration halves the active bracket and approaches the root."],[Lightbulb,"Understand","When |f(c)| < e, c is accepted as an approximate solution."]].map(([Icon,title,text],i)=><article key={String(title)}><Icon/><h2>{String(title)}</h2><p>{String(text)}</p>{i<3&&<ArrowRight/>}</article>)}</section>
  <section className="num434-learn"><article><h2>How it works</h2><p>Bisection repeatedly halves the interval containing a root.</p><div><b>Rule</b><span>If f is continuous and opposite signs are found, a root lies between them.</span></div><h3>Algorithm (Bisection)</h3><ol><li>Compute c = (a+b)/2 and f(c).</li><li>Keep the half with a sign change.</li><li>Stop when |f(c)| &lt; e.</li><li>Repeat until tolerance or max iterations.</li></ol><aside><b>Key fact</b><p>Convergence is guaranteed once a continuous function has a sign-changing bracket.</p><Target/></aside></article><article><h2>Worked example (one root)</h2><p>Solve x² - 5x + 6 = 0 with the selected interval and tolerance.</p><h3>Step 1 <span>Evaluate endpoints and midpoint to isolate one root.</span></h3><h3>Step 2 <span>Iterations shrink the active interval toward the root.</span></h3><h3>Step 3 <span>Result: x ~ {result.root.toFixed(6)} within tolerance.</span></h3><p className="check">Check: f({result.root.toFixed(6)}) = {result.fc.toExponential(2)} <Check/></p></article></section>
  <section className="num434-bottom"><article><h2>Common misconception <TriangleAlert/></h2><p>A starting interval containing roots at both ends does not automatically give a sign change. Inspect the midpoint or choose a tighter bracket.</p><p>Newton's method can also diverge when the initial guess is far from a root or f'(x) is near zero.</p></article><article><h2>Practice challenge</h2><p>Find a root of f(x) = cos x - x using Bisection.</p><label>Use interval [a,b] = <input data-lesson-control="numerical-practice-a" aria-label="Practice interval start" value={practice[0]} onChange={e=>setPractice([e.target.value,practice[1]])}/> - <input data-lesson-control="numerical-practice-b" aria-label="Practice interval end" value={practice[1]} onChange={e=>setPractice([practice[0],e.target.value])}/></label><label>Tolerance e = <input value="1e-6" readOnly/></label><button data-lesson-control="numerical-practice-check" onClick={checkPractice}>Check my answer</button><aside><b>Answer</b><span>x ~ 0.739085</span><button data-lesson-control="numerical-practice-hint" onClick={()=>act(()=>setHint(v=>!v))}>Hint</button>{hint&&<p>f(0)=1&gt;0 and f(1)&lt;0, so [0,1] brackets a root.</p>}{practiceResult!=="idle"&&<em className={practiceResult}>{practiceResult==="correct"?"Valid bracket.":"Choose endpoints with opposite signs."}</em>}</aside></article></section>
  <nav className="num434-nav"><a href="/lessons/symbolic-mathematics/433-solve"><ArrowLeft/><span><small>Previous</small>Solve</span></a><a href="/lessons/symbolic-mathematics/435-solve-systems"><span><small>Next</small>Solve Systems</span><ArrowRight/></a></nav>
 </section>
}
function ControlTitle({n,text}:{n:string;text:string}){return <h2><b>{n}</b> {text}</h2>}
function parseQuadratic(source:string){const s=source.replaceAll(" ","").replaceAll("²","^2");const m=s.match(/^x\^2(?:(\+|-)(\d*)x)?(?:(\+|-)(\d+))$/);const b=m&&m[1]?(m[1]==="-"?-1:1)*Number(m[2]||1):-5,c=m?(m[3]==="-"?-1:1)*Number(m[4]):6;return{b,c,f:(x:number)=>x*x+b*x+c,df:(x:number)=>2*x+b}}
function bisect(model:ReturnType<typeof parseQuadratic>,start:number,end:number,tol:number,max:number){let a=Math.min(start,end),b=Math.max(start,end),fa=model.f(a),fb=model.f(b),root=(a+b)/2,fc=model.f(root);const rows:Row[]=[];for(let k=0;k<Math.max(1,max);k++){root=(a+b)/2;fc=model.f(root);rows.push({k,a,b,c:root,fc});if(Math.abs(fc)<=Math.max(tol,1e-12)){break}if(fa*fc<0){b=root;fb=fc}else if(fc*fb<0){a=root;fa=fc}else{const left=(a+root)/2,right=(root+b)/2,fl=model.f(left),fr=model.f(right);if(fa*fl<=0){b=left;fb=fl}else if(fl*fc<=0){a=left;fa=fl;b=root;fb=fc}else if(fc*fr<=0){a=root;fa=fc;b=right;fb=fr}else{a=right;fa=fr}}}return{rows,root,fc,converged:Math.abs(fc)<=Math.max(tol,1e-12)}}
function newton(model:ReturnType<typeof parseQuadratic>,guess:number,tol:number,max:number){let x=guess,fc=model.f(x);const rows:Row[]=[];for(let k=0;k<Math.max(1,max);k++){fc=model.f(x);const next=x-fc/model.df(x);rows.push({k,a:x,b:next,c:next,fc:model.f(next)});x=next;fc=model.f(x);if(Math.abs(fc)<=Math.max(tol,1e-12))break}return{rows,root:x,fc,converged:Math.abs(fc)<=Math.max(tol,1e-12)}}
function visibleRows(rows:Row[]){return rows.length<=7?rows:[...rows.slice(0,5),rows.at(-1)!]}
function fmt(value:number){return Number.isInteger(value)?String(value):value.toFixed(3)}
function Graph({model,a,b,root}:{model:ReturnType<typeof parseQuadratic>;a:number;b:number;root:number}){const x=(v:number)=>42+(v+0.5)*50,y=(v:number)=>202-v*24,pts=Array.from({length:141},(_,i)=>{const n=-.5+i*.05;return`${x(n)},${y(model.f(n))}`}).join(" ");return <svg viewBox="0 0 390 300" role="img" aria-label="Quadratic graph with numerical root bracket"><defs><pattern id="num434grid" width="50" height="48" patternUnits="userSpaceOnUse"><path d="M50 0H0V48" fill="none" stroke="#dbe4ed"/></pattern></defs><rect x="17" y="10" width="350" height="250" fill="url(#num434grid)"/><rect x={x(a)} y="10" width={Math.max(0,x(b)-x(a))} height="250" fill="#dce8ff" opacity=".5"/><line x1="17" y1={y(0)} x2="375" y2={y(0)} className="axis"/><line x1={x(0)} y1="10" x2={x(0)} y2="270" className="axis"/><polyline points={pts} className="plot"/><line x1={x(a)} y1={y(0)} x2={x(a)} y2="270" className="aline"/><line x1={x(b)} y1={y(0)} x2={x(b)} y2="270" className="bline"/><circle cx={x(root)} cy={y(0)} r="4" className="root"/><text x={x(root)} y={y(0)-38}>Root</text><text x={x(root)} y={y(0)-25}>x ~ {root.toFixed(2)}</text>{[0,1,2,3,4,5,6].map(n=><text x={x(n)} y="285" key={n}>{n}</text>)}</svg>}
