import { ArrowLeft, ArrowRight, Eye, Expand, Hand, Lightbulb, Pencil, Star, Target, TriangleAlert, ZoomIn, ZoomOut } from "lucide-react";
import { useEffect, useMemo, useRef, useState, type KeyboardEvent, type PointerEvent } from "react";
import type { LessonAdapterProps } from "../../types";
import "./SolveSystemsTargetLesson435.css";

type Equation={a:number;b:number;c:number};type Kind="unique"|"none"|"infinite";
const INITIAL:[Equation,Equation]=[{a:2,b:3,c:13},{a:1,b:-1,c:1}];

export default function SolveSystemsTargetLesson435({resetToken,onInteraction}:LessonAdapterProps){
 const [equations,setEquations]=useState<[Equation,Equation]>(INITIAL),[editing,setEditing]=useState<number|null>(null),[method,setMethod]=useState("Elimination (add/subtract)"),[zoom,setZoom]=useState(1),[fullscreen,setFullscreen]=useState(false),[actions,setActions]=useState(0),[practice,setPractice]=useState<[string,string]>(["",""]),[feedback,setFeedback]=useState<"idle"|"correct"|"incorrect">("idle"),[hint,setHint]=useState(false);const graphRef=useRef<SVGSVGElement>(null);
 useEffect(()=>{setEquations(INITIAL);setEditing(null);setMethod("Elimination (add/subtract)");setZoom(1);setFullscreen(false);setActions(0);setPractice(["",""]);setFeedback("idle");setHint(false)},[resetToken]);
 const model=useMemo(()=>solveSystem(equations[0],equations[1]),[equations]);const act=(run:()=>void)=>{run();setActions(v=>v+1);onInteraction()};
 const update=(row:number,key:keyof Equation,value:number)=>act(()=>setEquations(current=>current.map((eq,index)=>index===row?{...eq,[key]:value}:eq) as [Equation,Equation]));
 const chooseKind=(kind:Kind)=>act(()=>setEquations(kind==="unique"?INITIAL:kind==="none"?[{a:2,b:3,c:13},{a:4,b:6,c:20}]:[{a:2,b:3,c:13},{a:4,b:6,c:26}]));
 const drag=(event:PointerEvent<SVGCircleElement>)=>{event.currentTarget.setPointerCapture(event.pointerId);moveIntersection(event)};const moveIntersection=(event:PointerEvent<SVGCircleElement>)=>{if(!event.currentTarget.hasPointerCapture(event.pointerId))return;const box=graphRef.current!.getBoundingClientRect(),x=((event.clientX-box.left)/box.width*20-10)/zoom,y=(10-(event.clientY-box.top)/box.height*20)/zoom;act(()=>setEquations(current=>current.map(eq=>({...eq,c:eq.a*x+eq.b*y})) as [Equation,Equation]))};
 const keyMove=(event:KeyboardEvent<SVGCircleElement>)=>{const dx=event.key==="ArrowRight"?.2:event.key==="ArrowLeft"?-.2:0,dy=event.key==="ArrowUp"?.2:event.key==="ArrowDown"?-.2:0;if(!dx&&!dy)return;event.preventDefault();act(()=>setEquations(current=>current.map(eq=>({...eq,c:eq.c+eq.a*dx+eq.b*dy})) as [Equation,Equation]))};
 const checkPractice=()=>act(()=>{const x=Number(practice[0]),y=Number(practice[1]);setFeedback(Math.abs(x-2.5)<1e-8&&Math.abs(y-2.25)<1e-8?"correct":"incorrect")});
 return <section className={`sys435-page${fullscreen?" fullscreen":""}`} data-testid="symbolic-cas-mockup-0341" data-dedicated-lesson="435" data-object-model="two-equation-determinant-elimination-intersection-drag-classification-practice" data-kind={model.kind} data-solution={`${model.x},${model.y}`} data-equations={equations.map(eq=>`${eq.a},${eq.b},${eq.c}`).join(";")} data-zoom={zoom} data-actions={actions} data-practice-feedback={feedback}>
  <h2 className="sr-only">Solve Systems</h2><section className="sys435-how"><h2>How to use this CAS Workspace</h2><div>{[[Eye,"1 Observe","See both equations as lines on the graph and the current solution."],[Hand,"2 Manipulate","Edit coefficients in the inputs or choose a method to solve."],[Lightbulb,"3 Notice","Watch the graph and solution update instantly."],[Target,"4 Understand","Connect algebraic steps to intersections and the system type."]].map(([Icon,title,text],i)=><article key={String(title)}><Icon/><span><b>{String(title)}</b><p>{String(text)}</p></span>{i<3&&<ArrowRight/>}</article>)}</div></section>
  <section className="sys435-grid"><article className="sys435-cas"><header><h2>CAS Workspace</h2><span>Live</span></header><h3>System of linear equations</h3>{equations.map((eq,index)=><div className="equation" key={index}><b>{index?"B":"A"}</b>{editing===index?<span className="coefficients"><input aria-label={`Equation ${index?"B":"A"} x coefficient`} type="number" value={eq.a} onChange={e=>update(index,"a",Number(e.target.value))}/><i>x +</i><input aria-label={`Equation ${index?"B":"A"} y coefficient`} type="number" value={eq.b} onChange={e=>update(index,"b",Number(e.target.value))}/><i>y =</i><input aria-label={`Equation ${index?"B":"A"} constant`} type="number" value={round(eq.c)} onChange={e=>update(index,"c",Number(e.target.value))}/></span>:<strong>{equationText(eq)}</strong>}<button data-lesson-control={`systems-edit-${index}`} aria-label={`Edit equation ${index?"B":"A"}`} onClick={()=>act(()=>setEditing(editing===index?null:index))}><Pencil/></button><i className="color"/></div>)}<label>Method<select data-lesson-control="systems-method" aria-label="System solving method" value={method} onChange={e=>act(()=>setMethod(e.target.value))}><option>Elimination (add/subtract)</option><option>Substitution</option><option>Graph intersection</option></select></label></article>
   <article className="sys435-graph"><h2>Graph</h2><SystemGraph graphRef={graphRef} equations={equations} model={model} zoom={zoom} onStart={drag} onMove={moveIntersection} onKey={keyMove}/><aside><button data-lesson-control="systems-zoom-in" aria-label="Zoom in graph" onClick={()=>act(()=>setZoom(v=>Math.min(1.6,v+.2)))}><ZoomIn/></button><button data-lesson-control="systems-zoom-out" aria-label="Zoom out graph" onClick={()=>act(()=>setZoom(v=>Math.max(.6,v-.2)))}><ZoomOut/></button><button data-lesson-control="systems-fullscreen" aria-label="Toggle systems fullscreen" onClick={()=>act(()=>setFullscreen(v=>!v))}><Expand/></button></aside><footer><span>A: {equationText(equations[0])}</span><span>B: {equationText(equations[1])}</span></footer></article>
   <article className="sys435-solution"><header><h2>Solution ({method.split(" ")[0]})</h2><span>{kindLabel(model.kind)}</span></header><ol>{model.kind==="unique"?<><li><b>Write equations</b><span>A: {equationText(equations[0])}<br/>B: {equationText(equations[1])}</span></li><li><b>Multiply B by {Math.abs(equations[0].b)}</b><span>{formatScaled(equations[1],Math.abs(equations[0].b))}</span></li><li><b>Add equations to eliminate y</b><span>det(A,B) = {round(model.det)}</span></li><li><b>Solve for x</b><span>x = {fraction(model.x)} = {round(model.x)}</span></li><li><b>Substitute to solve y</b><span>y = {fraction(model.y)} = {round(model.y)}</span></li></>:<li><b>{kindLabel(model.kind)}</b><span>{model.kind==="none"?"Parallel lines have no intersection.":"Both equations describe the same line."}</span></li>}</ol><footer><b>Solution:</b><span>{model.kind==="unique"?`x = ${fraction(model.x)}, y = ${fraction(model.y)} ~ (${round(model.x)}, ${round(model.y)})`:kindLabel(model.kind)}</span></footer></article>
   <article className="sys435-rule"><h2>Rule / Formula</h2><p>A system of two linear equations in two variables</p><strong>a1x + b1y = c1<br/>a2x + b2y = c2</strong><p>has</p><ul><li>Unique solution if a1/a2 != b1/b2</li><li>No solution if coefficient ratios match but constants do not</li><li>Infinitely many solutions if all ratios match</li></ul></article>
   <article className="sys435-types"><h2>System type</h2><div><button className={model.kind==="unique"?"active":""} data-lesson-control="systems-kind-unique" onClick={()=>chooseKind("unique")}>Unique<br/>solution</button><button className={model.kind==="none"?"active":""} data-lesson-control="systems-kind-none" onClick={()=>chooseKind("none")}>No<br/>solution</button><button className={model.kind==="infinite"?"active":""} data-lesson-control="systems-kind-infinite" onClick={()=>chooseKind("infinite")}>Infinitely many<br/>solutions</button></div><p>Current status: <b>{kindLabel(model.kind)}</b></p></article>
  </section>
  <section className="sys435-lower"><article><header><h2>Worked Example (You try)</h2><span>Correct</span></header><p>Solve the system:<br/><b>2x - y = 4<br/>3x + y = 7</b></p><h3>Add equations:</h3><p>(2x - y) + (3x + y) = 4 + 7<br/>5x = 11 &nbsp; =&gt; &nbsp; x = 11/5</p><h3>Substitute in 2x - y = 4:</h3><p>2(11/5) - y = 4 &nbsp; =&gt; &nbsp; y = 2/5</p><footer><b>Answer:</b> (11/5, 2/5)</footer></article><article><h2><TriangleAlert/> Common Misconception</h2><p>Don't add or subtract unlike terms.</p><h3>Wrong: Adding A + B directly</h3><div>(2x + 3y = 13) + (x - y = 1)<br/><b>3x + 2y = 14</b> (incomplete)</div><h3>Why wrong?</h3><p>Align coefficients so one variable is eliminated before solving.</p></article><article><header><h2>Quick Practice</h2><button data-lesson-control="systems-practice-check" onClick={checkPractice}>Check</button></header><p>Solve the system below.</p><strong>3x + 2y = 12<br/>x - 2y = -2</strong><label>x = <input data-lesson-control="systems-practice-x" aria-label="Practice x solution" value={practice[0]} onChange={e=>setPractice([e.target.value,practice[1]])} placeholder="Enter value"/></label><label>y = <input data-lesson-control="systems-practice-y" aria-label="Practice y solution" value={practice[1]} onChange={e=>setPractice([practice[0],e.target.value])} placeholder="Enter value"/></label><button data-lesson-control="systems-practice-hint" onClick={()=>act(()=>setHint(v=>!v))}><Lightbulb/> Show hint</button>{hint&&<p className="hint">Add the equations to eliminate y.</p>}{feedback!=="idle"&&<em className={feedback}>{feedback==="correct"?"Correct: (2.5, 2.25).":"Check both equations."}</em>}</article></section>
  <section className="sys435-insight"><Star/><span><b>Key Insight</b><p>The solution to a linear system is the intersection point of the two lines. Changing coefficients moves the lines and changes the intersection.</p></span><aside>Domain: x, y in R &nbsp;&nbsp; Range: All real numbers</aside></section>
  <nav className="sys435-nav"><a href="/lessons/symbolic-mathematics/434-numerical-solve"><ArrowLeft/><span><small>Previous</small>Numerical Solve</span></a><a href="/lessons/symbolic-mathematics/436-eliminate-variables"><span><small>Next</small>Eliminate Variables</span><ArrowRight/></a></nav>
 </section>
}

const SystemGraph=({graphRef,equations,model,zoom,onStart,onMove,onKey}:{graphRef:{current:SVGSVGElement|null};equations:[Equation,Equation];model:ReturnType<typeof solveSystem>;zoom:number;onStart:(e:PointerEvent<SVGCircleElement>)=>void;onMove:(e:PointerEvent<SVGCircleElement>)=>void;onKey:(e:KeyboardEvent<SVGCircleElement>)=>void})=>{const line=(eq:Equation)=>{const divisor=Math.abs(eq.b)<1e-8?1e-8:eq.b,y1=(eq.c-eq.a*(-10/zoom))/divisor,y2=(eq.c-eq.a*(10/zoom))/divisor;return{x1:0,y1:150-y1*15*zoom,x2:300,y2:150-y2*15*zoom}};const l1=line(equations[0]),l2=line(equations[1]),cx=150+model.x*15*zoom,cy=150-model.y*15*zoom;return <svg ref={graphRef} viewBox="0 0 300 300" role="img" aria-label="Interactive graph of the linear system"><defs><pattern id="sys435grid" width="15" height="15" patternUnits="userSpaceOnUse"><path d="M15 0H0V15" fill="none" stroke="#e3e9f0"/></pattern></defs><rect width="300" height="300" rx="7" fill="url(#sys435grid)"/><line x1="0" y1="150" x2="300" y2="150" className="axis"/><line x1="150" y1="0" x2="150" y2="300" className="axis"/><line {...l1} className="line a"/><line {...l2} className="line b"/>{model.kind==="unique"&&<><circle data-lesson-control="systems-intersection-drag" tabIndex={0} aria-label="Drag system intersection" cx={cx} cy={cy} r="6" onPointerDown={onStart} onPointerMove={onMove} onKeyDown={onKey}/><g transform={`translate(${cx+8} ${cy-25})`}><rect width="42" height="23" rx="3"/><text x="21" y="15">({round(model.x)}, {round(model.y)})</text></g></>}</svg>};
function solveSystem(e1:Equation,e2:Equation){const det=e1.a*e2.b-e2.a*e1.b,dx=e1.c*e2.b-e2.c*e1.b,dy=e1.a*e2.c-e2.a*e1.c;if(Math.abs(det)>1e-8)return{kind:"unique" as Kind,det,x:dx/det,y:dy/det};return{kind:(Math.abs(dx)<1e-8&&Math.abs(dy)<1e-8?"infinite":"none") as Kind,det,x:0,y:0}}
function equationText(eq:Equation){return`${coef(eq.a,"x")} ${eq.b<0?"-":"+"} ${Math.abs(eq.b)}y = ${round(eq.c)}`}function coef(n:number,v:string){return`${n===1?"":n===-1?"-":n}${v}`}function round(n:number){return Number(n.toFixed(2))}function kindLabel(kind:Kind){return kind==="unique"?"Unique solution":kind==="none"?"No solution":"Infinitely many solutions"}function fraction(n:number){const d=5,k=Math.round(n*d);return Math.abs(k/d-n)<1e-7?`${k}/${d}`:String(round(n))}function formatScaled(eq:Equation,k:number){return`${coef(eq.a*k,"x")} ${eq.b*k<0?"-":"+"} ${Math.abs(eq.b*k)}y = ${round(eq.c*k)}`}
