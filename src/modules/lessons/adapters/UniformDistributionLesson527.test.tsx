import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it,vi}from"vitest";import{lessonCatalog}from"../catalog/lessonCatalog";import ProbabilityLessonAdapter from"./ProbabilityLessonAdapter";
describe("Uniform Distribution dedicated surface",()=>{it("routes lesson 527 to its draggable interval lab",()=>{const lesson=lessonCatalog.find(item=>item.id===527)!;const html=renderToStaticMarkup(<ProbabilityLessonAdapter lesson={lesson} resetToken={0} onInteraction={vi.fn()}/>);expect(html).toContain('data-testid="probability-mockup-0490"');expect(html).toContain("Explore the Uniform Distribution");expect(html).toContain("0.4167");expect(html).toContain("1.7321")})});
