import { ArrowLeft, ArrowRight, Check, Grid3X3, Lightbulb, Maximize2, RotateCcw, Share2, Sparkles, Target, TriangleAlert } from "lucide-react";
import { useEffect, useMemo, useRef, useState, type PointerEvent as ReactPointerEvent } from "react";
import type { LessonAdapterProps } from "../types";
import "./EnvelopeLinesTargetLesson250.css";

type Point={x:number;y:number};const initialTraces=Array.from({length:23},(_,i)=>-5.5+i*.5);

export default function EnvelopeLinesTargetLesson250({resetToken,onInteraction}:LessonAdapterProps){const [m,setM]=useState(1),[c,setC]=useState(0),[accumulate,setAccumulate]=useState(true),[traces,setTraces]=useState(initialTraces),[tab,setTab]=useState("Interaction + Visualization"),[actions,setActions]=useState(0),[answer,setAnswer]=useState("A"),[feedback,setFeedback]=useState("Correct! The envelope is y = x² + 1.");const model=useMemo(()=>envelopeModel(m,c),[m,c]);const act=(fn:()=>void)=>{fn();setActions(n=>n+1);onInteraction()};const changeSlope=(value:number)=>act(()=>{setM(value);if(accumulate)setTraces(points=>[...points,value].slice(-80))});const reset=()=>{setM(1);setC(0);setAccumulate(true);setTraces(initialTraces);setTab("Interaction + Visualization");setActions(0);setAnswer("A");setFeedback("Correct! The envelope is y = x² + 1.");onInteraction()};useEffect(()=>{setM(1);setC(0);setAccumulate(true);setTraces(initialTraces);setTab("Interaction + Visualization");setActions(0);setAnswer("A");setFeedback("Correct! The envelope is y = x² + 1.")},[resetToken]);const check=()=>{setFeedback(answer==="A"?"Correct! The envelope is y = x² + 1.":"Not yet. Eliminate m from F = 0 and ∂F/∂m = 0.");onInteraction()};return <section className="target-envelope-page" data-testid="dynamic-geometry-mockup-0307" data-dedicated-lesson="250" data-object-model="parameterized-tangent-family-parabola-envelope" data-slope={m.toFixed(3)} data-offset={c.toFixed(3)} data-contact-x={model.contact.x.toFixed(3)} data-contact-y={model.contact.y.toFixed(3)} data-line-intercept={model.intercept.toFixed(3)} data-trace-count={traces.length} data-envelope={`y=x^2+${c}`}>
  <header className="target-envelope-header"><div><span>Geometry</span><span>Transformations and Loci</span><h1>Envelope of Lines</h1><p>Explore curve generation.</p><section><b>♙ Intermediate–Advanced</b><b>ϟ Investigation Lab</b><b>▣ Transformation / Locus Tools</b><b>◷ 6–10 min</b></section></div><aside><h2>Rule (Envelope condition)</h2><p>For a family of lines F(x,y,m)=0,<br/>the envelope satisfies</p><b>F(x,y,m)=0 &nbsp; and &nbsp; ∂F/∂m(x,y,m)=0</b></aside><footer><button type="button">⚑ English (English)⌄</button><button type="button" onClick={reset}><RotateCcw/>Reset</button><button type="button" onClick={()=>{void navigator.clipboard?.writeText(`Envelope y=x²+${c}`);onInteraction()}}><Share2/>Share</button><button type="button">↗ Workspace</button></footer></header>
  <nav className="target-envelope-tabs">{["Interaction + Visualization","Explain","Examples","Formulas","Misconceptions","Challenge"].map(name=><button type="button" key={name} className={tab===name?"is-active":""} onClick={()=>act(()=>setTab(name))}>{name}</button>)}</nav>
  <section className="target-envelope-stages">{[[Sparkles,"Observe","Lines move and a curve appears as their envelope."],[Grid3X3,"Manipulate","Adjust parameters to see how the envelope changes."],[Lightbulb,"Notice","The envelope is tangent to each line at one point."],[Target,"Understand","Apply the rule to find the envelope algebraically."]].map(([Icon,title,text])=><article key={String(title)}><Icon/><span><b>{String(title)}</b><p>{String(text)}</p></span></article>)}</section>
  <section className="target-envelope-work"><header><div><b>INTERACTION</b><h2>Work directly on the model</h2><h3>Envelope of Lines – dedicated tangent-family engine</h3></div><aside><span>✓ All good!</span><b>{actions} actions</b><button type="button"><Maximize2/></button></aside></header><div><article className="target-envelope-canvas"><header><b>-- Moving line (current)</b><b>■ Traces (tangency points)</b><b>■ Envelope</b><button type="button" onClick={()=>act(()=>setTraces([]))}>CLEAR TRACES</button></header><EnvelopeGraph model={model} traces={traces} onSlope={changeSlope}/><footer>Domain shown: x ∈ [−6,6] &nbsp;&nbsp; y ∈ [−4,4]</footer></article><aside className="target-envelope-controls"><h2>Family of lines</h2><b>y = mx + c − m²/4</b><Control label="m (slope)" min={-6} max={6} step={.25} value={m} onChange={changeSlope}/><Control label="c (offset)" min={-6} max={6} step={.25} value={c} onChange={value=>act(()=>setC(value))}/><section><h3>Current line</h3><b>y = {fmt(m)}x {signed(model.intercept)}</b></section><label>Accumulated traces<input aria-label="Accumulated tangency traces" type="checkbox" checked={accumulate} onChange={e=>act(()=>setAccumulate(e.target.checked))}/></label><p>Traces are tangency points.<br/><br/>Count: <b>{traces.length} points</b></p><section><h3>Envelope (detected) ✓</h3><b>y = x² {signed(c)}</b><strong>The envelope is tangent to every line.</strong></section><footer><p>Min point<b>{point({x:0,y:c})}</b></p><p>Vertex<b>{point({x:0,y:c})}</b></p><p>Axis<b>x = 0</b></p><p>Opens<b>Upward</b></p></footer></aside></div></section>
  <section className="target-envelope-explain"><article><h2>⌁ What's happening?</h2><p>The line family moves as m changes.</p><p>Blue points are tangency points between each line and the envelope.</p><p>The purple curve is the envelope: tangent to every line exactly once.</p></article><article><h2>♧ Why tangent?</h2><p>At a tangency point, the line and envelope have the same slope.</p><TangentMini/><footer>Slope of line = slope of envelope at P<br/>⇒ single point of contact.</footer></article><article><h2>▣ Rule (how we find it)</h2><p>Given F(x,y,m)=0 (family of lines), the envelope satisfies:</p><b>F(x,y,m)=0 &nbsp; and &nbsp; ∂F/∂m(x,y,m)=0</b></article></section>
  <section className="target-envelope-learning"><article><h2>Worked example (this family)</h2><p>Family: y = mx + c − m²/4</p><b>F(x,y,m)=y−mx−c+m²/4=0</b><p>∂F/∂m = −x + m/2 = 0 ⇒ m = 2x</p><p>Substitute into F=0:</p><strong>y = x² + c</strong><footer><Check/>Matches the graph.</footer></article><article><h2><TriangleAlert/>Common misconception</h2><b>Misconception: The envelope passes through all points of every line.</b><p><b>Why it’s wrong:</b> Only one point on each line is tangent to the envelope.</p><p><b>Fix:</b> Check tangency — the line just touches the envelope at exactly one point.</p><MisconceptionMini/></article><article><h2>Your turn: challenge</h2><p>Find the envelope of the family<br/>y = mx + 1 − m²/4</p>{[["A","y = x² + 1"],["B","y = x² − 1"],["C","y = −x² + 1"],["D","y = −x² − 1"]].map(([key,text])=><label key={key}><input aria-label={`Envelope challenge ${key}`} type="radio" name="envelope-answer" checked={answer===key} onChange={()=>setAnswer(key)}/>{key}. {text}</label>)}<p role="status" className={feedback.startsWith("Correct")?"correct":"incorrect"}>{feedback.startsWith("Correct")&&<Check/>}{feedback}</p><button type="button" onClick={check}>Check Idea</button></article></section>
  <nav className="target-envelope-nav"><a href="/lessons/geometry/249-moving-linkage-loci"><ArrowLeft/><span><b>Previous</b>Moving-Linkage Loci</span></a><a href="/lessons/geometry/251-dynamic-trace"><span><b>Next</b>Dynamic Trace</span><ArrowRight/></a></nav>
  </section>}

function Control({label,min,max,step,value,onChange}:{label:string;min:number;max:number;step:number;value:number;onChange:(v:number)=>void}){return <label className="target-envelope-control"><span>{label}</span><small>{min}</small><input aria-label={label} type="range" min={min} max={max} step={step} value={value} onChange={e=>onChange(Number(e.target.value))}/><small>{max}</small><input aria-label={`${label} exact value`} type="number" step={step} value={value} onChange={e=>onChange(Number(e.target.value))}/></label>}
function EnvelopeGraph({model,traces,onSlope}:{model:ReturnType<typeof envelopeModel>;traces:number[];onSlope:(m:number)=>void}){const ref=useRef<SVGSVGElement>(null),drag=useRef(false),origin={x:300,y:270},scale=36,s=(p:Point)=>({x:origin.x+p.x*scale,y:origin.y-p.y*scale}),contact=s(model.contact),world=(e:ReactPointerEvent<SVGSVGElement>)=>{const matrix=ref.current?.getScreenCTM();if(!matrix)return null;const q=new DOMPoint(e.clientX,e.clientY).matrixTransform(matrix.inverse()),x=(q.x-origin.x)/scale;return clamp(2*x,-6,6)};const path=Array.from({length:121},(_,i)=>-6+i*.1).map((x,i)=>`${i?"L":"M"}${s({x,y:x*x+model.c}).x},${s({x,y:x*x+model.c}).y}`).join(" "),lineA=s({x:-7,y:model.m*-7+model.intercept}),lineB=s({x:7,y:model.m*7+model.intercept});return <svg ref={ref} className="target-envelope-graph" viewBox="0 0 600 500" role="img" aria-label="Interactive tangent line family and detected parabola envelope" onPointerMove={e=>{if(drag.current){const value=world(e);if(value!==null)onSlope(value)}}} onPointerUp={()=>drag.current=false}><GraphGrid/><line x1="0" x2="600" y1={origin.y} y2={origin.y} stroke="#334155"/><line x1={origin.x} x2={origin.x} y1="0" y2="500" stroke="#334155"/><path data-testid="envelope-curve" d={path} fill="none" stroke="#8b3de2" strokeWidth="4"/>{traces.map((value,index)=>{const p=s({x:value/2,y:value*value/4+model.c});return <circle key={`trace-${index}`} cx={p.x} cy={p.y} r="4" fill="#38bdf8"/>})}<line data-testid="envelope-current-line" x1={lineA.x} y1={lineA.y} x2={lineB.x} y2={lineB.y} stroke="#64748b" strokeWidth="2" strokeDasharray="7 4"/><circle data-testid="envelope-contact-point" data-x={model.contact.x.toFixed(3)} data-y={model.contact.y.toFixed(3)} cx={contact.x} cy={contact.y} r="7" fill="#f59e0b" onPointerDown={e=>{drag.current=true;e.currentTarget.setPointerCapture(e.pointerId)}}/><text x={contact.x+9} y={contact.y-9} fontSize="12" fontWeight="800">P {point(model.contact)}</text></svg>}
function GraphGrid(){return <g stroke="#e8eef5">{Array.from({length:25},(_,i)=><line key={`v${i}`} x1={i*25} x2={i*25} y1="0" y2="500"/>)}{Array.from({length:21},(_,i)=><line key={`h${i}`} x1="0" x2="600" y1={i*25} y2={i*25}/>)}</g>}
function TangentMini(){return <svg viewBox="0 0 220 90"><path d="M15 70 Q110 5 205 70" fill="none" stroke="#8b5cf6" strokeWidth="2"/><line x1="28" x2="194" y1="62" y2="32" stroke="#94a3b8" strokeDasharray="5 3"/><circle cx="110" cy="47" r="5" fill="#111827"/><text x="116" y="43" fontSize="10">P</text></svg>}function MisconceptionMini(){return <svg viewBox="0 0 220 100"><path d="M20 85 Q110 5 200 85" fill="none" stroke="#8b5cf6" strokeWidth="2"/>{[25,55,90,125,160].map((x,i)=><line key={x} x1={x} x2={x+50} y1={85-i*12} y2={30+i*8} stroke="#94a3b8"/>)}{[55,90,125].map((x,i)=><circle key={x} cx={x} cy={58-i*9} r="4" fill="#06a7ca"/>)}</svg>}
function envelopeModel(m:number,c:number){const contact={x:m/2,y:m*m/4+c},intercept=c-m*m/4;return{m,c,contact,intercept}}function signed(n:number){return n<0?`− ${fmt(Math.abs(n))}`:`+ ${fmt(n)}`}function point(p:Point){return`(${fmt(p.x)}, ${fmt(p.y)})`}function fmt(n:number){return Number(n.toFixed(2)).toString()}function clamp(n:number,min:number,max:number){return Math.min(max,Math.max(min,Number.isFinite(n)?n:min))}
