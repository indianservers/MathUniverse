import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it,vi}from"vitest";import{lessonCatalog}from"../catalog/lessonCatalog";import ProbabilityLessonAdapter from"./ProbabilityLessonAdapter";
describe("Distribution Calculator dedicated surface",()=>{it("routes lesson 516 to its linked PDF and CDF calculator",()=>{const lesson=lessonCatalog.find(item=>item.id===516)!;const html=renderToStaticMarkup(<ProbabilityLessonAdapter lesson={lesson} resetToken={0} onInteraction={vi.fn()}/>);expect(html).toContain('data-testid="probability-mockup-0479"');expect(html).toContain("Probability Density Function (PDF)");expect(html).toContain("Cumulative Distribution Function (CDF)");expect(html).toContain("0.6827");});});
