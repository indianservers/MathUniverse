import { useEffect, useMemo, useState, type DragEvent } from "react";
import { ArrowLeft, ArrowRight, Check, CircleAlert, RefreshCw, RotateCcw, Sparkles, X } from "lucide-react";
import type { LessonAdapterProps } from "../types";
import "./FactorTheoremTargetLesson105.css";

type Parsed = { coefficients: number[]; valid: boolean };
const problems = [
  { polynomial: "x² − 3x + 2", factor: "x − 1" },
  { polynomial: "x² − 5x + 6", factor: "x − 2" },
  { polynomial: "x² + x − 6", factor: "x + 3" },
];
const superscripts: Record<string, string> = { "⁰": "0", "¹": "1", "²": "2", "³": "3", "⁴": "4", "⁵": "5" };
function parsePolynomial(source: string): Parsed { const terms = source.replace(/[⁰¹²³⁴⁵]/g, (value) => `^${superscripts[value]}`).replaceAll("−", "-").replace(/\s+/g, "").replace(/-/g, "+-").split("+").filter(Boolean); const values = new Map<number, number>(); for (const term of terms) { const variable = term.match(/^([+-]?)(\d*)x(?:\^([0-5]))?$/i); const constant = term.match(/^([+-]?\d+)$/); if (variable) { const sign = variable[1] === "-" ? -1 : 1; const coefficient = variable[2] ? sign * Number(variable[2]) : sign; const degree = variable[3] ? Number(variable[3]) : 1; values.set(degree, (values.get(degree) ?? 0) + coefficient); } else if (constant) values.set(0, (values.get(0) ?? 0) + Number(constant[1])); else return { coefficients: [], valid: false }; } const degree = Math.max(0, ...values.keys()); return { coefficients: Array.from({ length: degree + 1 }, (_, index) => values.get(degree - index) ?? 0), valid: terms.length > 0 && degree > 0 }; }
function parseFactor(source: string) { const match = source.replaceAll("−", "-").replace(/\s+/g, "").match(/^x(?:([+-])(\d+))?$/i); return match ? { valid: true, root: match[1] === "+" ? -Number(match[2]) : match[1] === "-" ? Number(match[2]) : 0 } : { valid: false, root: 0 }; }
function evaluate(coefficients: number[], value: number) { return coefficients.reduce((result, coefficient) => result * value + coefficient, 0); }
function divide(coefficients: number[], root: number) { const products = Array(coefficients.length).fill(0) as number[]; const sums = Array(coefficients.length).fill(0) as number[]; sums[0] = coefficients[0] ?? 0; for (let index = 1; index < coefficients.length; index += 1) { products[index] = sums[index - 1] * root; sums[index] = coefficients[index] + products[index]; } return { products, sums, quotient: sums.slice(0, -1), remainder: sums.at(-1) ?? 0 }; }
function formatPolynomial(coefficients: number[]) { const degree = coefficients.length - 1; const parts = coefficients.flatMap((coefficient, index) => { if (!coefficient) return []; const power = degree - index; const symbol = power === 0 ? "" : power === 1 ? "x" : `x${["⁰", "¹", "²", "³", "⁴", "⁵"][power]}`; const magnitude = symbol && Math.abs(coefficient) === 1 ? symbol : `${Math.abs(coefficient)}${symbol}`; return [{ coefficient, magnitude }]; }); return parts.length ? parts.map((part, index) => `${index === 0 ? (part.coefficient < 0 ? "−" : "") : part.coefficient < 0 ? " − " : " + "}${part.magnitude}`).join("") : "0"; }
function substitution(coefficients: number[], value: number) { const degree = coefficients.length - 1; return coefficients.map((coefficient, index) => { const power = degree - index; const factor = coefficient * value ** power; return `${factor}`; }).join(" + ").replaceAll("+ -", "− "); }
function factorText(root: number) { return root < 0 ? `x + ${Math.abs(root)}` : root === 0 ? "x" : `x − ${root}`; }

export default function FactorTheoremTargetLesson105({ resetToken, onInteraction }: LessonAdapterProps) {
  const [problemIndex, setProblemIndex] = useState(0);
  const [polynomialInput, setPolynomialInput] = useState(problems[0].polynomial);
  const [factorInput, setFactorInput] = useState(problems[0].factor);
  const [testValue, setTestValue] = useState(1);
  const [substitute, setSubstitute] = useState(true);
  const [checkZero, setCheckZero] = useState(true);
  const [revealPair, setRevealPair] = useState(true);
  const [tab, setTab] = useState("Interact");
  const [dragging, setDragging] = useState("");
  const [factorDrops, setFactorDrops] = useState(0);
  const [valueDrops, setValueDrops] = useState(0);
  const [invalidDrop, setInvalidDrop] = useState("");
  const [practiceSelected, setPracticeSelected] = useState(3);
  const [actions, setActions] = useState(0);
  const parsed = useMemo(() => parsePolynomial(polynomialInput), [polynomialInput]);
  const factor = useMemo(() => parseFactor(factorInput), [factorInput]);
  const division = useMemo(() => divide(parsed.coefficients, factor.root), [parsed.coefficients, factor.root]);
  const value = evaluate(parsed.coefficients, testValue);
  const isFactor = parsed.valid && factor.valid && testValue === factor.root && value === 0 && division.remainder === 0;
  const otherRoot = division.quotient.length === 2 && division.quotient[0] !== 0 ? -division.quotient[1] / division.quotient[0] : Number.NaN;
  const meter = Math.max(0, Math.min(100, 50 + value * 5));
  const practicePolynomial = parsePolynomial("x² − 5x + 6");
  const practiceValue = evaluate(practicePolynomial.coefficients, practiceSelected);
  const act = () => { setActions((count) => count + 1); onInteraction(); };
  const reset = () => { setProblemIndex(0); setPolynomialInput(problems[0].polynomial); setFactorInput(problems[0].factor); setTestValue(1); setSubstitute(true); setCheckZero(true); setRevealPair(true); setTab("Interact"); setDragging(""); setFactorDrops(0); setValueDrops(0); setInvalidDrop(""); setPracticeSelected(3); setActions(0); onInteraction(); };
  useEffect(() => reset(), [resetToken]); // eslint-disable-line react-hooks/exhaustive-deps
  const updateFactor = (source: string) => { setFactorInput(source); const next = parseFactor(source); if (next.valid) setTestValue(next.root); act(); };
  const startDrag = (event: DragEvent<HTMLButtonElement>, kind: "factor" | "value") => { const payload = kind === "factor" ? factorInput : String(testValue); event.dataTransfer.setData(`text/factor-theorem-${kind}`, payload); setDragging(`${kind}:${payload}`); setInvalidDrop(""); act(); };
  const drop = (event: DragEvent<HTMLElement>, kind: "factor" | "value") => { event.preventDefault(); const payload = event.dataTransfer.getData(`text/factor-theorem-${kind}`); const valid = kind === "factor" ? payload === factorInput : Number(payload) === testValue; if (valid) { if (kind === "factor") setFactorDrops((count) => count + 1); else setValueDrops((count) => count + 1); setInvalidDrop(""); } else setInvalidDrop(kind); setDragging(""); act(); };
  const newProblem = () => { const next = (problemIndex + 1) % problems.length; const problem = problems[next]; const root = parseFactor(problem.factor).root; setProblemIndex(next); setPolynomialInput(problem.polynomial); setFactorInput(problem.factor); setTestValue(root); setInvalidDrop(""); act(); };

  return <div className="factor105-page" data-testid="algebra-mockup-0162" data-dedicated-lesson="105" data-object-model="editable-polynomial-candidate-factor-root-extraction-draggable-substitution-zero-meter-synthetic-remainder-factor-pair-practice-model" data-polynomial={formatPolynomial(parsed.coefficients)} data-polynomial-valid={parsed.valid} data-factor={factorInput} data-factor-valid={factor.valid} data-factor-root={factor.root} data-test-value={testValue} data-evaluated={value} data-products={division.products.join(",")} data-sums={division.sums.join(",")} data-quotient={formatPolynomial(division.quotient)} data-remainder={division.remainder} data-is-factor={isFactor} data-other-root={Number.isNaN(otherRoot) ? "" : otherRoot} data-meter={meter} data-substitute={substitute} data-check-zero={checkZero} data-reveal-pair={revealPair} data-tab={tab} data-dragging={dragging} data-factor-drops={factorDrops} data-value-drops={valueDrops} data-invalid-drop={invalidDrop} data-practice-selected={practiceSelected} data-practice-value={practiceValue} data-actions={actions}>
    <nav className="factor105-breadcrumb"><a href="/">Home</a><span>&gt;</span><a href="/lessons">Lessons</a><span>&gt;</span><a href="/lessons/algebra">Algebra</a><span>&gt;</span><b>105 Factor Theorem</b></nav>
    <header className="factor105-intro"><small><b>ALGEBRA</b><b>EXPRESSIONS AND MANIPULATION</b></small><h1>Factor Theorem</h1><p><i>x − a</i> is a factor of <i>f(x)</i> if and only if <i>f(a) = 0</i>.</p><nav><b>Intermediate Algebra</b><b>6-10 min</b><b>Factor test</b></nav></header>
    <nav className="factor105-tabs">{[["Interact", "Test and explore"], ["Explain", "Understand the idea"], ["Examples", "See it in action"], ["Formulas", "Key formulas"], ["Know more", "Deepen understanding"]].map(([name, detail]) => <button type="button" className={tab === name ? "active" : ""} onClick={() => { setTab(name); act(); }} key={name}><b>{name}</b><span>{detail}</span></button>)}</nav>
    <main className="factor105-station"><header><h2>Factor Test Station</h2><p>Test whether <i>x − a</i> is a factor of <i>f(x)</i></p><button type="button" onClick={reset}><RotateCcw />Reset</button><button type="button" onClick={newProblem}><RefreshCw />New problem</button></header><section className="factor105-inputs"><label><b><i>1</i>Polynomial f(x)</b><small>Enter the function</small><span>f(x) =<input aria-label="Factor theorem polynomial" value={polynomialInput} onChange={(event) => { setPolynomialInput(event.target.value); act(); }} />{parsed.valid && <Check />}</span></label><label><b><i>2</i>Candidate factor</b><small>Enter divisor of the form x − a</small><span><input aria-label="Candidate factor" value={factorInput} onChange={(event) => updateFactor(event.target.value)} />{factor.valid && <Check />}</span></label><label><b><i>3</i>Test value a</b><small>a is the number you substitute</small><span>a =<input aria-label="Factor test value" type="number" value={testValue} onChange={(event) => { setTestValue(Number(event.target.value)); act(); }} />{testValue === factor.root && <Check />}</span></label></section>
      <section className="factor105-pipeline"><button type="button" draggable aria-label="Drag candidate factor" onDragStart={(event) => startDrag(event, "factor")} onDragEnd={() => setDragging("")}><b>Divisor</b><strong>{factorInput}</strong></button><i>→</i><article aria-label="Factor extraction drop target" onDragOver={(event) => event.preventDefault()} onDrop={(event) => drop(event, "factor")}><b>Extract a</b><strong>{factorInput} ⇒ a = {factor.root}</strong></article><i>→</i><article aria-label="Substitution value drop target" onDragOver={(event) => event.preventDefault()} onDrop={(event) => drop(event, "value")}><b>Substitute a into f(x)</b><p>Evaluate <button type="button" draggable aria-label="Drag extracted a" onDragStart={(event) => startDrag(event, "value")} onDragEnd={() => setDragging("")}>f({testValue})</button></p></article></section>
      <section className="factor105-test"><article><h3>Factor Theorem</h3><p><i>x − a</i> is a factor of <i>f(x)</i><br />iff <i>f(a) = 0</i>.</p></article><i>→</i><article className="meter"><h3>Substitution meter</h3>{substitute ? <><div><span style={{ left: `${meter}%` }} /></div><nav><b>−10</b><b>−5</b><b>0</b><b>5</b><b>10</b></nav><p><i>f({testValue})</i> = {substitution(parsed.coefficients, testValue)} = <b>{value}</b></p></> : <p>Substitution hidden</p>}</article><i>→</i><article className="zero"><h3>Zero target</h3>{checkZero ? <strong className={value === 0 ? "hit" : "miss"}>{value}</strong> : <p>Zero check hidden</p>}</article><i>→</i><article className={`verdict ${isFactor ? "yes" : "no"}`}><h3>Result</h3><strong>f({testValue}) = {value}</strong><p>{factorInput} is {isFactor ? "a factor" : "not a factor"}</p><b>{isFactor ? `Yes: ${factorInput} is a factor` : `No: ${factorInput} is not a factor`}{isFactor ? <Check /> : <X />}</b></article></section>
      <section className="factor105-controls"><h3>Controls</h3><Switch label="Substitute" detail="Evaluate f(a)" value={substitute} onToggle={() => { setSubstitute((state) => !state); act(); }} /><Switch label="Check zero" detail="Compare to 0" value={checkZero} onToggle={() => { setCheckZero((state) => !state); act(); }} /><Switch label="Reveal factor pair" detail="Show full factorization" value={revealPair} onToggle={() => { setRevealPair((state) => !state); act(); }} /></section>
      <section className="factor105-synthetic"><header><h2>Check by synthetic division</h2><p>Optional verification: divide f(x) by ({factorInput})</p></header><MiniSynthetic coefficients={parsed.coefficients} root={factor.root} division={division} /><article><strong>Remainder = {division.remainder}</strong><p>Confirms: {factorInput} is {division.remainder === 0 ? "a factor" : "not a factor"}.</p></article><aside><h3>Factorization revealed</h3>{revealPair && !Number.isNaN(otherRoot) ? <><strong>f(x) = ({factorText(factor.root)})({factorText(otherRoot)})</strong><p>Factor pair: ({factor.root}, {otherRoot}) <span>Zeros: x = {factor.root}, x = {otherRoot}</span></p></> : <p>Factor pair hidden</p>}</aside></section>
      <section className="factor105-practice"><h2>Practice: Test different factor candidates</h2><article className="factor"><h3>g(x) = x² − 5x + 6</h3><button type="button" onClick={() => { setPracticeSelected(3); act(); }}>Test x − 3<br />a = 3</button><div><b>Substitution</b><p>g(3) = 3² − 5(3) + 6 = 0</p><strong>Yes: x − 3 is a factor <Check /></strong></div><footer>Factorization <b>g(x) = (x − 2)(x − 3)</b><span>Zeros: x = 2, x = 3</span></footer></article><article className="nonfactor"><h3>Test a non-factor</h3><button type="button" onClick={() => { setPracticeSelected(4); act(); }}>Test x − 4<br />a = 4</button><div><b>Substitution</b><p>g(4) = 4² − 5(4) + 6 = 2</p><strong>Not a factor <X /></strong></div><footer>Remainder = 2 ≠ 0<br /><b>So, x − 4 is not a factor of g(x).</b></footer></article></section>
      <aside className="factor105-warning"><CircleAlert /><b>Important</b><p>A nonzero remainder means the candidate is not a factor. Only when f(a) = 0 (remainder 0) is x − a a factor.</p></aside>{invalidDrop && <p className="factor105-invalid">Complete the matching {invalidDrop} drag stage.</p>}
    </main>
    <nav className="factor105-navigation"><a href="/lessons/algebra/104-remainder-theorem"><ArrowLeft /><span>Previous<b>Remainder Theorem</b></span></a><a href="/lessons/algebra/106-identities"><span>Next<b>More examples</b></span><ArrowRight /></a></nav>
    <footer className="factor105-footer"><h3><Sparkles />Math Universe</h3><p>Interactive math labs, visual proofs, NCERT explorations,<br />graphing, CAS-style tools, and classroom-ready activities.</p><nav><a href="/sitemap">Sitemap</a><a href="/docs">Docs</a><a href="/about">About</a></nav><hr /><small>© 2026 INDIAN SERVERS PRIVATE LIMITED. NO RIGHT TO REPRODUCE IT.</small><p>www.IndianServers.com info@IndianServers.com</p></footer>
  </div>;
}

function Switch({ label, detail, value, onToggle }: { label: string; detail: string; value: boolean; onToggle: () => void }) { return <button type="button" role="switch" aria-checked={value} onClick={onToggle}><i className={value ? "on" : ""}><b /></i><span><b>{label}</b><small>{detail}</small></span></button>; }
function MiniSynthetic({ coefficients, root, division }: { coefficients: number[]; root: number; division: ReturnType<typeof divide> }) { return <div className="factor105-mini" style={{ "--factor-columns": coefficients.length } as React.CSSProperties}><b>{root}</b><header>{coefficients.map((value, index) => <span key={index}>{value}</span>)}</header><section><i>↓</i>{division.products.map((value, index) => <span key={index}>{index ? value : ""}</span>)}</section><footer>{division.sums.map((value, index) => <span className={index === division.sums.length - 1 ? "remainder" : ""} key={index}>{value}</span>)}</footer></div>; }
