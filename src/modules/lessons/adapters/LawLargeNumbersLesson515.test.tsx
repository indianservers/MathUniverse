import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it,vi}from"vitest";import{lessonCatalog}from"../catalog/lessonCatalog";import ProbabilityLessonAdapter from"./ProbabilityLessonAdapter";
describe("Law of Large Numbers dedicated surface",()=>{it("routes lesson 515 to its live convergence lab",()=>{const lesson=lessonCatalog.find(item=>item.id===515)!;const html=renderToStaticMarkup(<ProbabilityLessonAdapter lesson={lesson} resetToken={0} onInteraction={vi.fn()}/>);expect(html).toContain('data-testid="probability-mockup-0478"');expect(html).toContain("Live Simulation: Watch Convergence in Action");expect(html).toContain("0.503");expect(html).toContain("95% CI Half-Width");});});
