import {
  ArrowLeft,
  ArrowRight,
  Bookmark,
  Check,
  Ellipsis,
  GripVertical,
  Info,
  Lightbulb,
  Maximize2,
  RotateCcw,
  Share2,
  Trash2,
} from "lucide-react";
import {
  useEffect,
  useMemo,
  useRef,
  useState,
  type PointerEvent as ReactPointerEvent,
} from "react";
import type { LessonAdapterProps } from "../types";

type Point={x:number;y:number};
type Visibility={labels:boolean;grid:boolean;circle:boolean;bisectors:boolean;centre:boolean};
const initialPoints:Point[]=[{x:0,y:3.5},{x:-3,y:-1},{x:3,y:-1}];
const letters=['A','B','C'];
const initialVisibility:Visibility={labels:true,grid:true,circle:true,bisectors:true,centre:true};

export default function CircleThreePointsTargetLesson220({resetToken,onInteraction}:LessonAdapterProps){
  const [points,setPoints]=useState<Point[]>(initialPoints);
  const [visibility,setVisibility]=useState(initialVisibility);
  const [drag,setDrag]=useState<number|null>(null);
  const [answers,setAnswers]=useState(['','','','','','']);
  const [feedback,setFeedback]=useState<'idle'|'correct'|'incorrect'>('idle');
  const surfaceRef=useRef<HTMLElement>(null);
  const model=useMemo(()=>circumcircle(points),[points]);
  const reset=()=>{setPoints(initialPoints);setVisibility(initialVisibility);setAnswers(['','','','','','']);setFeedback('idle');onInteraction()};
  useEffect(()=>{setPoints(initialPoints);setVisibility(initialVisibility);setAnswers(['','','','','','']);setFeedback('idle')},[resetToken]);
  const updatePoint=(index:number,point:Point)=>{setPoints(current=>current.map((value,i)=>i===index?{x:clamp(point.x,-5.5,5.5),y:clamp(point.y,-4.5,4.5)}:value));setFeedback('idle');onInteraction()};
  const toggle=(key:keyof Visibility)=>{setVisibility(value=>({...value,[key]:!value[key]}));onInteraction()};
  const check=()=>{if(!model.valid){setFeedback('incorrect');onInteraction();return}const expected=[model.centre.x,model.centre.y,model.radius,model.centre.x,model.centre.y,model.radius*model.radius];const right=answers.every((value,index)=>Math.abs(Number(value)-expected[index])<.06)&&Math.abs(model.centre.y)<.08;setFeedback(right?'correct':'incorrect');onInteraction()};

  return <section ref={surfaceRef} className="space-y-3" style={{marginTop:-7}} data-testid="dynamic-geometry-mockup-0277" data-dedicated-lesson="220" data-object-model="three-point-circumcircle" data-direct-interaction="true" aria-label="Circle Through Three Points dedicated interactive geometry model">
    <span className="sr-only">Live Verification. Check Construction.</span>
    <header className="px-1 py-2"><div className="flex items-start justify-between gap-5"><div><div className="flex gap-2"><span className="rounded-full bg-blue-50 px-3 py-1 text-[7px] font-black uppercase text-blue-700">Geometry</span><span className="rounded-full bg-slate-100 px-3 py-1 text-[7px] font-black uppercase text-slate-600">Dynamic Geometry Constructions</span></div><h1 className="mt-3 text-[27px] font-black leading-8 text-slate-950">Circle through Three Points</h1><p className="mt-1 text-[10px] text-slate-600">Construct the unique circle passing through any three non-collinear points.</p><div className="mt-4 flex gap-2 text-[8px] font-bold"><span className="target-geometry-chip">Foundation-Advanced</span><span className="target-geometry-chip">Construction Studio</span><span className="target-geometry-chip">Geometry Tools</span><span className="target-geometry-chip">6-10 min</span></div></div><div className="mt-10 flex gap-3"><button type="button" className="target-three-icon" aria-label="Bookmark lesson"><Bookmark/></button><button type="button" className="target-three-icon" aria-label="Share lesson" onClick={()=>{void navigator.clipboard?.writeText(location.href);onInteraction()}}><Share2/></button><button type="button" className="target-three-icon" aria-label="More lesson options"><Ellipsis/></button></div></div></header>
    <nav className="grid grid-cols-5 overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm">{['Explore','Learn','Example','Insight','Practice'].map((label,index)=><button type="button" key={label} className={`h-12 border-b-2 text-[10px] font-black ${index===0?'border-purple-600 text-purple-700':'border-transparent text-slate-700'}`} onClick={()=>{document.getElementById(index===4?'three-practice':index===3?'three-insight':index===1?'three-steps':'three-main')?.scrollIntoView({behavior:'smooth',block:'center'});onInteraction()}}>{label}</button>)}</nav>

    <section id="three-main" className="rounded-xl border border-slate-200 bg-white p-3 shadow-sm"><div className="flex justify-between gap-3"><div><h2 className="text-[14px] font-black">1. Observe &amp; Manipulate</h2><p className="mt-1 text-[10px] text-slate-600">Drag the blue points A, B, C. The circle updates in real time.</p></div><div className="flex items-center gap-4 text-[8px]"><Switch label="Labels" checked={visibility.labels} onChange={()=>toggle('labels')}/><Switch label="Grid" checked={visibility.grid} onChange={()=>toggle('grid')}/><button type="button" className="target-three-icon" aria-label="Full screen construction" onClick={()=>{void surfaceRef.current?.requestFullscreen?.();onInteraction()}}><Maximize2/></button></div></div><div className="mt-3 grid gap-3 md:grid-cols-[minmax(0,2fr)_minmax(210px,1fr)]"><div className="overflow-hidden rounded-lg border border-slate-200"><CircumcircleCanvas points={points} model={model} visibility={visibility} drag={drag} onDrag={setDrag} onPoint={updatePoint} onAdd={point=>{if(points.length<3){setPoints(current=>[...current,point]);onInteraction()}}}/><p className="m-2 flex items-center gap-2 rounded-md border border-blue-200 bg-slate-50 px-3 py-2 text-[8px] text-slate-700"><Info className="h-3.5 w-3.5 text-cyan-600"/>Circumcentre O is the intersection of the perpendicular bisectors of AB, BC and CA.</p></div><aside className="space-y-2"><section className="rounded-xl border border-slate-200 p-3"><h3 className="text-[10px] font-black">Points</h3>{points.map((point,index)=><div key={index} className="mt-3 grid grid-cols-[14px_18px_1fr_16px_20px] items-center gap-2 text-[9px]"><span className="h-2.5 w-2.5 rounded-full bg-blue-500"/><b>{letters[index]}</b><span>({point.x.toFixed(2)}, {point.y.toFixed(2)})</span><GripVertical className="h-4 w-4 text-slate-400"/><button type="button" aria-label={`Remove point ${letters[index]}`} onClick={()=>{setPoints(current=>current.filter((_,i)=>i!==index));setFeedback('idle');onInteraction()}}><Trash2 className="h-4 w-4"/></button></div>)}<button type="button" className="target-geometry-action mt-4" onClick={reset}><RotateCcw/>Reset Points</button>{points.length<3&&<p className="mt-2 text-[8px] text-amber-700">Click the graph to add point {letters[points.length]}.</p>}</section><section className="rounded-xl border border-slate-200 p-3"><h3 className="text-[10px] font-black">Objects</h3><VisibilityRow label="Circle (through A, B, C)" checked={visibility.circle} mark="solid" onChange={()=>toggle('circle')}/><VisibilityRow label="Perpendicular bisectors" checked={visibility.bisectors} mark="dashed" onChange={()=>toggle('bisectors')}/><VisibilityRow label="Circumcentre O" checked={visibility.centre} mark="point" onChange={()=>toggle('centre')}/></section><section className={`rounded-xl border p-3 text-[9px] ${model.valid?'border-emerald-200 bg-emerald-50':'border-amber-300 bg-amber-50'}`}><h3 className={`font-black ${model.valid?'text-emerald-700':'text-amber-800'}`}>{model.valid?'Observation':'No unique circle'}</h3>{model.valid?<><p className="mt-2">A circle passes through A, B and C.</p><p className="mt-2">The circumcentre O is equidistant from all three points.</p><div className="mt-3 rounded-md bg-white p-2 font-serif text-purple-700"><Property label="OA" value={model.radius.toFixed(2)}/><Property label="OB" value={distance(model.centre,points[1]).toFixed(2)}/><Property label="OC" value={distance(model.centre,points[2]).toFixed(2)}/></div><small className="mt-2 block">Values rounded to 2 d.p.</small></>:<p className="mt-2">Three non-collinear points are required. Move or add a point to continue.</p>}</section></aside></div></section>

    <section className="grid gap-3 md:grid-cols-[1.1fr_1fr]" style={{marginTop:10}}><article id="three-steps" className="target-three-card"><h2>2. Construction Steps</h2><div className="grid grid-cols-[1fr_150px] gap-2"><div>{['Construct segment AB and its perpendicular bisector.','Construct segment BC and its perpendicular bisector.','The bisectors intersect at O, the circumcentre.','With centre O and radius OA, draw the circle.'].map((text,index)=><p key={text} className="target-three-step"><b>{index+1}</b>{text}</p>)}<div className="mt-3 rounded-md border border-amber-300 bg-amber-50 p-2"><Lightbulb className="mr-2 inline h-4 w-4 text-amber-500"/><b>Tip</b><p className="mt-1">If the three points are collinear, the bisectors are parallel and no unique circle exists.</p></div></div><MiniCircumcircle/></div></article><article id="three-insight" className="target-three-card"><h2>3. Insight</h2><p>A circle is uniquely determined by three non-collinear points.</p><h3 className="mt-4 font-black text-blue-700">Circumcentre <span className="font-normal text-slate-700">(Intersection of perpendicular bisectors)</span></h3><div className="mt-3 flex gap-5"><Formula>O = (x0, y0)</Formula><Formula>OA = OB = OC = R</Formula></div><h3 className="mt-5 font-black text-blue-700">Equation of the circle</h3><div className="mt-3 flex items-center gap-4"><Formula>(x - x0)^2 + (y - y0)^2 = R^2</Formula><span>where R = OA</span></div></article></section>
    <section id="three-practice" className="h-[159px] overflow-hidden rounded-xl border border-slate-200 bg-white px-3 py-2 shadow-sm" style={{marginTop:10}}><h2 className="text-[13px] font-black">4. Try It Yourself</h2><p className="mt-1 text-[9px]">Drag the points to new positions and record the circumcentre and radius.</p><div className="mt-2 grid gap-2 md:grid-cols-[1.05fr_1fr_.55fr_2.2fr]"><div className="target-three-practice"><b className="text-blue-700">Task</b><p className="mt-2">Place the three points so that O lies on the x-axis.</p><p className="mt-1">Is it possible?</p><button type="button" className="target-three-check" onClick={check}><Check/>Check Answer</button></div><div className="target-three-practice"><b className="text-blue-700">Circumcentre O</b><div className="mt-4 flex gap-2"><Input label="Circumcentre x" prefix="x0 =" value={answers[0]} onChange={value=>setAnswer(0,value,answers,setAnswers,setFeedback)}/><Input label="Circumcentre y" prefix="y0 =" value={answers[1]} onChange={value=>setAnswer(1,value,answers,setAnswers,setFeedback)}/></div></div><div className="target-three-practice"><b className="text-blue-700">Radius R</b><Input label="Circumcircle radius" prefix="R =" value={answers[2]} onChange={value=>setAnswer(2,value,answers,setAnswers,setFeedback)}/></div><div className="target-three-practice"><b>Equation of the circle</b><div className="mt-4 flex items-center gap-1 font-serif text-[11px]">(x - <BareInput label="Equation h" value={answers[3]} onChange={value=>setAnswer(3,value,answers,setAnswers,setFeedback)}/>)^2 + (y - <BareInput label="Equation k" value={answers[4]} onChange={value=>setAnswer(4,value,answers,setAnswers,setFeedback)}/>)^2 = <BareInput label="Equation radius squared" value={answers[5]} onChange={value=>setAnswer(5,value,answers,setAnswers,setFeedback)}/></div></div></div>{feedback!=='idle'&&<p role="status" className={`mt-1 text-right text-[8px] font-black ${feedback==='correct'?'text-emerald-700':'text-rose-700'}`}>{feedback==='correct'?'Correct circumcircle construction.':'Place O on the x-axis and record the live values.'}</p>}</section>
    <nav className="relative grid grid-cols-2 gap-3" style={{marginTop:10}} aria-label="Adjacent lessons"><a className="target-three-nav" href="/lessons/geometry/219-circle-centre-and-radius"><ArrowLeft/><span><b>Previous</b>Circle: Centre and Radius</span></a><a className="target-three-nav justify-end text-right" href="/lessons/geometry/221-compass"><span><b>Next</b>Compass</span><ArrowRight/></a><a className="absolute left-1/2 top-[58px] flex -translate-x-1/2 items-center gap-2 rounded-lg border bg-white px-8 py-2 text-[8px]" href="/lessons">Back to Lessons</a></nav>
  </section>
}

function CircumcircleCanvas({points,model,visibility,drag,onDrag,onPoint,onAdd}:{points:Point[];model:ReturnType<typeof circumcircle>;visibility:Visibility;drag:number|null;onDrag:(index:number|null)=>void;onPoint:(index:number,point:Point)=>void;onAdd:(point:Point)=>void}){const domain=(event:ReactPointerEvent<SVGSVGElement>)=>{const matrix=event.currentTarget.getScreenCTM();if(!matrix)return{x:0,y:0};const local=new DOMPoint(event.clientX,event.clientY).matrixTransform(matrix.inverse());return{x:(local.x-350)/55,y:(300-local.y)/55}};const screen=(p:Point)=>({x:350+p.x*55,y:300-p.y*55});return <svg role="img" aria-label="Interactive circumcircle through draggable points A B and C" className="h-[490px] w-full touch-none" viewBox="0 0 700 600" onPointerDown={event=>{if(points.length<3)onAdd(domain(event))}} onPointerMove={event=>{if(drag!==null)onPoint(drag,domain(event))}} onPointerUp={()=>onDrag(null)} onPointerCancel={()=>onDrag(null)}><defs><pattern id="three-grid" width="55" height="55" patternUnits="userSpaceOnUse"><path d="M55 0H0V55" fill="none" stroke="#dce7f2"/></pattern></defs>{visibility.grid&&<rect width="700" height="600" fill="url(#three-grid)"/>}<line x1="0" y1="300" x2="700" y2="300" stroke="#94a3b8"/><line x1="350" y1="0" x2="350" y2="600" stroke="#94a3b8"/>{model.valid&&visibility.bisectors&&points.map((point,index)=>{const next=points[(index+1)%3],mid={x:(point.x+next.x)/2,y:(point.y+next.y)/2},direction={x:-(next.y-point.y),y:next.x-point.x},length=Math.hypot(direction.x,direction.y),a={x:mid.x-direction.x/length*12,y:mid.y-direction.y/length*12},b={x:mid.x+direction.x/length*12,y:mid.y+direction.y/length*12},sa=screen(a),sb=screen(b);return <line key={index} x1={sa.x} y1={sa.y} x2={sb.x} y2={sb.y} stroke="#64748b" strokeDasharray="6 5"/>})}{model.valid&&visibility.circle&&<circle data-testid="three-point-circle" data-center-x={model.centre.x.toFixed(4)} data-center-y={model.centre.y.toFixed(4)} data-radius={model.radius.toFixed(4)} cx={screen(model.centre).x} cy={screen(model.centre).y} r={model.radius*55} fill="none" stroke="#8b5cf6" strokeWidth="2.5"/>}{model.valid&&visibility.centre&&<g><circle data-testid="circumcentre-o" cx={screen(model.centre).x} cy={screen(model.centre).y} r="7" fill="#7c3aed"/><text x={screen(model.centre).x+14} y={screen(model.centre).y+20} fill="#7c3aed" fontSize="14">O ({format(model.centre.x)}, {format(model.centre.y)})</text></g>}{points.map((point,index)=>{const p=screen(point);return <g key={index}><circle data-testid={`circumcircle-point-${index}`} cx={p.x} cy={p.y} r="8" fill="#1683e6" className="cursor-move" onPointerDown={event=>{event.stopPropagation();event.currentTarget.ownerSVGElement?.setPointerCapture(event.pointerId);onDrag(index)}}/>{visibility.labels&&<text x={p.x+(index===0?14:index===1?-72:14)} y={p.y+(index===0?-12:18)} fill="#0863be" fontSize="14" fontWeight="800">{letters[index]} ({format(point.x)}, {format(point.y)})</text>}</g>})}</svg>}
function Switch({label,checked,onChange}:{label:string;checked:boolean;onChange:()=>void}){return <label className="flex items-center gap-2">{label}<input type="checkbox" checked={checked} onChange={onChange}/></label>}
function VisibilityRow({label,checked,mark,onChange}:{label:string;checked:boolean;mark:string;onChange:()=>void}){return <label className="mt-4 flex items-center gap-2 text-[8px]"><span className={`h-2 w-2 ${mark==='point'?'rounded-full bg-purple-600':mark==='solid'?'rounded-full border border-purple-500':'text-slate-500'}`}/><span>{label}</span><span className={`ml-auto w-8 border-t ${mark==='dashed'?'border-dashed border-slate-500':'border-purple-600'}`}/><input type="checkbox" checked={checked} onChange={onChange}/></label>}
function Property({label,value}:{label:string;value:string}){return <p className="flex justify-center gap-4 py-1"><span>{label}</span><span>=</span><b>{value}</b></p>}
function Formula({children}:{children:React.ReactNode}){return <p className="rounded-md border border-purple-400 px-3 py-2 font-serif text-[12px] italic text-purple-700">{children}</p>}
function MiniCircumcircle(){return <svg className="h-[150px] w-full" viewBox="0 0 170 150"><polygon points="85,15 30,115 140,115" fill="none" stroke="#64748b"/><line x1="85" y1="5" x2="85" y2="145" stroke="#64748b" strokeDasharray="4 3"/><line x1="18" y1="80" x2="150" y2="80" stroke="#64748b" strokeDasharray="4 3"/><circle cx="85" cy="80" r="5" fill="#7c3aed"/><circle cx="85" cy="15" r="5" fill="#1683e6"/><circle cx="30" cy="115" r="5" fill="#1683e6"/><circle cx="140" cy="115" r="5" fill="#1683e6"/><text x="92" y="78" fontSize="10">O</text><text x="70" y="13" fontSize="10">A</text><text x="14" y="120" fontSize="10">B</text><text x="146" y="120" fontSize="10">C</text></svg>}
function Input({label,prefix,value,onChange}:{label:string;prefix:string;value:string;onChange:(value:string)=>void}){return <label className="flex items-center gap-1 font-serif">{prefix}<input aria-label={label} className="w-11 rounded border px-1 py-2 text-center" value={value} onChange={event=>onChange(event.target.value)}/></label>}
function BareInput({label,value,onChange}:{label:string;value:string;onChange:(value:string)=>void}){return <input aria-label={label} className="w-12 rounded border px-1 py-2 text-center" value={value} onChange={event=>onChange(event.target.value)}/>} 
function setAnswer(index:number,value:string,answers:string[],setAnswers:(values:string[])=>void,setFeedback:(value:'idle')=>void){setAnswers(answers.map((item,i)=>i===index?value:item));setFeedback('idle')}
function circumcircle(points:Point[]){if(points.length!==3)return{valid:false as const,centre:{x:0,y:0},radius:0};const[a,b,c]=points,d=2*(a.x*(b.y-c.y)+b.x*(c.y-a.y)+c.x*(a.y-b.y));if(Math.abs(d)<1e-5)return{valid:false as const,centre:{x:0,y:0},radius:0};const a2=a.x*a.x+a.y*a.y,b2=b.x*b.x+b.y*b.y,c2=c.x*c.x+c.y*c.y,centre={x:(a2*(b.y-c.y)+b2*(c.y-a.y)+c2*(a.y-b.y))/d,y:(a2*(c.x-b.x)+b2*(a.x-c.x)+c2*(b.x-a.x))/d};return{valid:true as const,centre,radius:distance(centre,a)}}
function distance(a:Point,b:Point){return Math.hypot(b.x-a.x,b.y-a.y)}
function format(value:number){return Number(value.toFixed(2)).toString()}
function clamp(value:number,min:number,max:number){return Math.min(max,Math.max(min,value))}
