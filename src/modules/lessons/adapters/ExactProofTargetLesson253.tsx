import { ArrowLeft, ArrowRight, Check, Eye, Hand, Info, Lightbulb, Maximize2, MousePointer2, RotateCcw, Share2, Target, TriangleAlert } from "lucide-react";
import { useEffect, useMemo, useRef, useState, type PointerEvent as ReactPointerEvent } from "react";
import type { LessonAdapterProps } from "../types";
import "./ExactProofTargetLesson253.css";

type Point={x:number;y:number};type Drag="a"|"b"|"t";const initial={a:{x:0,y:0},b:{x:2,y:1},t:{x:2,y:1}};
const correctReasons=["Given","Distance preserved under translation","Construction","Midpoint preserved under translation","Definition of midpoint","Substitution"];
const statements=["A′B′ is the image of AB under translation T.","AB = A′B′","M is the midpoint of AB.","M′ is the midpoint of A′B′.","M-A = M-B and M′-A′ = M′-B′.","Therefore T(M) = M′."];

export default function ExactProofTargetLesson253({resetToken,onInteraction}:LessonAdapterProps){const [a,setA]=useState<Point>(initial.a),[b,setB]=useState<Point>(initial.b),[t,setT]=useState<Point>(initial.t),[reasons,setReasons]=useState(correctReasons),[proofFeedback,setProofFeedback]=useState("All steps are valid. Well done!"),[tab,setTab]=useState("Explore"),[grid,setGrid]=useState(true),[labels,setLabels]=useState(true),[practiceAnswer,setPracticeAnswer]=useState("A"),[practiceReasons,setPracticeReasons]=useState(["Given","Definition of translation","Substitution","Simplify"]),[practiceFeedback,setPracticeFeedback]=useState("Correct. A′ = (2, 1), and every proof step is justified.");const model=useMemo(()=>proofModel(a,b,t),[a,b,t]);const setPoint=(setter:(p:Point)=>void,p:Point)=>{setter({x:clamp(p.x,-6,6),y:clamp(p.y,-4,4)});onInteraction()};const reset=(notify=true)=>{setA(initial.a);setB(initial.b);setT(initial.t);setReasons(correctReasons);setProofFeedback("All steps are valid. Well done!");setTab("Explore");setGrid(true);setLabels(true);setPracticeAnswer("A");setPracticeReasons(["Given","Definition of translation","Substitution","Simplify"]);setPracticeFeedback("Correct. A′ = (2, 1), and every proof step is justified.");if(notify)onInteraction()};useEffect(()=>{setA(initial.a);setB(initial.b);setT(initial.t);setReasons(correctReasons);setProofFeedback("All steps are valid. Well done!");setTab("Explore");setGrid(true);setLabels(true);setPracticeAnswer("A");setPracticeReasons(["Given","Definition of translation","Substitution","Simplify"]);setPracticeFeedback("Correct. A′ = (2, 1), and every proof step is justified.")},[resetToken]);const proofValid=reasons.every((reason,index)=>reason===correctReasons[index]);const checkProof=()=>{setProofFeedback(proofValid?"All steps are valid. Well done!":"Proof incomplete. At least one reason does not justify its statement.");onInteraction()};const checkPractice=()=>{const valid=practiceAnswer==="A"&&practiceReasons.every((reason,index)=>reason===["Given","Definition of translation","Substitution","Simplify"][index]);setPracticeFeedback(valid?"Correct. A′ = (2, 1), and every proof step is justified.":"Not yet. Use A′ = A + v and justify each algebraic step in order.");onInteraction()};return <section className="target-proof-page" data-testid="dynamic-geometry-mockup-0310" data-dedicated-lesson="253" data-object-model="exact-translation-isometry-proof-chain" data-original-length={model.length.toFixed(4)} data-image-length={model.imageLength.toFixed(4)} data-midpoint-x={model.m.x.toFixed(3)} data-image-midpoint-x={model.mp.x.toFixed(3)} data-proof-valid={proofValid} data-grid={grid} data-labels={labels}>
  <header className="target-proof-header"><span>Geometry</span><span>Transformations and Loci</span><h1>Exact Proof</h1><p>Verify geometric relationships.</p><section><b>♙ Intermediate-Advanced</b><b>ϟ Investigation Lab</b><b>▣ Transformation / Locus Tools</b><b>◷ 6-10 min</b></section><footer><button type="button">⚑ English (English)⌄</button><button type="button" onClick={()=>reset()}><RotateCcw/>Reset</button><button type="button" onClick={()=>{void navigator.clipboard?.writeText(`Exact translation proof: ${proofFeedback}`);onInteraction()}}><Share2/>Share</button><button type="button">↗ Workspace</button><button aria-label="Fullscreen workspace"><Maximize2/></button></footer></header>
  <nav className="target-proof-tabs">{[[Eye,"Explore"],[Info,"Explain"],[Target,"Examples"],[Lightbulb,"Formulas"],[Check,"Practice"]].map(([Icon,name])=><button type="button" key={String(name)} className={tab===name?"is-active":""} onClick={()=>{setTab(String(name));onInteraction()}}><Icon/>{String(name)}</button>)}</nav>
  <section className="target-proof-work"><header><div><b>INTERACTION + VISUALIZATION</b><h2>Work directly on the model</h2></div><aside><span><Check/>Ready</span><b>Exact arithmetic</b><button aria-label="Fullscreen proof lab"><Maximize2/></button></aside></header><div className="target-proof-model"><article className="target-proof-canvas"><h3>Exact Proof - reusable 2D geometry engine</h3><nav><button aria-label="Select and drag"><MousePointer2/></button><button aria-label="Toggle labels" className={labels?"is-active":""} onClick={()=>{setLabels(v=>!v);onInteraction()}}>A</button><button aria-label="Toggle grid" className={grid?"is-active":""} onClick={()=>{setGrid(v=>!v);onInteraction()}}>•</button><button aria-label="Reset construction" onClick={()=>reset()}><RotateCcw/></button></nav><ProofGraph model={model} grid={grid} labels={labels} onPoint={(key,p)=>key==="a"?setPoint(setA,p):key==="b"?setPoint(setB,p):setPoint(setT,{x:p.x-a.x,y:p.y-a.y})}/><p><Info/>Drag A, B, or the translation handle to vary the premises.</p><section><h4>Live measurements</h4><div><p>Distance AB<b>{model.length.toFixed(3)}</b></p><p>Distance A′B′<b>{model.imageLength.toFixed(3)}</b></p><p>Midpoint M<b>{point(model.m)}</b></p><p>Midpoint M′<b>{point(model.mp)}</b></p></div></section></article><aside className="target-proof-controls"><h2>Transforming construction</h2><h3>2D geometry params</h3><nav><button className="is-active">Point</button><button>Reason</button><button>Proof</button></nav><Control label="A x" min={-6} max={6} value={a.x} onChange={v=>setPoint(setA,{...a,x:v})}/><Control label="A y" min={-4} max={4} value={a.y} onChange={v=>setPoint(setA,{...a,y:v})}/><Control label="B x" min={-6} max={6} value={b.x} onChange={v=>setPoint(setB,{...b,x:v})}/><Control label="B y" min={-4} max={4} value={b.y} onChange={v=>setPoint(setB,{...b,y:v})}/><Control label="Translation x" min={-4} max={4} value={t.x} onChange={v=>setPoint(setT,{...t,x:v})}/><Control label="Translation y" min={-4} max={4} value={t.y} onChange={v=>setPoint(setT,{...t,y:v})}/><section><p>Distance equality<b>{model.lengthDifference.toFixed(4)}</b></p><p>Midpoint map<b>{distance(model.mp,{x:model.m.x+t.x,y:model.m.y+t.y}).toFixed(4)}</b></p><p>Vector<b>{point(t)}</b></p><p>Proof state<b>{proofValid?"valid":"invalid"}</b></p></section></aside></div></section>
  <section className="target-proof-stages">{[[Eye,"Observe","Notice the original segment AB and its translated image A′B′."],[Hand,"Manipulate","Drag points or change sliders to apply a translation."],[Lightbulb,"Notice","Measurements update instantly. Exact facts stay invariant."],[Target,"Understand","Use exact facts to build a correct proof step by step."]].map(([Icon,title,text],index)=><article key={String(title)}><Icon/><span><b>{String(title)}</b><p>{String(text)}</p></span>{index<3&&<ArrowRight/>}</article>)}</section>
  <section className="target-proof-builder"><article><h2>Build your exact proof</h2><p>Fill each step with a true statement and its reason.</p><div className="target-proof-table"><b>Step</b><b>Statement</b><b>Reason</b>{statements.map((statement,index)=><div className="target-proof-row" key={statement}><span>{index+1}</span><input aria-label={`Proof statement ${index+1}`} value={statement} readOnly/><select aria-label={`Proof reason ${index+1}`} value={reasons[index]} onChange={e=>{const next=[...reasons];next[index]=e.target.value;setReasons(next);onInteraction()}}>{[correctReasons[index],"Given","Measurement from diagram","Assumption","Definition of midpoint","Substitution"].filter((value,i,array)=>array.indexOf(value)===i).map(value=><option key={value}>{value}</option>)}</select></div>)}</div><footer className={proofFeedback.startsWith("All")?"correct":"incorrect"}><span role="status">{proofFeedback}</span><button type="button" onClick={checkProof}>Check proof</button></footer></article><aside><section><h2>Why this works</h2><p>Translations are rigid motions. They preserve distance, betweenness, and midpoint relationships.</p><b>Rule used</b><p>Distance and midpoint are invariants under translation.</p><strong>T<sub>v</sub>(P) = P + v</strong></section><section><h2>Worked example</h2><p>Translation by v = (2,1)</p><p>Given: A(0,0), B(2,1)</p><p>Image: A′(2,1), B′(4,2)</p><ProofMini/><p><Check/>AB = A′B′ = √5</p><p><Check/>M′ = M + v</p></section><section><h2><TriangleAlert/>Misconception alert</h2><b>Exact Proof ≠ Measurement Proof</b><p>Use exact facts and reasons that are always true, not only rounded measurements.</p></section></aside></section>
  <section className="target-proof-practice"><h2>Quick practice <span>Challenge</span></h2><p>A point A(-1,2) is translated by v=(3,-1) to A′. Select the correct image and fill the proof.</p><div><article><h3>1) Choose A′</h3><PracticeMini/>{[["A","A′(2,1)"],["B","A′(-4,3)"],["C","A′(-2,2)"],["D","A′(4,1)"]].map(([key,text])=><label key={key}><input aria-label={`Exact proof practice ${key}`} type="radio" name="proof-practice" checked={practiceAnswer===key} onChange={()=>setPracticeAnswer(key)}/>{text}</label>)}</article><article><h3>2) Build proof</h3>{["A′ is the image of A under translation by v=(3,-1).","A′ = A + (3,-1).","A′ = (-1+3, 2-1).","A′ = (2,1)."].map((statement,index)=><div key={statement}><span>{index+1}</span><input value={statement} readOnly/><select aria-label={`Practice proof reason ${index+1}`} value={practiceReasons[index]} onChange={e=>{const next=[...practiceReasons];next[index]=e.target.value;setPracticeReasons(next)}}>{[["Given","Assumption"],["Definition of translation","Measurement"],["Substitution","Construction"],["Simplify","Given"]][index].map(value=><option key={value}>{value}</option>)}</select></div>)}</article></div><footer><p role="status" className={practiceFeedback.startsWith("Correct")?"correct":"incorrect"}>{practiceFeedback}</p><button type="button" onClick={checkPractice}>Check answers</button></footer></section>
  <nav className="target-proof-nav"><a href="/lessons/geometry/252-conjecture-testing"><ArrowLeft/><span><b>Previous</b>Conjecture Testing</span></a><a href="/lessons/geometry/254-collinearity-test"><span><b>Next</b>Collinearity Test</span><ArrowRight/></a></nav>
  </section>}

function ProofGraph({model,grid,labels,onPoint}:{model:ReturnType<typeof proofModel>;grid:boolean;labels:boolean;onPoint:(key:Drag,p:Point)=>void}){const ref=useRef<SVGSVGElement>(null),drag=useRef<Drag|null>(null),origin={x:320,y:250},scale=42,s=(p:Point)=>({x:origin.x+p.x*scale,y:origin.y-p.y*scale}),world=(e:ReactPointerEvent<SVGSVGElement>)=>{const matrix=ref.current?.getScreenCTM();if(!matrix)return null;const q=new DOMPoint(e.clientX,e.clientY).matrixTransform(matrix.inverse());return{x:round((q.x-origin.x)/scale),y:round((origin.y-q.y)/scale)}};const pts={a:s(model.a),b:s(model.b),ap:s(model.ap),bp:s(model.bp)};return <svg ref={ref} className="target-proof-graph" viewBox="0 0 640 500" role="img" aria-label="Exact translation proof construction with draggable premises" onPointerMove={e=>{if(!drag.current)return;const p=world(e);if(p)onPoint(drag.current,p)}} onPointerUp={()=>drag.current=null}>{grid&&<Grid/>}<line x1="0" x2="640" y1={origin.y} y2={origin.y} stroke="#334155"/><line x1={origin.x} x2={origin.x} y1="0" y2="500" stroke="#334155"/><line x1={pts.a.x} y1={pts.a.y} x2={pts.b.x} y2={pts.b.y} stroke="#1685f8" strokeWidth="3"/><line x1={pts.ap.x} y1={pts.ap.y} x2={pts.bp.x} y2={pts.bp.y} stroke="#8b5cf6" strokeWidth="3"/><line x1={pts.a.x} y1={pts.a.y} x2={pts.ap.x} y2={pts.ap.y} stroke="#f59e0b" strokeDasharray="6 4"/>{(["a","b"] as const).map(key=><circle key={key} data-testid={`exact-proof-point-${key}`} cx={pts[key].x} cy={pts[key].y} r="8" fill="#1685f8" onPointerDown={e=>{drag.current=key;e.currentTarget.setPointerCapture(e.pointerId)}}/>)}<circle data-testid="exact-proof-vector-handle" cx={pts.ap.x} cy={pts.ap.y} r="12" fill="transparent" stroke="#f59e0b" strokeWidth="3" onPointerDown={e=>{drag.current="t";e.currentTarget.setPointerCapture(e.pointerId)}}/><circle cx={pts.ap.x} cy={pts.ap.y} r="7" fill="#8b5cf6"/><circle cx={pts.bp.x} cy={pts.bp.y} r="7" fill="#8b5cf6"/>{labels&&[[pts.a,"A"],[pts.b,"B"],[pts.ap,"A′"],[pts.bp,"B′"]].map(([p,label])=><text key={String(label)} x={(p as {x:number}).x+9} y={(p as {y:number}).y-9} fontWeight="900">{String(label)}</text>)}</svg>}
function Grid(){return <g stroke="#e5edf6">{Array.from({length:27},(_,i)=><line key={`v${i}`} x1={i*25} x2={i*25} y1="0" y2="500"/>)}{Array.from({length:21},(_,i)=><line key={`h${i}`} x1="0" x2="640" y1={i*25} y2={i*25}/>)}</g>}function Control({label,min,max,value,onChange}:{label:string;min:number;max:number;value:number;onChange:(v:number)=>void}){return <label className="target-proof-control"><span>{label}</span><small>{min}</small><input aria-label={label} type="range" min={min} max={max} step=".25" value={value} onChange={e=>onChange(Number(e.target.value))}/><small>{max}</small><input aria-label={`${label} exact value`} type="number" min={min} max={max} step=".25" value={value} onChange={e=>onChange(Number(e.target.value))}/></label>}
function ProofMini(){return <svg viewBox="0 0 180 80"><line x1="20" y1="62" x2="72" y2="43" stroke="#1685f8"/><line x1="94" y1="42" x2="146" y2="23" stroke="#8b5cf6"/><line x1="20" y1="62" x2="94" y2="42" stroke="#f59e0b" strokeDasharray="4 3"/>{[[20,62],[72,43],[94,42],[146,23]].map(([x,y],i)=><circle key={i} cx={x} cy={y} r="4" fill={i<2?"#1685f8":"#8b5cf6"}/>)}</svg>}function PracticeMini(){return <svg viewBox="0 0 120 100"><g stroke="#e2e8f0">{[20,40,60,80,100].map(x=><line key={x} x1={x} x2={x} y1="5" y2="95"/>)}{[20,40,60,80].map(y=><line key={y} x1="5" x2="115" y1={y} y2={y}/>)}</g><line x1="60" x2="60" y1="5" y2="95" stroke="#334155"/><line x1="5" x2="115" y1="60" y2="60" stroke="#334155"/><circle cx="40" cy="20" r="5" fill="#8b5cf6"/><text x="27" y="15" fontSize="8">A</text></svg>}
function proofModel(a:Point,b:Point,t:Point){const ap={x:a.x+t.x,y:a.y+t.y},bp={x:b.x+t.x,y:b.y+t.y},m={x:(a.x+b.x)/2,y:(a.y+b.y)/2},mp={x:(ap.x+bp.x)/2,y:(ap.y+bp.y)/2},length=distance(a,b),imageLength=distance(ap,bp);return{a,b,t,ap,bp,m,mp,length,imageLength,lengthDifference:Math.abs(length-imageLength)}}function distance(a:Point,b:Point){return Math.hypot(b.x-a.x,b.y-a.y)}function point(p:Point){return`(${fmt(p.x)}, ${fmt(p.y)})`}function fmt(n:number){return Number(n.toFixed(2)).toString()}function round(n:number){return Math.round(n*20)/20}function clamp(n:number,min:number,max:number){return Math.min(max,Math.max(min,Number.isFinite(n)?n:min))}
