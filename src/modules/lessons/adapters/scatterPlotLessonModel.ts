export const scatterDefault = [{ x: 1, y: 2.4 }, { x: 2.4, y: 3.7 }, { x: 4, y: 5 }, { x: 5.4, y: 5.9 }, { x: 6.5, y: 6.6 }, { x: 7.5, y: 7.4 }, { x: 9, y: 8.6 }];
export function regression(points = scatterDefault) { const n = points.length, x = points.reduce((s, p) => s + p.x, 0) / n, y = points.reduce((s, p) => s + p.y, 0) / n; const numerator = points.reduce((s, p) => s + (p.x - x) * (p.y - y), 0), dx = points.reduce((s, p) => s + (p.x - x) ** 2, 0), dy = points.reduce((s, p) => s + (p.y - y) ** 2, 0); return { slope: numerator / dx, intercept: y - numerator / dx * x, r: numerator / Math.sqrt(dx * dy) }; }
