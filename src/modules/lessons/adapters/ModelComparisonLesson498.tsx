import { useState } from "react";
import { RotateCcw } from "lucide-react";
import type { LessonAdapterProps } from "../types";
import { comparisonDefault, compareModels } from "./modelComparisonLessonModel";
import "./ModelComparisonLesson498.css";
import { LessonTopicStudyBoard } from "../components/LessonTopicStudyBoard";
export default function ModelComparisonLesson498({ resetToken, onInteraction }: LessonAdapterProps) { return <CompareActivity key={resetToken} onInteraction={onInteraction} />; }
function CompareActivity({ onInteraction }: Pick<LessonAdapterProps, "onInteraction">) { const [points, setPoints] = useState(comparisonDefault), [choice, setChoice] = useState("exponential"); const models = compareModels(points), update = (index: number, raw: string) => { const value = Number(raw); if (!Number.isFinite(value)) return; setPoints(points.map((p, i) => i === index ? { ...p, y: value } : p)); onInteraction(); }; const selected = models[choice as keyof typeof models]; return <div className="mc498" data-testid="statistics-mockup-0461" data-target-family="statistics-and-regression"><header><div><span>DATA AND PROBABILITY · STATISTICS AND REGRESSION</span><h2>Model Comparison</h2><p>Compare linear, polynomial, and exponential models for one dataset.</p></div><span>Lesson 498 · Share</span></header><nav><b>Interact</b><span>Learn</span><span>Example</span><span>Formula</span><span>Practice</span></nav><section className="mc498-work"><aside><h3>Dataset (editable)</h3><table><thead><tr><th>x</th><th>y</th></tr></thead><tbody>{points.map((point, index) => <tr key={index}><td>{point.x.toFixed(1)}</td><td><input aria-label={`Comparison y ${index + 1}`} value={point.y} onChange={event => update(index, event.target.value)} /></td></tr>)}</tbody></table><button type="button" onClick={() => { setPoints(comparisonDefault); onInteraction(); }}><RotateCcw size={14} /> Reset</button></aside>{(["linear", "quadratic", "exponential"] as const).map(kind => <article key={kind} className="mc498-model"><h3>{kind} model</h3><div className="mc498-mini"><svg viewBox="0 0 250 180" aria-label={`${kind} model chart`}>{points.map(p => <circle key={p.x} cx={20 + p.x * 20} cy={160 - p.y * 2} r="4" fill="#176fc5" />)}</svg></div><p>R² <b>{models[kind].r2.toFixed(3)}</b></p><p>RMSE <b>{models[kind].rmse.toFixed(2)}</b></p><p>AICc <b>{(models[kind].sse + models[kind].params * 2).toFixed(2)}</b></p><label><input type="radio" name="model" checked={choice === kind} onChange={() => setChoice(kind)} /> Select</label></article>)}</section><section className="mc498-selection"><h3>Evidence-based model selection</h3><p>Best model: <b>{choice}</b></p><p>Selected R² = {selected.r2.toFixed(3)} · RMSE = {selected.rmse.toFixed(2)}</p><p>Choose the simplest model with strong fit, using error and complexity together.</p></section><section className="mc498-lower"><article><h3>Model fit</h3><p>R² measures explained variation. RMSE measures typical error. Lower error is better.</p></article><article><h3>Worked example</h3><p>Compare each model's R², RMSE, and complexity penalty before choosing.</p></article><article><h3>Misconception guard</h3><p>Higher R² is not always best. Check AICc, RMSE, and extrapolation risk.</p></article></section><section className="mc498-practice"><h3>Practice · Model Comparison</h3><p>Select the best model for each dataset using evidence and explain your choice.</p><button type="button" onClick={() => onInteraction()}>Check answer</button></section><footer><button type="button" onClick={() => { setPoints(comparisonDefault); setChoice("exponential"); onInteraction(); }}><RotateCcw size={14} /> Reset lesson</button><span>Previous: Residual Plot &nbsp; Next: Interpolation and Extrapolation →</span></footer>
      <LessonTopicStudyBoard lessonId={498} alwaysVisible onInteraction={onInteraction} />
</div>; }
