import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it,vi}from"vitest";import{lessonCatalog}from"../catalog/lessonCatalog";import ProbabilityLessonAdapter from"./ProbabilityLessonAdapter";
describe("Probability Plot dedicated surface",()=>{it("routes lesson 517 to its editable Q-Q analysis lab",()=>{const lesson=lessonCatalog.find(item=>item.id===517)!;const html=renderToStaticMarkup(<ProbabilityLessonAdapter lesson={lesson} resetToken={0} onInteraction={vi.fn()}/>);expect(html).toContain('data-testid="probability-mockup-0480"');expect(html).toContain("Q–Q Plot against Normal(0, 1)");expect(html).toContain("Fit Summary");expect(html).toContain("0.991");});});
