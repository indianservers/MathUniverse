import{Play,Plus,RotateCcw,Scale,Trash2}from"lucide-react";import{useMemo,useState}from"react";import type{LessonAdapterProps}from"../types";import{dicePayoffPreset,expectedValueSummary,simulateExpectedValue}from"./expectedValueLessonModel";import"./ExpectedValueLesson513.css";
export default function ExpectedValueLesson513({resetToken,onInteraction}:LessonAdapterProps){return <ExpectedValueActivity key={resetToken} onInteraction={onInteraction}/>}
function ExpectedValueActivity({onInteraction}:Pick<LessonAdapterProps,"onInteraction">){const[rows,setRows]=useState(dicePayoffPreset),[trials,setTrials]=useState(10000),[seed,setSeed]=useState(513),[quick,setQuick]=useState("35"),[quickChecked,setQuickChecked]=useState(true),[practiceOpen,setPracticeOpen]=useState(false);const summary=useMemo(()=>expectedValueSummary(rows),[rows]),simulation=useMemo(()=>simulateExpectedValue(rows,trials,seed),[rows,seed,trials]);const edit=(id:number,key:"payoff"|"probability",value:number)=>{setRows(current=>current.map(row=>row.id===id?{...row,[key]:value}:row));setQuickChecked(false);onInteraction();};const reset=()=>{setRows(dicePayoffPreset);setTrials(10000);setSeed(513);setQuick("35");setQuickChecked(true);setPracticeOpen(false);onInteraction();};const add=()=>{const id=Math.max(0,...rows.map(row=>row.id))+1;setRows(current=>[...current,{id,outcome:Math.max(0,...current.map(row=>row.outcome))+1,payoff:0,probability:0}]);onInteraction();};const maxPayoff=Math.max(1,...rows.map(row=>Math.abs(row.payoff)));const chartPoints=simulation.points.map((point,index)=>`${(index/Math.max(1,simulation.points.length-1))*100},${90-Math.max(0,Math.min(80,(point.average/maxPayoff)*80))}`).join(" ");return <div className="ev513" data-testid="probability-mockup-0476" data-target-family="probability-and-distributions"><header className="ev513-hero"><span>DATA AND PROBABILITY · PROBABILITY AND DISTRIBUTIONS</span><h2>Expected Value</h2><p>The long-run average payoff of a random experiment.</p><button type="button" onClick={reset}><RotateCcw size={14}/>Reset</button></header><nav aria-label="Lesson sections"><b>Interact</b><span>Learn</span><span>Example</span><span>Formula</span><span>Practice</span></nav><section className="ev513-objective"><b>Objective:</b> Understand expected value as the probability-weighted average of outcomes and estimate it using long-run simulations.</section>
<section className="ev513-main"><article><h3><i>1</i>Experiment Setup</h3><p>Define outcomes, their prizes (payoffs), and probabilities.</p><table><thead><tr><th>Outcome (x)</th><th>Prize (₹)</th><th>P(x)</th><th>Complement</th><th>xp</th><th/></tr></thead><tbody>{rows.map((row,index)=><tr key={row.id}><td>{row.outcome}</td><td><input aria-label={`Payoff ${row.outcome}`} type="number" value={row.payoff} onChange={event=>edit(row.id,"payoff",Number(event.target.value))}/></td><td><input aria-label={`Probability ${row.outcome}`} type="number" min="0" max="1" step="0.01" value={row.probability} onChange={event=>edit(row.id,"probability",Math.max(0,Math.min(1,Number(event.target.value))))}/></td><td>{(1-row.probability).toFixed(2)}</td><td>{summary.contributions[index].toFixed(2)}</td><td><button type="button" aria-label={`Delete outcome ${row.outcome}`} onClick={()=>{setRows(current=>current.filter(item=>item.id!==row.id));onInteraction();}}><Trash2 size={13}/></button></td></tr>)}<tr><th>Total</th><td/><th className={Math.abs(summary.probabilityTotal-1)<.0001?"valid":"invalid"}>{summary.probabilityTotal.toFixed(2)}</th><td/><th>{summary.expectedValue.toFixed(2)}</th><td/></tr></tbody></table><div className="ev513-tablebuttons"><button type="button" onClick={add}><Plus size={13}/>Add outcome</button><button type="button" onClick={()=>{setRows(dicePayoffPreset);onInteraction();}}>Use dice preset</button></div><strong>Sample Space (S): {`{${rows.map(row=>row.outcome).join(", ")}}`} <span>n(S) = {rows.length}</span></strong></article><article><h3><i>2</i>Probability-Weighted Balance</h3><p>Each outcome’s contribution to the expected value.</p><div className="ev513-balance"><span>Less payoff</span><b>E(X)<i style={{left:`${Math.max(0,Math.min(100,summary.expectedValue/Math.max(1,maxPayoff)*100))}%`}}/></b><span>More payoff</span></div><div className="ev513-contributions">{summary.contributions.map((value,index)=><span key={rows[index].id} style={{flex:Math.max(.2,Math.abs(value))}}>{value.toFixed(2)}</span>)}</div><article><h3>Expected Value (E(X)) <b>₹{summary.expectedValue.toFixed(2)}</b></h3><p>E(X) = Σ x p<br/>= {rows.map(row=>`(${row.payoff})(${row.probability.toFixed(2)})`).join(" + ")}<br/>= {summary.expectedValue.toFixed(2)}</p></article><aside><Scale size={22}/><div><h3>Fairness Indicator</h3><p>Since E(X) {summary.expectedValue>0?">":summary.expectedValue<0?"<":"="} 0, the game is <b>{summary.fairness}</b>.</p></div></aside></article></section>
<section className="ev513-sim"><header><div><h3><i>3</i>Long-Run Simulation</h3><p>Run many trials to see the average payoff approach E(X).</p></div><label>Trials<input type="number" min="10" max="100000" value={trials} onChange={event=>setTrials(Math.max(10,Math.min(100000,Number(event.target.value))))}/></label><button type="button" onClick={()=>{setSeed(value=>value+1);onInteraction();}}>Run <Play size={13}/></button></header><div><article><p>Average payoff (₹)</p><b>{simulation.average.toFixed(2)}</b><p>Target E(X) (₹)</p><strong>{summary.expectedValue.toFixed(2)}</strong><p>Difference</p><b>{(simulation.average-summary.expectedValue).toFixed(2)}</b></article><article><h3>Average Payoff vs. Number of Trials</h3><svg viewBox="0 0 100 100" preserveAspectRatio="none"><line x1="0" y1={90-summary.expectedValue/maxPayoff*80} x2="100" y2={90-summary.expectedValue/maxPayoff*80}/><polyline points={chartPoints}/></svg><p>Running average &nbsp; --- E(X) = {summary.expectedValue.toFixed(2)}</p></article><article><h3>Trial outcomes (last 10)</h3><div>{simulation.recent.map((row,index)=><b key={`${row.id}-${index}`}>{row.outcome}</b>)}</div><p>Payoffs (₹): {simulation.recent.map(row=>row.payoff).join("  ")}</p><strong>Running average (₹) {simulation.average.toFixed(2)}</strong></article></div></section>
<section className="ev513-bottom"><article><h3><i>4</i>Key Takeaways</h3><p>✓ Expected value is the long-run average payoff.</p><p>✓ E(X) = Σxp, where Σp = 1.</p><p>✓ Positive is favorable; zero is fair.</p></article><article className="warning"><h3>Common Misconception</h3><p><b>Mistake:</b> Adding or averaging payoffs without using probabilities.</p><p>Correct: weight each payoff by its probability.</p></article><article><h3>Quick Check</h3>{["8","35","45","negative"].map(value=><label key={value}><input type="radio" checked={quick===value} onChange={()=>{setQuick(value);setQuickChecked(false);onInteraction();}}/>E(X) = {value}</label>)}<button type="button" onClick={()=>setQuickChecked(true)}>Check</button>{quickChecked&&<p className={quick===summary.expectedValue.toFixed(0)?"correct":"incorrect"}>{quick===summary.expectedValue.toFixed(0)?"Correct":"Try again"}: E(X) = {summary.expectedValue.toFixed(2)}</p>}</article><article><h3>Practice</h3><p>A coin is tossed twice. You win ₹30 for 2 heads, ₹10 for 1 head, and ₹0 for 0 heads.</p><button type="button" onClick={()=>setPracticeOpen(value=>!value)}>{practiceOpen?"Hide solution":"Start Practice"}</button>{practiceOpen&&<p>E(X)=30(1/4)+10(1/2)+0(1/4)=₹12.50</p>}</article></section><footer><button type="button" onClick={reset}><RotateCcw size={14}/>Reset lesson</button><span>Previous: Bayes’ Theorem &nbsp; Next: Simulation →</span></footer></div>}
