import { ArrowLeft, ArrowRight, Check, Eye, Grid3X3, Hand, Lightbulb, Maximize2, Move, RotateCcw, Share2, Target, TriangleAlert, ZoomIn } from "lucide-react";
import { useEffect, useMemo, useRef, useState, type PointerEvent as ReactPointerEvent, type ReactNode } from "react";
import type { LessonAdapterProps } from "../types";
import "./CollinearityTestTargetLesson254.css";

type Point={x:number;y:number};type Key="a"|"b"|"c";const initial={a:{x:-3,y:-1},b:{x:1,y:.5},c:{x:5,y:2}};

export default function CollinearityTestTargetLesson254({resetToken,onInteraction}:LessonAdapterProps){const [points,setPoints]=useState(initial),[grid,setGrid]=useState(true),[snap,setSnap]=useState(false),[zoom,setZoom]=useState(1),[tab,setTab]=useState("Interaction + visualization"),[answer,setAnswer]=useState("A"),[feedback,setFeedback]=useState("Correct. All exact tests are zero, so the points are collinear.");const model=useMemo(()=>collinearityModel(points.a,points.b,points.c),[points]);const update=(key:Key,p:Point)=>{const clean={x:clamp(snap?Math.round(p.x):round(p.x),-6,6),y:clamp(snap?Math.round(p.y):round(p.y),-4,4)};setPoints(current=>({...current,[key]:clean}));onInteraction()};const reset=(notify=true)=>{setPoints(initial);setGrid(true);setSnap(false);setZoom(1);setTab("Interaction + visualization");setAnswer("A");setFeedback("Correct. All exact tests are zero, so the points are collinear.");if(notify)onInteraction()};useEffect(()=>{setPoints(initial);setGrid(true);setSnap(false);setZoom(1);setTab("Interaction + visualization");setAnswer("A");setFeedback("Correct. All exact tests are zero, so the points are collinear.")},[resetToken]);const check=()=>{const correct=answer==="A";setFeedback(correct?"Correct. All exact tests are zero, so the points are collinear.":"Not yet. Compute the determinant or compare both segment slopes.");onInteraction()};return <section className="target-collinear-page" data-testid="dynamic-geometry-mockup-0311" data-dedicated-lesson="254" data-object-model="three-point-synchronized-exact-collinearity-tests" data-determinant={model.determinant.toFixed(4)} data-cross-product={model.cross.toFixed(4)} data-area={model.area.toFixed(4)} data-slope-ab={finite(model.slopeAB)} data-slope-bc={finite(model.slopeBC)} data-collinear={model.collinear} data-vertical={model.vertical} data-grid={grid} data-snap={snap}>
  <header className="target-collinear-header"><span>Geometry</span><span>Transformations and Loci</span><h1>Collinearity Test</h1><p>Check shared-line conditions.</p><section><b>♙ Intermediate-Advanced</b><b>ϟ Investigation Lab</b><b>▣ Transformation / Locus Tools</b><b>◷ 6-10 min</b></section><footer><button type="button">⚑ English (English)⌄</button><button type="button" onClick={()=>reset()}><RotateCcw/>Reset</button><button type="button" onClick={()=>{void navigator.clipboard?.writeText(`A${point(points.a)} B${point(points.b)} C${point(points.c)}: ${model.collinear?"collinear":"not collinear"}`);onInteraction()}}><Share2/>Share</button><button type="button">↗ Workspace</button></footer></header>
  <section className="target-collinear-steps">{[["1",Eye,"Observe","Three points A, B, C lie on a straight line."],["2",Hand,"Manipulate","Drag any point. Watch the tests update together."],["3",Lightbulb,"Notice","All three tests are zero when the points are collinear."],["4",Target,"Understand","Collinearity ⇔ determinant = 0 ⇔ area = 0 ⇔ directions match."]].map(([number,Icon,title,text],index)=><article key={String(title)}><i>{String(number)}</i><Icon/><span><b>{String(title)}</b><p>{String(text)}</p></span>{index<3&&<ArrowRight/>}</article>)}</section>
  <section className="target-collinear-work"><header><nav>{[[Eye,"Interaction + visualization"],[Lightbulb,"Explain"],[Target,"Examples"],[Target,"Formulas"],[Lightbulb,"Know more"]].map(([Icon,name])=><button type="button" key={String(name)} className={tab===name?"is-active":""} onClick={()=>{setTab(String(name));onInteraction()}}><Icon/>{String(name)}</button>)}</nav><aside><span className={model.collinear?"good":"bad"}>{model.collinear?"All tests in sync":"Counterexample detected"}</span><b>{model.collinear?"0":"1"} conflicts</b><button aria-label="Fullscreen collinearity lab"><Maximize2/></button></aside></header><h2>Work directly on the model</h2><div><article className="target-collinear-canvas"><h3>Drag the three points</h3><p>Move any point. All tests update instantly.</p><nav><button aria-label="Toggle snap" className={snap?"is-active":""} onClick={()=>{setSnap(v=>!v);onInteraction()}}><Move/></button><button aria-label="Toggle collinearity grid" className={grid?"is-active":""} onClick={()=>{setGrid(v=>!v);onInteraction()}}><Grid3X3/></button><button aria-label="Reset collinearity points" onClick={()=>reset()}><RotateCcw/></button><button aria-label="Zoom collinearity graph" onClick={()=>{setZoom(value=>value===1?1.2:1);onInteraction()}}><ZoomIn/></button></nav><CollinearGraph points={points} model={model} grid={grid} zoom={zoom} onPoint={update}/><section>{(["a","b","c"] as const).map(key=><div key={key}><b>{key.toUpperCase()} (x, y)</b><input aria-label={`${key.toUpperCase()} x coordinate`} type="number" step=".25" value={points[key].x} onChange={e=>update(key,{...points[key],x:Number(e.target.value)})}/><input aria-label={`${key.toUpperCase()} y coordinate`} type="number" step=".25" value={points[key].y} onChange={e=>update(key,{...points[key],y:Number(e.target.value)})}/></div>)}</section><footer><h4>Drag point legend</h4><span><i/>A (x₁,y₁)</span><span><i/>B (x₂,y₂)</span><span><i/>C (x₃,y₃)</span></footer></article><aside className="target-collinear-tests"><h3>Synchronized tests</h3><p>All must agree for collinearity.</p><TestCard title="Slope test (AB vs BC)"><b>m<sub>AB</sub> = (y₂-y₁)/(x₂-x₁) &nbsp; and &nbsp; m<sub>BC</sub> = (y₃-y₂)/(x₃-x₂)</b><p>Values</p><strong>m<sub>AB</sub> = {showSlope(model.slopeAB)} &nbsp;&nbsp; m<sub>BC</sub> = {showSlope(model.slopeBC)}</strong><p>Difference / direction test</p><strong>{model.slopeAgreement?"directions agree":"directions differ"} {model.slopeAgreement&&<Check/>}</strong></TestCard><TestCard title="Zero-area test (determinant)"><b>D = (x₂-x₁)(y₃-y₁) - (y₂-y₁)(x₃-x₁)</b><p>Value</p><strong>D = {fmt(model.determinant)} {model.zeroDet&&<Check/>}</strong><small>Twice the signed area of triangle ABC</small></TestCard><TestCard title="Vector (cross product) test in 2D"><b>AB × AC = (x₂-x₁)(y₃-y₁) - (y₂-y₁)(x₃-x₁)</b><p>Value</p><strong>AB × AC = {fmt(model.cross)} {model.zeroCross&&<Check/>}</strong></TestCard><footer className={model.collinear?"good":"bad"}>{model.collinear?<Check/>:<TriangleAlert/>}<span><b>Result: {model.collinear?"Collinear":"Not collinear"}</b><p>{model.collinear?"All exact tests agree.":"The determinant and triangle area are non-zero."}</p></span></footer></aside></div></section>
  <section className="target-collinear-learning"><article><h2>⌁ Rule / Formula</h2><p>Three points A(x₁,y₁), B(x₂,y₂), C(x₃,y₃) are collinear if any of the following is true:</p><ul><li><b>Slope test:</b> directions AB and BC agree.</li><li><b>Determinant test:</b> D = 0.</li><li><b>Vector test:</b> AB × AC = 0.</li></ul></article><article><h2>▣ Worked example (correct)</h2><p>Are A(-2,1), B(1,3), C(4,5) collinear?</p><b>m<sub>AB</sub> = 2/3, &nbsp; m<sub>BC</sub> = 2/3 <Check/></b><b>D = 3·4 - 2·6 = 0 <Check/></b><b>AB × AC = 0 <Check/></b><footer>All zero ⇒ Collinear.</footer></article><article><h2><TriangleAlert/>Common misconception</h2><b>Equal slopes are not always enough.</b><p>If x₂=x₁, the slope m<sub>AB</sub> is undefined. Check using determinant or vector test.</p><VerticalMini/><strong>Vertical line: slopes undefined but points are collinear.</strong></article></section>
  <section className="target-collinear-practice"><h2>Try it yourself</h2><p>For A(-2,1), B(1,3), and C(4,5), classify the points using an exact test.</p><section>{[["A","Collinear"],["B","Not collinear"],["C","Cannot determine"]].map(([key,text])=><label key={key}><input aria-label={`Collinearity practice ${key}`} type="radio" name="collinear-practice" checked={answer===key} onChange={()=>setAnswer(key)}/>{text}</label>)}</section><button type="button" onClick={check}>Quick check</button><p role="status" className={feedback.startsWith("Correct")?"correct":"incorrect"}>{feedback}</p><aside><h3>Challenge</h3><p>1. Move any point off the line.</p><p>2. Bring the points back on the line.</p><p>3. Try a vertical line.</p></aside></section>
  <nav className="target-collinear-nav"><a href="/lessons/geometry/253-exact-proof"><ArrowLeft/><span><b>Previous</b>Exact Proof</span></a><a href="/lessons/geometry/255-concurrency-test"><span><b>Next</b>Concurrency Test</span><ArrowRight/></a></nav>
  </section>}

function CollinearGraph({points,model,grid,zoom,onPoint}:{points:Record<Key,Point>;model:ReturnType<typeof collinearityModel>;grid:boolean;zoom:number;onPoint:(key:Key,p:Point)=>void}){const ref=useRef<SVGSVGElement>(null),drag=useRef<Key|null>(null),origin={x:320,y:250},scale=42*zoom,s=(p:Point)=>({x:origin.x+p.x*scale,y:origin.y-p.y*scale}),world=(e:ReactPointerEvent<SVGSVGElement>)=>{const matrix=ref.current?.getScreenCTM();if(!matrix)return null;const q=new DOMPoint(e.clientX,e.clientY).matrixTransform(matrix.inverse());return{x:(q.x-origin.x)/scale,y:(origin.y-q.y)/scale}};const p={a:s(points.a),b:s(points.b),c:s(points.c)},line=model.line&&lineBounds(model.line).map(s);return <svg ref={ref} className="target-collinear-graph" viewBox="0 0 640 500" role="img" aria-label="Three draggable points with exact synchronized collinearity tests" onPointerMove={e=>{if(!drag.current)return;const q=world(e);if(q)onPoint(drag.current,q)}} onPointerUp={()=>drag.current=null}>{grid&&<Grid/>}<line x1="0" x2="640" y1={origin.y} y2={origin.y} stroke="#334155"/><line x1={origin.x} x2={origin.x} y1="0" y2="500" stroke="#334155"/>{line&&<line data-testid="collinearity-line" x1={line[0].x} y1={line[0].y} x2={line[1].x} y2={line[1].y} stroke="#1685f8" strokeWidth="2"/>}<polygon points={`${p.a.x},${p.a.y} ${p.b.x},${p.b.y} ${p.c.x},${p.c.y}`} fill={model.collinear?"none":"rgba(239,68,68,.12)"} stroke={model.collinear?"none":"#ef4444"}/>{(["a","b","c"] as const).map((key,index)=><g key={key}><circle data-testid={`collinearity-point-${key}`} data-x={points[key].x.toFixed(3)} data-y={points[key].y.toFixed(3)} cx={p[key].x} cy={p[key].y} r="9" fill={["#ef4444","#1685f8","#22a35a"][index]} onPointerDown={e=>{drag.current=key;e.currentTarget.setPointerCapture(e.pointerId)}}/><text x={p[key].x+9} y={p[key].y-10} fontWeight="900" fill={["#dc2626","#1769c8","#15803d"][index]}>{key.toUpperCase()}</text></g>)}</svg>}
function Grid(){return <g stroke="#e5edf6">{Array.from({length:27},(_,i)=><line key={`v${i}`} x1={i*25} x2={i*25} y1="0" y2="500"/>)}{Array.from({length:21},(_,i)=><line key={`h${i}`} x1="0" x2="640" y1={i*25} y2={i*25}/>)}</g>}function TestCard({title,children}:{title:string;children:ReactNode}){return <section><h4>{title}</h4>{children}</section>}function VerticalMini(){return <svg viewBox="0 0 160 90"><line x1="80" x2="80" y1="5" y2="85" stroke="#64748b" strokeWidth="2"/>{[20,45,70].map((y,i)=><g key={y}><circle cx="80" cy={y} r="5" fill={["#ef4444","#1685f8","#22a35a"][i]}/><text x="90" y={y+3} fontSize="9">{["A","B","C"][i]}</text></g>)}</svg>}
function collinearityModel(a:Point,b:Point,c:Point){const ab={x:b.x-a.x,y:b.y-a.y},bc={x:c.x-b.x,y:c.y-b.y},ac={x:c.x-a.x,y:c.y-a.y},determinant=ab.x*ac.y-ab.y*ac.x,cross=determinant,area=Math.abs(determinant)/2,slopeAB=Math.abs(ab.x)<1e-9?Infinity:ab.y/ab.x,slopeBC=Math.abs(bc.x)<1e-9?Infinity:bc.y/bc.x,vertical=!Number.isFinite(slopeAB)&&!Number.isFinite(slopeBC),slopeAgreement=vertical||(Number.isFinite(slopeAB)&&Number.isFinite(slopeBC)&&Math.abs(slopeAB-slopeBC)<1e-8),zeroDet=Math.abs(determinant)<1e-8,zeroCross=Math.abs(cross)<1e-8,collinear=zeroDet&&zeroCross&&slopeAgreement,line=Math.hypot(ab.x,ab.y)>1e-8?{a:ab.y,b:-ab.x,c:-(ab.y*a.x-ab.x*a.y)}:Math.hypot(ac.x,ac.y)>1e-8?{a:ac.y,b:-ac.x,c:-(ac.y*a.x-ac.x*a.y)}:null;return{a,b,c,ab,bc,ac,determinant,cross,area,slopeAB,slopeBC,vertical,slopeAgreement,zeroDet,zeroCross,collinear,line}}function lineBounds(line:{a:number;b:number;c:number}){if(Math.abs(line.b)>.001)return[{x:-7,y:-(-7*line.a+line.c)/line.b},{x:7,y:-(7*line.a+line.c)/line.b}];const x=-line.c/line.a;return[{x,y:-6},{x,y:6}]}function showSlope(n:number){return Number.isFinite(n)?fmt(n):"undefined"}function finite(n:number){return Number.isFinite(n)?n.toFixed(4):"undefined"}function point(p:Point){return`(${fmt(p.x)}, ${fmt(p.y)})`}function fmt(n:number){return Number(n.toFixed(3)).toString()}function round(n:number){return Math.round(n*20)/20}function clamp(n:number,min:number,max:number){return Math.min(max,Math.max(min,Number.isFinite(n)?n:min))}
