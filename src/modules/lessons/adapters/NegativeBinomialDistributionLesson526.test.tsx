import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it,vi}from"vitest";import{lessonCatalog}from"../catalog/lessonCatalog";import ProbabilityLessonAdapter from"./ProbabilityLessonAdapter";
describe("Negative Binomial dedicated surface",()=>{it("routes lesson 526 to its rth-success lab",()=>{const lesson=lessonCatalog.find(item=>item.id===526)!;const html=renderToStaticMarkup(<ProbabilityLessonAdapter lesson={lesson} resetToken={0} onInteraction={vi.fn()}/>);expect(html).toContain('data-testid="probability-mockup-0489"');expect(html).toContain("Simulate until the r-th success");expect(html).toContain("0.0656");expect(html).toContain("38.889")})});
