import{describe,expect,it}from"vitest";import{normalDistributionAnalysis,normalPdf}from"./normalDistributionLessonModel";
describe("normal distribution lesson model",()=>{it("computes the target one-standard-deviation interval",()=>{const result=normalDistributionAnalysis(100,15,85,115,"between");expect(result.zLow).toBe(-1);expect(result.zHigh).toBe(1);expect(result.probability).toBeCloseTo(.68269,5);expect(result.leftTail).toBeCloseTo(.15866,5);expect(result.rightTail).toBeCloseTo(.15866,5)});it("supports all shading modes and a normalized density",()=>{const outside=normalDistributionAnalysis(0,1,-1,1,"outside");expect(outside.probability+outside.between).toBeCloseTo(1,10);expect(normalPdf(0,0,1)).toBeCloseTo(.39894,5)})});
