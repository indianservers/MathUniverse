import { useState } from "react";
import { CheckCircle2, RotateCcw, ToggleRight } from "lucide-react";
import type { LessonAdapterProps } from "../types";
import { maxValue, minValue, rangeAnswer, rangeDefault, rangeValue } from "./rangeLessonModel";
import "./RangeLesson474.css";
import { LessonTopicStudyBoard } from "../components/LessonTopicStudyBoard";
export default function RangeLesson474({ resetToken, onInteraction }: LessonAdapterProps) { return <RangeActivity key={resetToken} onInteraction={onInteraction} />; }
function RangeActivity({ onInteraction }: Pick<LessonAdapterProps, "onInteraction">) {
  const [values, setValues] = useState(rangeDefault); const [outlier, setOutlier] = useState(false); const [show, setShow] = useState(true); const [answer, setAnswer] = useState(""); const [checked, setChecked] = useState<boolean | null>(null); const data = outlier ? [...values, 20] : values; const min = minValue(data); const max = maxValue(data); const range = rangeValue(data);
  function update(index: number, raw: string) { const parsed = Number(raw); if (!Number.isFinite(parsed)) return; const next = [...values]; next[index] = Math.max(-20, Math.min(50, parsed)); setValues(next); setChecked(null); onInteraction(); }
  return <div className="range474" data-testid="statistics-mockup-0437" data-target-family="statistics-and-regression"><header><div><span className="range474-eyebrow">Data and Probability · Intermediate-Advanced</span><h2>Range</h2><p>Measure overall spread.</p></div><span className="range474-tags">English (English) · 6-10 min · Interactive</span></header><div className="range474-tabs"><b>Interact</b><span>Learn</span><span>Example</span><span>Formula</span><span>Practice</span></div><section className="range474-work"><div className="range474-title"><div><span className="range474-eyebrow">Observe → Manipulate</span><h3>Drag the dataset points or move the min/max handles.</h3></div><label><input type="checkbox" checked={show} onChange={event => { setShow(event.target.checked); onInteraction(); }} /> Show values</label><button type="button" onClick={() => { setValues(rangeDefault); setOutlier(false); onInteraction(); }}><RotateCcw size={14} /> Reset</button></div><div className="range474-numberline">{show && data.map((value, index) => <div key={index} className={value === min || value === max ? "extreme" : ""} style={{ left: `${Math.max(1, Math.min(99, (value + 2) / 14 * 100))}%` }}><b>{value}</b></div>)}<span className="range474-axis">-2&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;0&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;2&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;4&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;6&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;8&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;10&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;12</span></div><p>Drag points or handles to explore.</p><div className="range474-panels"><section><h3>Current dataset (n = {data.length})</h3><div className="range474-values">{values.map((value, index) => <input key={index} aria-label={`Range data value ${index + 1}`} value={value} onChange={event => update(index, event.target.value)} />)}</div></section><section className="range474-equation"><h3>Range = max − min</h3><strong>{max ?? "-"} − {min ?? "-"} = <b>{range ?? "-"}</b></strong><em>Range = {range ?? "-"}</em></section><section><h3>Outlier toggle</h3><p>Add a possible outlier to see how range is sensitive.</p><button className={outlier ? "on" : ""} type="button" onClick={() => { setOutlier(!outlier); onInteraction(); }}><ToggleRight size={18} /> {outlier ? "On" : "Off"}</button>{outlier && <p>Outlier value: <b>20</b></p>}</section></div></section><div className="range474-notes"><section><h3>Notice</h3><p>Range looks only at the smallest and largest values.</p><p>Moving a single extreme value changes the range a lot.</p><p>Values in between do not affect the range.</p></section><section><h3>Rule</h3><p>Range measures overall spread between the minimum and maximum values.</p><strong>Range = Maximum value − Minimum value</strong><p>Lower values → smaller range.<br />Extreme values → larger range.</p></section></div><p className="range474-insight"><b>Quick insight</b> Range is simple and intuitive, but it ignores most of the data and is sensitive to outliers.</p><section className="range474-misconception"><h3>Misconception guard</h3><p>Range uses only two values (min and max), not the middle or most data. Order does not matter: the range of 10, 2, 7 is the same as 2, 7, 10.</p></section><section className="range474-example"><div><h3>Example</h3><p>Data: 5, 9, 12, 15, 20</p><p><b>Step 1:</b> Min = 5, Max = 20</p><p><b>Step 2:</b> Range = 20 − 5 = <strong>15</strong></p></div><div><h3>See sensitivity (outlier effect)</h3><p>Without outlier: Min = 2, Max = 10, <b>Range = 8</b></p><p>With outlier = 20: Min = 2, Max = 20, <b>Range = 18</b></p></div></section><section className="range474-practice"><h3>Your turn: Find the range</h3><p>Data: 11, 4, 9, 7, 3</p><input aria-label="Range practice answer" placeholder="Your answer" value={answer} onChange={event => { setAnswer(event.target.value); setChecked(null); }} /><button type="button" onClick={() => { setChecked(rangeAnswer([11, 4, 9, 7, 3], answer)); onInteraction(); }}>{checked ? "Correct" : "Check answer"}</button>{checked === false && <span className="range474-bad">Expected 8</span>}{checked && <span className="range474-ok"><CheckCircle2 size={14} /> Correct: 8</span>}</section><footer><button type="button" onClick={() => { setValues(rangeDefault); setOutlier(false); setAnswer(""); setChecked(null); onInteraction(); }}><RotateCcw size={14} /> Reset lesson</button><span>Next: Quartiles and IQR →</span></footer>
      <LessonTopicStudyBoard lessonId={474} alwaysVisible onInteraction={onInteraction} />
</div>;
}
