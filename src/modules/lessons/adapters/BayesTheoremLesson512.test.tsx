import {renderToStaticMarkup} from "react-dom/server";import {describe,expect,it,vi} from "vitest";import {lessonCatalog} from "../catalog/lessonCatalog";import ProbabilityLessonAdapter from "./ProbabilityLessonAdapter";
describe("Bayes Theorem dedicated surface",()=>{it("routes lesson 512 to its diagnostic-test Bayes lab",()=>{const lesson=lessonCatalog.find(item=>item.id===512)!;const html=renderToStaticMarkup(<ProbabilityLessonAdapter lesson={lesson} resetToken={0} onInteraction={vi.fn()}/>);expect(html).toContain('data-testid="probability-mockup-0475"');expect(html).toContain("Probability tree");expect(html).toContain("True Positive");expect(html).toContain("50.0%");});});
