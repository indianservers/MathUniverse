import { formula, seed } from "./coreWorkspaceLessonFactory";

export const coreWorkspaceLesson035 = seed(35, "Piecewise Definitions", "piecewise-definitions", "Algebra and Dynamic Variables", "algebra", "symbolic_steps", "A piecewise definition uses different rules on different parts of the domain.", [["Piecewise", "Made from separate rules for separate cases."], ["Condition", "The test that chooses the active rule."]], formula("Piecewise rule", "f(x)=rule1 if condition1; rule2 if condition2", [["x", "input"], ["condition", "true or false test"]]), "Piecewise definitions model rules that change at boundaries. They matter in taxi fares, taxes, discounts, and grading.", "A piecewise function chooses one rule based on the input. Only the matching condition should be used. A common mistake is applying every rule at once.", "Test the input against each condition. Choose the one true case. Substitute into that rule only. Check boundary symbols carefully.", "The conditions split the input set, so each allowed input has the intended rule.", [["Taxi fare", "A base fee then distance fee can change by interval."], ["Electricity bill", "Rates may change after a usage limit."], ["Grades", "Letter grades depend on score ranges."]], ["ALL_RULES", "Using all pieces for one input.", "Choose only the rule whose condition is true."], ["If f(x)=x+1 for x<0 and x^2 for x>=0, find f(3).", ["3 satisfies x>=0.", "Use x^2.", "f(3)=9."], "9"], { prompt: "For f(x)=x+1 if x<0 and x^2 if x>=0, find f(3).", expected: "9", hint: "3 uses the x^2 rule.", kind: "numeric", factoryId: "algebra.piecewise-definitions" });
