export type ReleaseEvidence={id:string;area:string;passed:number;total:number;source:string;unsupportedClear?:number;unsupportedTotal?:number;criticalAccessibilityViolations?:number;unverifiedClaims?:number;scaffoldsPresentedComplete?:number};
export function generateProductScorecard(evidence:ReleaseEvidence[]){const passed=evidence.reduce((s,e)=>s+e.passed,0),total=evidence.reduce((s,e)=>s+e.total,0),unsupportedClear=evidence.reduce((s,e)=>s+(e.unsupportedClear??0),0),unsupportedTotal=evidence.reduce((s,e)=>s+(e.unsupportedTotal??0),0),critical=evidence.reduce((s,e)=>s+(e.criticalAccessibilityViolations??0),0),claims=evidence.reduce((s,e)=>s+(e.unverifiedClaims??0),0),scaffolds=evidence.reduce((s,e)=>s+(e.scaffoldsPresentedComplete??0),0);const metrics={supportedCorrectness:total?passed/total:0,unsupportedHonesty:unsupportedTotal?unsupportedClear/unsupportedTotal:1,criticalAccessibilityViolations:critical,unverifiedDirectClaims:claims,scaffoldsPresentedComplete:scaffolds};return{generatedAt:new Date(0).toISOString(),evidenceIds:evidence.map(e=>e.id),metrics,gates:{supportedCorrectness:{target:.999,pass:metrics.supportedCorrectness>=.999},unsupportedHonesty:{target:1,pass:metrics.unsupportedHonesty===1},criticalAccessibility:{target:0,pass:critical===0},unverifiedClaims:{target:0,pass:claims===0},scaffoldExposure:{target:0,pass:scaffolds===0}},releaseRecommendation:Object.values({correct:metrics.supportedCorrectness>=.999,honest:metrics.unsupportedHonesty===1,access:critical===0,claims:claims===0,scaffolds:scaffolds===0}).every(Boolean)?"ENGINEERING_GATES_PASSED_REVIEW_REQUIRED":"NOT_READY"};}

