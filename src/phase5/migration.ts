import type { Phase5DocumentState } from "./types";
export function migratePhase5State(value:unknown):{status:"VALID"|"MIGRATED"|"QUARANTINED";state?:Phase5DocumentState;diagnostics:string[]}{if(!value||typeof value!=="object")return{status:"QUARANTINED",diagnostics:["Phase 5 state is not an object."]};const candidate=value as Omit<Partial<Phase5DocumentState>,"schemaVersion">&{schemaVersion?:number;objects?:Phase5DocumentState["nodes"]};if(candidate.schemaVersion===1&&Array.isArray(candidate.nodes))return{status:"VALID",state:candidate as Phase5DocumentState,diagnostics:[]};if((candidate.schemaVersion===0||candidate.schemaVersion===undefined)&&Array.isArray(candidate.objects)){const state:Phase5DocumentState={schemaVersion:1,nodes:candidate.objects,linkedViews:candidate.linkedViews??[],camera:candidate.camera??{projection:"PERSPECTIVE",preset:"ISOMETRIC",position:{x:7,y:7,z:6},target:{x:0,y:0,z:0},zoom:1},accuracyProfile:candidate.accuracyProfile??"BALANCED",generation:candidate.generation??1};return{status:"MIGRATED",state,diagnostics:["Migrated legacy Phase 5 objects to schema version 1; mathematical definitions were preserved."]};}return{status:"QUARANTINED",diagnostics:["Unsupported Phase 5 schema or missing node collection."]};}
