import{renderToString}from"react-dom/server";import{describe,expect,it}from"vitest";import StatisticsEnhancementWorkbench from"./StatisticsEnhancementWorkbench";describe("Statistics enhancement workbench",()=>{it("renders 25 computed statistics tools and editable data",()=>{const html=renderToString(<StatisticsEnhancementWorkbench/>);expect(html.match(/data-enhancement-id="STAT-/g)).toHaveLength(25);expect(html.match(/type="number"/g)).toHaveLength(5);expect(html).toContain("Linked numeric dataset");expect(html).toContain("MCMC diagnostics");expect(html).toContain("Reproducible analysis report")})});
