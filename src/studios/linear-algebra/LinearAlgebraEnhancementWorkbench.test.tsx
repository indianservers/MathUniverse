import{renderToString}from"react-dom/server";import{describe,expect,it}from"vitest";import LinearAlgebraEnhancementWorkbench from"./LinearAlgebraEnhancementWorkbench";describe("Linear Algebra workbench",()=>{it("renders 25 computed tools and editable matrix",()=>{const html=renderToString(<LinearAlgebraEnhancementWorkbench/>);expect(html.match(/data-enhancement-id="LINALG-/g)).toHaveLength(25);expect(html.match(/type="number"/g)).toHaveLength(4);expect(html).toContain("Matrix CSV");expect(html).toContain("Gram–Schmidt");expect(html).toContain("Conditioning and error amplification")})});
