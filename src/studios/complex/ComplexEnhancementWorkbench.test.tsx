import{renderToString}from"react-dom/server";import{describe,expect,it}from"vitest";import ComplexEnhancementWorkbench from"./ComplexEnhancementWorkbench";describe("Complex workbench",()=>{it("renders 25 computed tools and five controls",()=>{const html=renderToString(<ComplexEnhancementWorkbench/>);expect(html.match(/data-enhancement-id="CPLX-/g)).toHaveLength(25);expect(html.match(/type="number"/g)).toHaveLength(5);expect(html).toContain("Cauchy–Riemann");expect(html).toContain("Mandelbrot and Julia");expect(html).toContain("Quaternion rotation bridge")})});
