import{renderToString}from"react-dom/server";import{describe,expect,it}from"vitest";import DiscreteEnhancementWorkbench from"./DiscreteEnhancementWorkbench";describe("Discrete workbench",()=>{it("renders 25 computed tools and five controls",()=>{const html=renderToString(<DiscreteEnhancementWorkbench/>);expect(html.match(/data-enhancement-id="DISC-/g)).toHaveLength(25);expect(html.match(/type="number"/g)).toHaveLength(4);expect(html).toContain("Input word");expect(html).toContain("Chinese Remainder Theorem");expect(html).toContain("CFG and Turing workbench")})});
