import{renderToString}from"react-dom/server";import{describe,expect,it}from"vitest";import GeometryEnhancementWorkbench from"./GeometryEnhancementWorkbench";describe("Geometry enhancement workbench",()=>{it("renders 25 computed geometry tools and five inputs",()=>{const html=renderToString(<GeometryEnhancementWorkbench/>);expect(html.match(/data-enhancement-id="GEO-/g)).toHaveLength(25);expect(html.match(/type="number"/g)).toHaveLength(5);expect(html).toContain("Compass-and-straightedge");expect(html).toContain("Non-Euclidean triangle");expect(html).toContain("Construction exchange")})});
