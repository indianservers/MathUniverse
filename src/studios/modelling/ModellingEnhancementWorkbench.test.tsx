import{renderToString}from"react-dom/server";import{MemoryRouter}from"react-router-dom";import{describe,expect,it}from"vitest";import ModellingEnhancementWorkbench from"./ModellingEnhancementWorkbench";describe("Modelling workbench",()=>{it("renders 25 computed tools and editable observations",()=>{const html=renderToString(<MemoryRouter><ModellingEnhancementWorkbench/></MemoryRouter>);expect(html.match(/data-enhancement-id="MODEL-/g)).toHaveLength(25);expect(html.match(/type="number"/g)).toHaveLength(5);expect(html).toContain("Observed x,y data");expect(html).toContain("Identifiability warning");expect(html).toContain("Model report export")})});
