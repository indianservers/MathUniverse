type MainNavigationProps = {
  mobileOpen?: boolean;
  onClose?: () => void;
};

/** The persistent left application menu has been retired. */
export default function MainNavigation(_props: MainNavigationProps) {
  return null;
}
