# Equation Grapher baseline and confirmed defects

Original source is GraphLessonAdapter.tsx (baseline copy retained here). Defaults in React state: test x=2, test y=3, trace x=1.5, guides=true, all slider bounds [-5,5], step 0.5. The static y control caption says 1 but actual input/state is 3; preserve the actual default. The stated equation is x²/9+y²/4=1. The existing lesson preset supplies coherent view x/y=[-5,5]; do not use its unrelated circle formula to replace this ellipse.

Confirmed defects: original fixed ellipse center (360,250) does not match axes at (360,292), and neither agrees with tick coordinates. An additional unlabeled diagonal and fixed “solution” point do not represent the specified ellipse. Test sliders do not affect the shape or substitution status. Status always says satisfies, including the actual initial (2,3), whose left side is 97/36. The sample (2,1) is incorrectly marked true: its left side is 25/36. The sample (4,1) remains false (73/36). Both sample coordinates must remain unchanged.

The old sample table returns abs(x²/9−1), which is neither y branch of the ellipse. Real branches are ±2 sqrt(1−x²/9), defined only when |x|≤3. The old trace uses an unrelated sine expression in pixel coordinates. Correct these confirmed defects while preserving original inputs, example coordinates and explanations. The old renderer remains in GraphLessonAdapter for later consumers.

Verification: all ellipse samples and every trace half-step satisfy the equation; both endpoints and absence of real points outside the domain are checked. All test slider values, axis points, off-curve examples, mouse/touch/keyboard, buttons and live output pass at three viewport sizes. A full 57-check regression passed 56 checks and caught a desktop drag layout shift. Moving the changing observation below the graph keeps plot coordinates stationary; all nine follow-up checks across 41, 40 and 39 pass. Light/dark screenshots and scoped lint evidence are retained.
